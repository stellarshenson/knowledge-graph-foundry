# Temporary quota-outage override (2026-07-06): Bedrock daily quotas exhausted
# (Sonnet, then Haiku). Reader routes through the local claude-cli engine.
# Embeddings stay on Bedrock Titan (separate quota, still live). Delete after.
llm:
  engine: claude-cli
  model: haiku
