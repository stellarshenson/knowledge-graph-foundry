# SOTA Promotions

Running ledger of adjudicated findings that materially advance the goal (SOTA vs published methods, production readiness). One line per promotion: date, source hypothesis, what it changes. Maintained during the backlog burn-down; consumed by the H113 head-to-head configuration and the release notes.

- 2026-07-06 **H53** - vector seed budget top_k 8 -> 16: +28% relative pure-seed evidence recall at trivial cost; the cheapest retrieval lever measured; ships to config before H113
- 2026-07-06 **H34/H67** - retrieval architecture validated end-to-end: 100% of gold evidence within 2 hops of production seeds, full-pipeline recall 1.000; traversal-depth engineering permanently closed
- 2026-07-06 **H37** - PPR removed from the quality path (99.4% contained in seeds+1-hop, zero exclusive gold): simplification promotion
- 2026-07-06 **H50** - drift detection formally benchmarked at matched false-alarm budget (CFAR refuted; CUSUM class characterized) - superseded on the ship decision by the 2026-07-07 H59/H120 entry below
- 2026-07-06 **H33 side-finding** - curing gate validated at scale: cure at doc 7 held 99.96% forward coverage over 474 unseen documents; DEF-3 concern closed for this corpus class
- 2026-07-06 **H47 final** - Heaps exponent b = 0.771 on the full wave: macro identity health confirmed; joins the panel as the ingest-axis marker
- 2026-07-06 **H107** - identity root cause re-mapped: 71% extraction variance; H119 (canonicalization) promoted to primary identity lever ahead of any resolver work
- 2026-07-06 **H91** - hub audit standardized: provenance purity (single-doc star + zero foreign-family neighbors), deterministic and cheap
- 2026-07-06 **H108** - the global proposition channel confirmed as the correct mechanism (not redundancy); no render change ships - a simplification promotion
- 2026-07-07 **H59/H120** - drift detector DECIDED for the SOTA config: the production all-3-consecutive criterion retained after losing two formal challenges by the CUSUM class (H50: matched detection at delta=0.30; H59: LLR variant false-alarms on the benign full-remap spike that persistence correctly ignores) - a validation promotion; no code change ships
- 2026-07-07 **R12 bake-off (H121/H122/H123/H125/H126/H143/H95)** - identity RANKING signal decided: Titan cosine unbeaten by seven challenger classes (cross-encoders -0.04, local bi-encoders -0.19, instruction-7B -0.34, NLI -0.26, field split ~0, structure -0.53, ensemble +0.004) - multi-directional confirmation that the fix is CALIBRATION (H129), not the scorer; plus one new shipped-able signal: the NLI contradiction channel as a defer-band sibling veto (90% sibling recall, H122 -> H128)
- 2026-07-07 **H51** - root cause added to the register: the PDF parse layer loses entity-name strings in 66.7% of documents (union of three parsers preserves +9.1 points over the project parser); table-heavy catalogues worst - multi-parser union pass is the queued lever
- 2026-07-07 **H54** - Bayesian resolver RETAINED (its demolition case failed): two rules reproduce only 74.4% of 4625 logged decisions, the description-likelihood term is load-bearing; combined with the R12 bake-off, the identity fix is now fully scoped as calibration-on-top-of-existing-machinery (H129)
- 2026-07-07 **H101** - the identity benchmark shipped: 298 adjudicated pairs with evidence strings, 100% sanity accuracy; finding: standing SAME_AS precision proxy is 14.2% - the model_code repair queue is now ordered and actionable
- 2026-07-07 **H106/H129/H128-residue** - the identity DECISION STACK decided and confirmed on adjudicated labels: isotonic-calibrated Titan cosine (ECE 0.050, 7-point support) + GLOBAL NLI contradiction veto (83% false-merge cut at 2.4% cost) + logistic arbitration over detectors (F1 0.811, false merges 39 -> 11); the Bayesian posterior is retired from the decision path (anti-informative from three angles: AUC 0.117 vs truth, negative ensemble weight, empty defer band) - ships to the resolver behind a config flag before the H113 head-to-head
- 2026-07-07 **H81/H83 (potentials family)** - the improvement gradient + stopping certificate the goal demanded, now measured: cost-benefit greedy over Phi_task (sufficiency - lambda x tokens) lifts direct-render recall 0.697 -> 0.879 within +10% tokens, single-edit gains decay monotonically, and the swap-stable fixed point shows 0% supermodular pairs - a computable "improvement still possible = false" certificate; the (dPhi_task, dPhi_MDL) sign pair classifies defect regions at 100% alignment - ships as the self-auditing foundry's edit selector and gap-ledger labeler
- 2026-07-07 **H86** - materialized-view machinery REFUTED as saturated (a simplification promotion): 91% of recalled evidence is on-seed, no legitimate view set cuts query cost at equal recall - no view subsystem ships, permanently closing that design avenue alongside H37's PPR removal
- 2026-07-07 **three-arm benchmark** - the goal's SOTA-vs-baseline measurement recorded ([`three-arm-benchmark-20260707T123450Z.json`](../reports/three-arm-benchmark-20260707T123450Z.json)): the promoted retrieval config (top_k 16) beats baseline +28.1% relative pure-seed recall on the honest fixed-graph arm (0.667 -> 0.854); the full-stack SOTA rebuild nets only +9.4% and sits BELOW baseline+lever because the local-extractor swap loses two probes to extraction variance (P08/P14) - re-confirming H107: extraction determinism (H119) is the binding constraint, and the rebuild is not a net win until it lands; measurement exposed DEF-4 (env-over-config precedence), fixed same day
- 2026-07-07 **R19 trio (H180/H181/H182)** - three query-time token-economy defaults proven shippable: 1-hop fanout cap k=5-10 query-similarity-ranked (zero recall loss at k=5 - fanout blowup structurally closed, though savings are modest since propositions carry 61.8% of render mass); miss detector top-seed-similarity ~0.67 + short-circuit abstention render (86.7% miss detection, 0% false-abstention, 95% token cut on the miss class - the gap-ledger abstention path made real); adaptive render budget B=60% ranked by query similarity (~40% token cut at zero recall loss)
- 2026-07-07 **H173** - deterministic verbatim proposition splitting at >300 tokens ships as an ingest-time operator: cuts PROP-attached materialization cost 3-4x (290->81, 278->108 tok), zero regressions across 137 fat-proposition gold occurrences, pure segmentation so P19-class paraphrase risk is zero
- 2026-07-07 **H171** - the improvement certificate's budget parameter becomes data-driven: the recall-vs-token knee (Kneedle, +5.6% on this graph, 10.7x post-knee marginal cost) replaces the fixed +10% bar; H81's near-miss re-adjudicated as a budget artifact, the gradient machinery stands validated end-to-end
- 2026-07-07 **H184** - the SOTA-graph retrieval deficit root-caused: 80% embedding-text DILUTION from local-extractor descriptions (carriers exist, rank lower) - H119 extraction canonicalization is now doubly promoted (identity AND retrieval both point at it) as the single highest-leverage pending fix
- 2026-07-07 **H186 (discipline)** - benchmark-instrument rule: probe-set expansion must be document-grounded, never graph-derived - graph-derived golds saturate at 99.2% and erase the k-lever; recorded as a standing methodology constraint for every future probe set
- 2026-07-07 **parser round (H146-H151)** - the ingest-fidelity parser decision: ship a TEXT-layer swap, not vision - pypdf recovers 93.7% of the 95 lost names at ~0.03 s/page CPU (union partner for pymupdf4llm), Docling 86.3% with a table-structure stage (backend candidate feeding H152/H153); MinerU2.5 inoperable here (VLM crash), olmOCR needs GPU without beating pypdf; the H51 name-recall harness becomes the standing acceptance test for any parser change (H150)
- 2026-07-07 **H147 dissolution** - the all-parser-absent mode family was a trademark-glyph artifact, not a parser gap: symbol-stripped matching rescues 69/676 absent names with zero parser change - the glyph normalization operator (H190) is the cheapest fidelity lever on the board and likely feeds the H107 surface-form variance
- 2026-07-07 **H172** - the entailment sufficiency scorer ships as THE benchmark instrument (mDeBERTa fp16, atomic-unit max-entailment, 1.8 ms/pair, 94% human agreement), retiring the fuzzy matcher; discipline rule: any conclusion whose candidate GENERATION used the fuzzy matcher is flagged for re-adjudication (H81 magnitude, H83 labels -> H192)
- 2026-07-07 **H81 promotion AMENDED** - the gradient promotion's magnitude claim (0.697 -> 0.879 within +10%) was instrument-inflated: the honest entailment-instrument gradient is +3pp at the knee on the fuzzy-derived menu; the certificate/submodularity machinery stands, the edit-selector ships only after H192 re-derives the menu under the honest instrument
- 2026-07-07 **H188** - probes-wide-h188.json (101 document-grounded golds, 85.1% clean, difficulty restored to 77% vs the invalid set's 99%) ADOPTED as the standing wide benchmark; discipline: absolute-recall CI bars are unmeetable at n~100 on a valid instrument - future bars use paired-delta CIs or n~500-1000; the 19% never-recalled residue is the measured corpus-coverage ceiling (H193 closes the loop to ingest fidelity)
- 2026-07-07 **H172 promotion CONTESTED (same day)** - an accidental independent replication with a balanced audit found the NLI scorer at 78% agreement with systematic value-identity failures; the fuzzy-matcher retirement STANDS (both runs agree it is broken) but the successor is now adjudicated by H194 (deterministic value-extraction comparator is the joint favorite); every recall number recorded today carries the instrument caveat until H194 closes
- 2026-07-07 **H188 adoption CAVEATED** - the duplicate run showed difficulty is rule-mix-dependent (feature golds saturate, catalogue/spec golds stay hard); the canonical wide set remains run A's catalogue-weighted 101 golds; harness discipline from run B: always retrieve at generous top_k and truncate - never query HNSW at exactly the evaluation k (H195 audits prior k-conclusions under the corrected convention)
- 2026-07-07 **H190** - the glyph normalization operator ships (parser post-process + resolver name-identity detector): 69/69 previously-absent names recovered, exactly 0 false conflations on the 2797-name vocabulary; scoped honestly - it does NOT touch the H107 word-level variance (that remains H119's job)
- 2026-07-07 **H153** - header carryover ships as the chunker fix: all 64 severed table rows re-associated at 0.258% token overhead, zero false header injections; the cheap floor beneath H152, which now must earn its place on extraction lift alone
- 2026-07-07 **H194** - the instrument contest SETTLED on a pre-registered blind balanced 60-pair bench: the deterministic value comparator is THE numeric/dimension sufficiency instrument (1.000 on its home strata, perfect present/absent look-alike separation), and the GPU-free router (comparator + word-overlap) ships as the harness default at 0.967 agreement (0.983 with NLI on the tiny feature stratum); fuzzy matcher AND NLI-as-primary both retired - run B's contest vindicated, the H172 run-A promotion superseded, every recall number's instrument caveat now resolvable by re-scoring under the router (H195)
- 2026-07-07 **H192** - the honest gradient EXISTS and the edit selector re-ships: evidence-search menu derivation (scan all graph units for the top-entailing carrier per missing gold) lifts recall 0.667 -> 0.970 at +0.28% tokens with monotone gains and swap-stability intact - H172's "greedy stalls" was menu contamination by the fuzzy matcher, not a missing gradient; supersedes the amended H81 magnitude, and the router delta puts the 33-gold ceiling at 33/33 (zero extraction-gap residue on the small set)
- 2026-07-07 **H193 (strategic claim)** - the wide-set coverage ceiling is INGEST-side under both instruments (retrieval-side refutation branch fired under neither): under the settled router 85.2% of misses are absent-from-graph and 87% of those map to known fidelity classes (18 catalogue-code table-cell losses, 2 glyph/format) - the fidelity fix queue is confirmed as the coverage lever and the post-fix H188 re-run is the program's end-to-end acceptance test; classification magnitudes await the H199 sealing run
- 2026-07-07 **H200 (methodology)** - variational hypothesis selection REFUTED and inverted on the campaign's own 109-verdict history: uniform-prior expected-information-gain is ANTI-predictive of realized hypothesis value (rho -0.185, top-10 overlap 0/10) because value concentrated in late refutations-that-earned-promotions on high-defect flanks (identity 0.75, fidelity 0.75 posterior defect rates) - judgment-ordered selection stands unchallenged, R20 closed, and the standing lesson for any future acquisition scheme: track persistence-against-refutation, not maximal prior uncertainty
- 2026-07-07 **H195(a)** - the convention audit CLEARS the record: zero verdict flips, 0.0 pt shift on every cell (HNSW is exact at 2798 embeddings), the top_k=16 lever survives in 6/6 scorer x convention cells; generous-top_k truncation promoted as harness LAW (costless here, protective at scale - the scale question is H203); the instrument caveat on this week's recall numbers is fully resolved
- 2026-07-07 **H195(b)** - probes-wide-v2-h195.json (219 document-grounded golds; catalogue+spec difficulty core recall@8 0.618 with an 8.2 pt k-lever, features quarantined as a saturating coverage tier) ships as the H113-ready benchmark; the consolidation-is-hard prediction refuted (corroboration golds saturate) - the hard multi-doc class is CONFLICT, registered H204
- 2026-07-07 **H196** - NLI is DROPPED from the harness instrument entirely: stop-word-filtered word-overlap dominates it on every stratum (prose 0.917 vs 0.667), the GPU scorer retires, and the sufficiency harness becomes fully deterministic and CPU-only; the residual prose/feature ceiling (~0.81, attribution ambiguity) belongs to the entity-scoped attribution check (H205), which no similarity scorer of any kind resolves
- 2026-07-07 **H197** - the graph snapshot fingerprint (counts + gold-carrier content hash + embedding digest, 2 ms) promoted as a standing recall-harness PRECONDITION: 60/60 label reproduction on matched state, guaranteed trip on mutation - cross-day bench comparisons are no longer silently corruptible by re-embedding
- 2026-07-07 **H199 (correction entry)** - the H193 "ceiling is ingest-side" promotion is SUSPENDED: the sealing census refuted the flip (unranked 62% / absent 38%), exposed a router blind spot on catalogue codes (61% of the wide set unmeasured - H206 registers the exact-identifier arm + device-seeded query rewrite), and revealed that recall itself swings 0.41-0.76 with the harness RENDER SURFACE on a provably unchanged graph - H207 (render-spec freeze + fingerprint-pinned 3-repeat census) now GATES every wide-set number including H113; solid residue: the 3 unmapped absents are extraction drops (not parse losses) and the H144 flag set extends to the AirStart-10 fact sheet and PrismaSmart brochure
- 2026-07-07 **H204 (provenance evidence)** - the both-values census is the R17 reconciliation layer's evidence base: 87% of cross-document value conflicts SILENTLY DROP one value (both kept 2/15, one kept 6/15, neither 7/15) - the graph preserves no provenance-tagged variants; the home-doc-restricted conflict derivation ships as the reusable conflict-probe harness; caveat honestly recorded - genuine conflicts are rare on this single-sourced corpus (3 physical, 1 same-unit), scale test registered H210
- 2026-07-07 **H205 (directional)** - foreign-device exclusion ships as an optional router attribution layer (+5.5 pt feature, prose 0.917, zero numeric regression by construction); full attribution bars await the shared-feature bench stratum (H209) - the residual errors are contestable labels, not scorer faults
- 2026-07-07 **H207** - the wide-set measurement crisis RESOLVED and the H193 suspension LIFTED: the census divergence was a cross-instance seed/render bug (DEF-5), not render-surface ambiguity - renders were byte-identical; the pinned harness (explicit retrieval driver + render fingerprint 96ab16d299fbbc71 + graph fingerprint, canonical CPU scorer) reproduces recall@64 0.762 at zero variance across 3 runs and is THE frozen wide-set recall definition for everything downstream including H113; FINAL classification: two-thirds of misses absent-from-graph - the coverage ceiling is ingest-side, the fidelity queue re-confirmed as the lever
- 2026-07-07 **H206(a)** - the exact-identifier arm joins the canonical router at ceiling performance (1.000 agreement, zero false credit on digit-perturbed look-alikes) - the catalogue-code blind spot H199 exposed is closed; (b)'s refuter routes the unranked-code residue to render/carrier-linkage policy (H211), not query rewriting
- 2026-07-07 **discipline (DEF-5 lesson)** - EVERY graph consumer, engine or notebook, pins its Neo4j instance explicitly and stamps both fingerprints before measuring; inheriting .env defaults in a multi-instance campaign has now caused two incidents (DEF-4 ingest, DEF-5 benchmark)
- 2026-07-07 **H158** - the identity stack is SHIPPED IN-ENGINE behind `resolution.identity_stack: v2`: E2E SAME_AS precision proxy 11.8% -> 52.2% (bar 50%) and false merges 30 -> 11, landing on the offline replay's number EXACTLY - the H106 decision stack survives real threading/arrival-order/config plumbing at zero wall-clock cost (NLI veto rides Bedrock latency); plus the versioned-calibration-artifact pattern (offline-fit, runtime-frozen coefficients) and decision-neutral resolution observability emits; default stays v1 until H212's clean-conditions recall re-run closes the extraction-noise confound (2-probe drop whose gold strings never entered the graph)
- 2026-07-07 **H208** - dimension-aware unit conversion (mass/length/pressure/flow, 2% tolerance) joins the canonical value comparator: both H204 unit-variant conflicts reclassify as consistent, zero verdict changes / zero false credits on both frozen benches; the null on clause (c) is honest - this corpus is single-unit-system, the conversion's value case is version-diverse corpora (H210's scale test)
- 2026-07-07 **H209 (refutation with routing)** - render-side attribution scoping is CLOSED as the path to full attribution bars: under a fair blind-adjudicated shared/exclusive taxonomy the exclusion variant tops at 0.833 exclusive / 0.907 combined and the residual errors are graph-side provenance gaps (owned features never attached to the owning node at ingest) that no query-time policy can retrofit; the H205 directional promotion (optional exclusion layer) stands; the attribution ceiling routes ingest-side via the H226 attachment census
- 2026-07-07 **H216** - the image census is the R21 round's frozen reference (2124 per-image records, 27 docs): the extraction-tool contest is DECIDED by measurement - Docling primary (raster figures + 226 structured tables), page-render for vector-drawn content (558 regions no other arm sees), pymupdf demoted (zero vector/table capture, decoration-inflated); 81.5% of images information-bearing but only 26% of documents carry image text absent from the text layer - image ingestion's value on this corpus class is NEW-content capture (photos, schematics, vector tables), not hidden-text recovery
- 2026-07-07 **H217 (strategic refutation)** - pixels claim ZERO of the benchmark coverage ceiling: 0/32 absent golds are pixel-only, all 16 H207 absents (every catalogue code included) are text-layer-present extraction/normalization drops, and the H191 "vision residue" dissolves as a benchmark cross-pairing artifact (H227 audits the sets); the fidelity queue's next lever is confirmed text-side - the sixth independent finding converging on H119, whose objective test is running
- 2026-07-07 **H227 (instrument integrity)** - the canonical probe sets are certified clean: 0/320 genuinely cross-paired golds across h188, wide-v2 and conflict-h204 - the H191 artifact was residue-set-local, the H186 document-grounding discipline held, and every wide-set difficulty number and absent-share classification stands un-inflated; the hardened H190-normalized presence matcher (split-glyph, range-separator, unit-glue handling) is the standing gold-presence checker
- 2026-07-07 **H226 (strategic census)** - feature-ownership attachment is a first-class ingest gap: 41.3% of document-stated ownerships are missing from the owning node, and the driver is NOT multi-product ambiguity (refuted - single-product manuals miss MORE, 47.8% vs 24.0%) but entity fragmentation: 61% of misses are sibling-fragment/orphan cases in exactly the naming-canonicalization territory H119 tests, 32% are true extraction recall (H228 registers the targeted-glean fix); attachment rate joins H214's post-rebuild measurement set
- 2026-07-07 **H203/H210 (scale legs)** - the wave-2 graph (952 docs, 4649 embeddings, fingerprinted) adjudicates two scale questions: generous-top_k truncation RE-CONFIRMED costless/protective as HNSW approximation first emerges (+0.83 pt @ k=16, up from exactly 0.0 at 2798 embeddings - the >= 3 pt magnitude stays open pending a true 10x graph); and H210 REFUTED - version diversity yields 4 genuine conflicts, not 40, because technical PROSE states no datasheet attributes: the conflict-reconciliation surface (R17) is a property of spec-bearing corpora, and its priority drops for prose corpus classes
- 2026-07-08 **H119 (strategic refutation)** - the campaign's most-converged-upon fix is DEAD as registered: the in-prompt canonicalization cure made variance worse (-9.7% vs the >= 50% bar) while losing 31% of stable entities; the objective test also re-sized the disease (production temp-0 extraction runs at 0.749 mean pairwise Jaccard distance, noise floor between identical configs 0.105) and corrected the mechanism theory - run-to-run churn is 76% CONTENT churn, not surface-form naming (H107's 71% surface figure described duplicate pairs, a different phenomenon); consequences: the resolver/v2-stack carries the identity burden (H212 gates the default), the cure class moves to R22 (voting already 35% at 100% retention on this experiment's own checkpoints, post-pass canonicalization, decoding levers), and every prior "H119 territory" routing is re-read as "extraction-fix territory, mechanism now under R22"
- 2026-07-08 **R22 CPU sweep (strategic inversion)** - seven-for-seven refutations that flip the extraction-fix doctrine: churn has no class structure (even product/code names churn at base rate, H230), chunking is exonerated (H235), surface-form rules do nothing (-1.5%, H234), dangling-relationship drops are correct (H238), sibling merges collect only a quarter of the attachment gap (H236) - and the two decisive results: single-run extraction samples only ~55% of the gold carriers the model CAN extract (union-of-5 = 100%, H231), while majority voting "stabilizes" precisely by discarding the low-frequency entities the benchmark needs (60% gold retention, H237). Extraction variance is UNDERSAMPLING, not noise: the cure is aggregation (union-of-K + the v2 resolver absorbing duplicates, registered H240), not stabilization - every stabilization cure tested (prompt H119, rules H234, voting H237) is now measured dead
- 2026-07-08 **H236 correction (same day)** - the sweep's one CONFIRMED contrarian: under the executor's corrected adjudication, sibling-fragment merges close 76.5% of sibling misses and **34.2% of the total attachment gap is resolver-side** - a third of H226's gap dissolves under the v2 identity default flip with zero new extraction work; H212's stakes rise again, and the R22 sweep line amends from seven-for-seven to six refutations + one confirmation
- 2026-07-08 **R23 CPU tier (mechanism narrowing)** - three free adjudications on the frozen H119 checkpoints narrow the undersampling mechanism by elimination: NOT a length/effort budget (coverage is density-flat and churned entities are not late-text casualties, H242 refuted), NOT prompt-sensitive (cross-prompt union 86.8% vs same-prompt 89.0% - run stochasticity is the ONLY measured diversity source, H244 refuted, which makes H249's determinism-kills-union contrarian the round's live wire), and the H247 slot-bias test is VOID with a load-bearing side-finding: the entire H119/R22/R23 variance corpus was measured in DISCOVERY mode (empty ontology) - post-cure churn is a registered unknown that H240 clause (b) checks. The one pass: the H248 lexical pre-scanner sees 87.3% of union-of-5 gold carriers for zero LLM tokens (misses are consumable-class names) - candidate priming earns its LLM clause, and the scanner promotes as a standing instrument. Selection - not budget, not format, not prompt - is where the other half of extraction dies; H246 enumerate-then-extract is now the primary cheaper-than-union candidate
- 2026-07-08 **H229 + determinism family closure (strategic)** - the temp-0 extraction variance is MODEL-INHERENT: serial/concurrent ratio 0.924 (H229 refuted - concurrency exonerated), seed pinning leaves JD 0.55 with zero identical reproductions (H232 closed), and guided-JSON decoding makes variance WORSE by 21% (H233 closed - choice churn, not format, fourth convergent selection-locus datum). Consequences: deterministic extraction mode DOES NOT EXIST on this stack (H249's determinism-kills-union contrarian refuted - blind union's fuel cannot be switched off, with H232's 6-call gate as the standing per-engine-upgrade canary), the citable quiet-conditions variance figure is ~0.60 mean pairwise JD, and the one surprise survives: a 7B-class model is ~2x LESS variable than the 120B (H239 gate GO in the unpredicted direction, recall-gated full test earned). The aggregation doctrine now stands on fully closed ground: variance is model-inherent, undersampling is the mechanism, union is the cure, and the resolver absorbs the froth
- 2026-07-08 **H157 (corpus transfer - the generalization verdict)** - the engine's first cross-corpus adjudication lands exactly on prediction: the LIFECYCLE machinery transfers (Good-Turing curing gate fires at doc 13 / 51 types on the paper corpus vs doc 4 / 12 types on the device corpus - later and wider on the denser genre, zero spurious recures, FSM held STABLE) while the IDENTITY CALIBRATION breaks (transfer ECE 0.2606 = 5.18x the H129 held-out reference, systematic under-confidence). The saving nuance: decisions transfer even though probabilities miscalibrate - blind labeling shows merge precision 0.95 / block correctness 1.00 / well-placed defers on the new corpus, with the one false merge being the predicted per-domain morphology surface (author names). Shipping consequences: isotonic constants are corpus-class-bound and must not ship across classes; a per-corpus self-calibration path (H142 lineage) is now an H198 requirement; per-domain deterministic detector surfaces (model-code on devices, author-name on papers) need per-corpus audit
- 2026-07-08 **H260 (instrument-to-lever promotion, ledger completeness)** - the GLiNER lexicon stage gets its own line (it previously rode the R23 tier entry, flagged by the H198 audit): gliner_multi-v2.1 with the 8 cured-ontology labels at confidence 0.30 sees 95.2% of union-of-5 gold-carrier spans in 7.6 s on the Ada card - a strict superset of the regex scanner it supersedes; ships as the audit/priming lexicon stage (per-document residue scalar to the gap ledger + candidate priming for the H250 extraction recipe)
- 2026-07-08 **H268 (design promotion)** - soft similarity links promote as the abstention-as-primitive design: defer-zone pairs materialize as posterior-weighted SIMILAR_TO edges (link, never merge) and the render may hop them - 64.7% of sibling-fragment attachment misses close on the frozen graph (bar 30%) at zero false-merge risk with the H212 attribution rule preserved by construction; this is the third resolver-side lever on the H226 attachment gap (with H236's sibling merges and the v2 default flip) and the only one that pays BEFORE a merge decision exists; wires via H198
- 2026-07-08 **H267/H269 (peer-mechanism refutations with competitive evidence)** - both harvested peer levers die on measurement and the deaths are informative: exact-normalized name matches are 4.5% of real merge decisions (pooled 1.3%) so graphify-class deterministic-ID identity silently fragments the other 95% - the strongest quantitative evidence yet that extraction variance, not lexical form, owns the duplicate population; and TrustGraph-class domain/range validation flags 48.8% of relationships but only 46.7% junk with 29.2% touching gold carriers - schema validation on free-extracted graphs is a recall killer wearing a precision uniform (H237's voting lesson, relationship axis); neither mechanism enters H198
- 2026-07-08 **H271/H274 (usage-coupling design decided)** - the R26 round resolves to exactly two shippers: demand-weighted repair allocation (1.7x-16.4x the uniform-allocation gain at every budget K=1..10, mechanism honestly named - demand CONCENTRATION finds the big under-served neighborhood, doc-level demand-gap correlation is null) and the external fingerprint-keyed answer cache (100% correct invalidation of evidence-changed answers, zero false invalidation, ZERO graph coupling - dominates the in-graph derived layer H273 at equal safety); the mess-proof answer to usage coupling is now measured, not argued: signals allocate repair, content lives outside the graph keyed to graph state; refuted alongside: recurrence gating (retains only 44% of the allocator's gain - one-off demand carries the recoverable mass), materialized render views (494% storage, hub blast radius 185 - joins H86's closed avenue), alias harvesting (probe vocabulary already matches graph names; production-telemetry territory)
- 2026-07-08 **R27 composition (H288/H282/H290): demote-don't-delete identity cleanup** - the agentic-escalation round resolves to a one-call design that achieves zero-true-loss false-merge removal BY CONSTRUCTION: the single-shot judge detects 98.0% of SAME_AS false merges at 513 tok/pair (~65k tokens per graph docket), judged-false edges are demoted to soft SIMILAR_TO links rather than deleted, and H288's measured merge-vs-soft-link equivalence (+0.0 pts render recall) makes the judge's only failure mode (over-splitting cross-type aliases, all caused by conflicting extractor type labels - H292 registered) costless; the expensive alternatives died on measurement - tool-agent 4x cost to trade detection for leniency (H283), fetch-then-judge theater on detection (H285), effort past K=3 mildly negative with late rounds re-arguing instead of re-fetching (H284), web territory 1.1% of the defer band (H281); the escalation ladder the round set out to build collapses to one call plus one edge rewrite
- 2026-07-08 **H279 (doctrine parameterization)** - the staleness law gets its first measured curve: evidence-staleness 9.5% on the single-wave window (low - content persistence not broadly unsafe here) but mutation concentrates 2.05x in query-touched neighborhoods - persisted answers rot fastest exactly where they would be reused most; the R26 signals-over-content doctrine (write back demand/aliases/gaps, treat answers/renders as invalidatable derived artifacts) stands as the usage-coupling design law pending the H273-vs-H274 head-to-head
- 2026-07-07 **H211** - the unranked residue is PRICED and closed as retrieval work: full recovery (5/8) costs 51.6x the H171 knee, so the residue goes to the gap ledger as priced-out abstention territory (H161 doctrine); three instruments promote - the firing-geometry enumeration (seed-rank x linkage-rule x carrier-size per gold) as the standard residue-pricing tool, the prop-val linkage rule (property value == entity name, 224-target map) as the only cheap non-edge recovery found, and the monotone-scorer regression lemma (appended render text cannot un-hit a pinned gold) for render-append policies; evidence note: half the residue is extraction misattribution (values/codes on the wrong entity) - the FOURTH independent measured finding pointing at H119 territory (H107 variance, H184 dilution, H207 absent codes, H211 misattribution); H119 itself remains UNTESTED and is adjudicated only by its own pre-registered 5x2 variance experiment
- 2026-07-08 **R23/R24 LLM tier close: H246 enumerate-then-extract is the extraction recipe (SLOT-2 decided)** - the undersampling campaign's remedy question resolves by frontier: one enumerate call ("list every entity name, terse") + one primed extract call reaches **97.1% of union-of-5 coverage at 1.05x single-pass cost** (gate 1.000 vs 0.447 baseline; coverage-per-token 0.925 vs union-of-2's 0.445) - the only operator with every registered clause measured and passed, and it strictly dominates blind union-of-2. Ships as the default extraction path in H198. Staged above it: **H258 mention-emission** (emit every surface MENTION, resolver does the dedup) posts the frontier's strict dominator - **100% coverage at 0.759x cost** (mention-only output is terse), 1.53x mention inflation - but its resolver-precision clause (<= 5 pt loss under inflation) is untestable offline and decides in H240(b)'s scratch-ingest arm; if it holds, mention-emission displaces H246 as default. Mechanism ledger from the same report: the model's silent internal dedup is the loss (H258 coverage 1.000 proves regeneration recovers everything it knows) while every instruction-based retrieval of the residue fails - complement pass zero-new (H243 refuted), GLiNER-cue priming +8.6 pts vs 20-pt bar (H248 partial-final), closed-set adjudication drops 8 pts of valid spans (H260 LLM clause refuted by 1.7 pts, instrument stands), union-demo distillation NEGATIVE -15.3 pts (H261 refuted - demos anchor, not calibrate); no token-level confidence channel exists (H245 refuted at 0.0066 nats vs 0.5 bar, churn emits at near-certainty) which kills logit harvesting (H257 refuted, 34.5% vs 85%, with the build qualifier); n-sampling economics unrealizable on this build (H251 refuted-on-stack: vLLM n>1 500s - harmony bug - emulation costs 3.05x and correlates; n>1 joins H232's per-upgrade canary). Doctrine line: the exhaustivity prior moves only by RESTRUCTURING the task, never by instructing, cueing, or demonstrating - four independent refutations of tell-the-model-what-to-do converge with the two restructure wins
- 2026-07-08 **H241 (SLOT-1 decided: v1 stays default, verdict PARTIAL)** - the paired local-engine identity A/B (both arms fresh, same engine/corpus/day, wipe between - the quota-free design H212's Bedrock ordeal forced) lands 2 of 3 registered clauses: recall tie **0.7708 = 0.7708 exactly** (non-regression PASS - the stack only vetoes merges, it does not touch extraction, precisely as predicted), precision proxy **0.611 vs 0.50 bar PASS** (v1 same-run: 0.083, i.e. 11 of 12 sampled merges false), but the raw-count false-merge clause misses at **1.57x fewer vs the 2x bar** (11 -> 7) - so the default flip is NOT approved and H158 is not lifted. The record keeps what the clause missed: v2 merged 1.94x the volume with 11x the bench-pair recall and a 3.1x lower per-merge false rate, with 355 NLI vetoes active. Shipped identity precision is the demote-don't-delete court instead (its docket IS v1's false-merge surface, 98.0% detection); v2 stays fully wired behind `resolution.identity_stack` with a gap-ledger entry (GAP-1) and a reopen condition: a NEW pre-registered decider, never a renegotiated bar. Discipline note: this is the campaign's bar-holding pattern applied to its own favorite - the evidence favors v2, the registered rule says no, the rule wins
- 2026-07-09 **H240b (SLOT-4 decided: composed ingest path validated, H246 enumerate CONFIRMED default, H258 mention-emission REFUTED as displacement)** - the two-arm scratch-ingest A/B on neo4j4 (both recipes with the v2 resolver, matched corpus of 27 kgf docs / 247 chunks, wipe between) resolves the SLOT-2 staging question and the SLOT-4 survival gate together. Survival PASS: both arms cured and produced coherent graphs end-to-end, the v2 resolver held false merges at exactly 13 in each arm (H158 band, no drowning) - the composed ingest path survives the full pipeline before the production rebuild. Recipe contest goes to enum decisively: recall **0.7292 enum vs 0.6667 mention** (-6.25 pt), SAME_AS precision **0.5517 enum vs 0.48 mention** (-7.2 pt, PAST the registered 5-pt tolerance), 17/24 vs 14/24 fully-covered probes. Mention's 1.17x entity inflation (5124 vs 4363) fragmented identity rather than lifting recall - the offline "strictly dominates 1.000 at 0.76x" did NOT survive composition, the exact silent-degradation-under-composition risk H198 clause (b) exists to catch. Per-probe: mention's absent-class help is real but narrow (wins P01/P03/P23 that enum missed) and net-negative (loses P20/P22/P24, half-loses P11/P14/P18); both recipes still fail P08/P09/P21 (persistent, recipe-independent). **H246 enumerate stays the shipped default extraction recipe; no flip.** Confounds recorded: both arms are v2-resolver while the shipped default is v1 (SLOT-1) so absolute recall/precision are v2-arm figures - the enum-vs-mention delta is clean but the comparison to the H241-v2 reference (0.7708) carries the v1/v2 + tranche-1-levers confound; the enum arm lost ~16 chunks to a client-timeout burst on the 8.4 MB catalog (retries=1) so enum recall is under-counted and its win over mention is conservative. Evidence: [h240b-enum-recall.json](../reports/h240b-enum-recall.json), [h240b-enum-stats.json](../reports/h240b-enum-stats.json), [h240b-mention-recall.json](../reports/h240b-mention-recall.json), [h240b-mention-stats.json](../reports/h240b-mention-stats.json)
- 2026-07-10 **H382 (design promotion: progressive context ships as a calibrated escalation gate)** - the composed-frontier recovery (0.9583 / 23-of-24, R34) ships as a sufficiency-gated ladder, not a flat render cost: the FREE H181 top-seed signal at a fitted threshold (~0.765; 2-fold cross-calibration, out-of-fold cuts stable at 0.7650/0.7644) escalates 25% of queries and delivers the full frontier at **+6.9% average context vs +27.8% static** - a 4x standing-cost reduction with zero recall loss, zero training, zero added LLM calls. Mechanism is distinct from the archived lineage on every cost axis: Adaptive-RAG needs a trained router, FLARE needs token logprobs frontier APIs do not expose, Self-RAG needs fine-tuning - KGF's gate is retrieval-side and pre-LLM. Gates on the ship: the threshold is pile-specific and H157 already proved calibration constants are corpus-class-bound (transfer ECE 5.18x) - the a-priori prior + per-corpus self-calibration + persisted calibration state is the R38 research question; the LLM self-report precision rider stays registered post-H363; the external SOTA claim is deferred to the public benchmark (#59), where the gate rides as the shipped read path
