# Claude Code Journal

This journal tracks substantive work on documents, diagrams, and documentation content.

**Note**: Entries 1-55 have been archived to [JOURNAL_ARCHIVE.md](JOURNAL_ARCHIVE.md).

---

56. **Task - Generative curing advisory** (v0.1.12): Implemented LLM-assisted cure/re-cure decision layer as opt-in replacement for metric-based curing checks<br>
    **Result**: Created `kg_builder_cli/curing/generative.py` with `CureDecision` and `RecureDecision` Pydantic response models, `llm_should_cure()` and `llm_should_recure()` functions following `type_clustering.py` pattern (sync instructor+litellm, temp 0.0, max_retries 2, broad except returning None). Cure prompt presents per-document metrics timeline table (JSD, entropy delta, type accumulation rate, Chao1 coverage, Heaps beta, new types) plus sorted type list with frequencies and domain intent, with explicit structured decision rules (CURE if type coverage + JSD<0.05 for 2+ docs + <=1 new type + Chao1>0.7; BLOCK if missing categories + TAR>1.0 + Chao1<0.5). Re-cure prompt presents remap rate timeline with structured rules (RE-CURE if 3+ missing types + sustained 30%+ remap; DISMISS if synonyms + rate dropped below 20%). Wired into `cli.py._ingest_fluid()` with new check order: force_cure -> generative LLM -> metric fallback (`_check_metric_curing` helper extracted) -> safety net (always). Cured phase drift: generative LLM -> `re_cure_on_drift` boolean fallback. Added `generative_curing: bool = False` to `CuringConfig` and `defaults.py`. Updated `docs/DESIGN.md` section 5.8 with full generative curing documentation including check order tables, timeline format, and cost model. Updated `DESIGN_ASSUMPTIONS.md` with generative curing checklist items. Created `tests/test_generative_curing.py` with 10 tests (model validation, mocked LLM calls, failure fallback, config defaults). All 226 tests pass, lint clean.

57. **Task - Graph query tool + early stopping** (v0.1.12): Added two-phase graph query tool for generative curing and LLM escalation, plus patience-based early stopping<br>
    **Result**: Created `kg_builder_cli/curing/graph_query.py` with `GraphQueryRequest`/`GraphQueryResult` models and two backends: `query_fluid()` for in-memory FluidAccumulator (entity counts, relationship patterns, entity search) and `query_graph()` for Neo4j Cypher. Modified `curing/generative.py` with two-phase structured output - first call returns `CureProbe` (includes `needs_query` flag), second call with enriched context produces `CureDecision`. Query only honoured when metrics genuinely ambiguous (JSD 0.02-0.08 or Chao1 0.5-0.75). Added `_is_ambiguous_for_query()` gate function. Extended `CuringDetector` with `record_llm_vote()` / `patience_exceeded()` for early stopping - tracks consecutive LLM cure votes, threshold computed as `max(3, int(max_fluid_documents * generative_patience))` so it scales with corpus size. Default `generative_patience=0.4` means 0.4 * 20 = 8 consecutive document-level cure votes at default settings, with floor of 3. Modified `type_resolver.py._llm_resolve()` with graph query enrichment when top-2 posterior gap < 0.15 and entity has description. Added `neo4j_config` parameter to `BayesianTypeResolver` and wired through `unstructured.py`. Updated `cli.py._ingest_fluid()` to pass accumulator/buffer to `llm_should_cure()`, record LLM votes, check patience, and pass `neo4j_config` to `llm_should_recure()`. Config: `generative_patience=0.4` (fraction), `generative_max_tool_calls=2` in `CuringConfig` and `defaults.py`. Updated `docs/DESIGN.md` with graph query tool, early stopping, and revised check order. Updated `docs/DESIGN_ASSUMPTIONS.md` with 4 new checklist items. Created `tests/test_graph_query.py` (9 tests), extended `tests/test_generative_curing.py` (added two-phase, scaling, query-not-honoured, early stopping tests), extended `tests/test_curing_detector.py` (added patience tracking tests). All 252 tests pass, lint clean.

58. **Task - README feature update** (v0.1.12): Rewrote `README.md` with comprehensive feature list, usage examples, config reference, and technology stack<br>
    **Result**: Replaced template-generated README with project-specific content modeled on `nb_venv_kernels/README.md` style - features as bullet list with bold lead, install section, usage examples showing key CLI patterns, YAML config sample with extensive inline comments for every setting, technology stack organized by category, makefile targets, and project directory structure. Covers all major capabilities: adaptive ontology buffer, any-format seeding, structured + unstructured support, multi-signal entity resolution, fluid schema curing, generative curing advisory with graph query tool, drift detection, dual indexing, extraction modes, interactive + batch modes, TUI, and instructor/pydantic enforcement.

59. **Task - Embedding fallback for cross-type resolution** (v0.1.12): Added cosine similarity on Titan v2 embeddings as secondary gate when bag-of-words description similarity fails in cross-type entity merging<br>
    **Result**: Modified `_resolve_cross_type()` in `kg_builder_cli/extraction/resolution.py` to accept `embedding_threshold` parameter (default 0.75). When description Jaccard similarity falls below threshold, the function now checks embedding cosine similarity as fallback before blocking the merge - if both entities have embeddings and cosine >= 0.75, the merge proceeds with an "via embedding" log message. When neither gate passes or embeddings are absent, the original blocking behavior is preserved with enhanced log messages showing both similarity scores. Added `cross_type_embedding_threshold: float = 0.75` to `ExtractConfig` in `kg_builder_cli/types/config.py`. Updated `resolve_entities()` signature to accept and pass through the new parameter. Updated `FluidAccumulator.consolidate()` in `kg_builder_cli/curing/accumulator.py` to wire `config.cross_type_embedding_threshold` into `resolve_entities()`. All 271 tests pass.

60. **Task - Specification value/unit enrichment** (v0.1.12): Added post-extraction parser to backfill structured `value` and `unit` properties on Specification entities - REVERTED<br>
    **Result**: Created `spec_parser.py` with regex-based value/unit extraction. Reverted after review - regex parsing increases model variance and fights against the LLM. Correct approach is improving the extraction prompt so the LLM interprets spec properties at extraction time. Deleted `kg_builder_cli/extraction/spec_parser.py`, `tests/test_spec_parser.py`, and removed integration from `unstructured.py`.

61. **Task - Design doc update for H1/H2 fixes** (v0.1.12): Updated DESIGN.md and failure-hypothesis.md with architectural fixes from benchmark analysis<br>
    **Result**: Documented two systemic fixes in `docs/DESIGN.md`: (1) H1 skip double type enforcement - `FluidAccumulator.consolidate()` now accepts `skip_type_enforcement=True` when type clustering has already run, eliminating the double enforcement bug that created 12 singleton types; (2) H2 embedding fallback for cross-type resolution - added paragraph documenting the cosine similarity secondary gate in `_resolve_cross_type()` when description Jaccard fails, with `cross_type_embedding_threshold` config. Updated the curing event flow diagram to show `skip_type_enforcement=True`. Updated `docs/failure-hypothesis.md` to mark H3 regex approach as rejected, documenting the correct LLM-based interpretation approach instead. All 271 tests pass, lint clean.

62. **Task - Intent-driven discovery model** (v0.1.12): Implemented 5-change plan to address systematic benchmark regression (v08 88% -> v17 72%) by providing domain compass during early ontology discovery<br>
    **Result**: The core insight was that the pipeline had no domain compass during the critical first documents when the ontology is being discovered. The intent prompt doesn't seed a fixed schema - it seeds the use case, so the LLM discovers the right ontology naturally. Five structural changes were made to `config.yml`, `types/config.py`, `ontology/buffer.py`, `cli.py`, and `docs/DESIGN.md`, with test fixes in `test_ontology_buffer.py`. (1) Added `ontology_buffer.intent` to `.kg-builder/config.yml` with a CPAP domain use-case statement that propagates to all extraction and curing prompts from doc 1, giving every LLM call domain context for type discovery. (2) Lowered `min_frequency_to_emerge` from 2 to 1, eliminating the 2-document blind spot where docs 1-2 always used the free prompt because no types had reached freq>=2 yet - now all discovered types appear as emerging suggestions in the constrained prompt immediately after first discovery. (3) Changed relationship type filtering in `ontology/buffer.py` `snapshot()` to use `emerge_threshold` (1) instead of `threshold` (2) - relationship types like HAS_SPECIFICATION appear in the constrained prompt after doc 1 instead of requiring two independent discoveries. (4) Removed both `prune_low_frequency_types()` calls before LLM clustering in `cli.py` - pruning at 0.5% of total entities was deleting semantically important low-frequency types (e.g. Mode with 5 entities out of 1400 fell below the 7-entity threshold) that the clustering LLM needs to make good merge decisions. (5) Lowered `min_chao1_coverage` from 0.7 to 0.5 - the previous threshold was never reached in 10-doc corpora (max observed: 0.394), causing curing to only trigger via the safety net at doc 10 rather than through the heuristic detector. Benchmark v18 scored **82% hybrid** (deterministic 60/63=95%, generative 3.4/5.0), up from v17's 71% (55/63=87%, 2.8/5.0). Dimension-by-dimension analysis of why the score improved by 10 points: **document_coverage** jumped from 75% to 100% because v17 had 27 Document nodes (duplicates from safety-net curing at doc 10 where doc/chunk nodes were created both during accumulation and during the flush) while v18 has exactly 10 docs with proper NEXT_CHUNK chains - early curing at doc 4 means 6 documents load directly in cured phase with clean doc/chunk creation. **product_coverage** held at 100% deterministic but generative rose from 3/5 to 4/5 because the intent prompt steered extraction toward product-manufacturer linkages from the first document rather than discovering them incidentally. **cross_doc_resolution** improved from 67% to 83% deterministic - the "same-type name duplicates <= 2" check now passes because removing pre-clustering pruning preserved the type information the clustering LLM needed to consolidate variants like "CPAP Mode" and "Auto Mode" into canonical forms, and singleton types dropped from 12 to <3 because those 12 singletons were artefacts of the old pruning step deleting types the enforcement layer then couldn't match. **relationship_patterns** improved from 75% to 88% - HAS_SPECIFICATION went from 0 to passing because relationship types now enter the constrained prompt at emerge_threshold=1 instead of requiring freq>=2, so doc 2 onwards could produce HAS_SPECIFICATION relationships. SUPPORTS_MODE regressed from 1 to 0, which needs investigation - the relationship type may have been absorbed into a different type during clustering. **type_distribution** went from 80% to 100% - the singleton check now passes (was 12, now <3) because the old prune-before-cluster pipeline created orphan types that couldn't be merged, and all 8 expected ontology types are present with clean distributions (Specification:207, Component:179, Feature:170 as top three). **spec_extraction** generative improved from 2/5 to 4/5 - v17's LLM judge found "many specs lack concrete numeric values" and "all products are null indicating no linkage between specs and actual CPAP devices" while v18's judge found "good coverage across multiple CPAP products with concrete numeric values and proper units" including pressure ranges (4-20 cmH2O), dimensions (116x205x150 mm), sound levels (26.6 dBA) - the intent prompt's mention of "product specifications" and "component architecture" directly guided the LLM to extract structured spec data with values and units. **query_answerability** improved from 80% to 90% deterministic - the DreamStation pressure range query now passes because HAS_SPECIFICATION relationships connect products to their specs. Remaining failures: cross-type duplicates at 56 (threshold <20) where entities like Humidifier, Tube, Mask appear under both Component and Accessory types - this is a genuine semantic ambiguity in the domain that the current cross-type resolution gates (description Jaccard 0.3, embedding cosine 0.75) are too conservative to resolve. SUPPORTS_MODE=0 needs investigation. Generative cross_doc_resolution and query_answerability both scored 2/5 due to the cross-type duplication making the graph appear fragmented to the LLM judge.

63. **Task - Bayesian cross-type dedup v19** (v0.4.18): Replaced description-similarity gate with Bayesian posterior model for cross-type entity resolution, fixed SUPPORTS_MODE benchmark query, renamed DESIGN.md to KGB_DESIGN.md<br>
    **Result**: Replaced the binary description-similarity gate in `_resolve_cross_type()` (`resolution.py`) with a multi-signal Bayesian posterior combining name identity prior (0.8 for identical normalized names), description Jaccard likelihood ratio (floored at 0.3), embedding cosine likelihood ratio, and source chunk co-occurrence. Merge threshold configurable via `cross_type_merge_threshold` (default 0.6) in `ExtractConfig`. Updated `resolve_against_graph()` in `loader.py` with same Bayesian logic plus case-insensitive Neo4j query using `toLower()`. Fixed SUPPORTS_MODE benchmark query - the ontology has no `Mode` entity type, modes are extracted as `Feature`/`Setting`, so changed the query to match entities with "mode" in their name and type in `['feature', 'setting']` with expanded relationship type list. Updated tests from `TestCrossTypeDescriptionGate` to `TestCrossTypeBayesianMerge` with computed posterior values in docstrings, added `test_cross_type_shared_chunks_boosts_merge`. Renamed `docs/DESIGN.md` to `docs/KGB_DESIGN.md` with header "Knowledge Graph Builder - CLI and Advanced Ingestion Engine v19", updated all references in `INSTRUCTIONS.md`, `README.md`, `TASKS.md`, `DESIGN_ASSUMPTIONS.md`. Benchmark v19: **84% hybrid** (deterministic 61/63=97%, generative 3.6/5.0) - cross-type duplicates dropped from 56 to 39, SUPPORTS_MODE now passes, cross_doc_resolution LLM improved from 2/5 to 3/5. Remaining failures: cross-type duplicates=39 (threshold <20), SleepStyle modes not linked.

64. **Task - Benchmark spec rewrite for answer quality** (v0.4.18): Rewrote `docs/benchmarks/multidoc_benchmark_spec.md` to be explicit and extensive about answer quality evaluation and failure diagnosis<br>
    **Result**: Comprehensive rewrite of the benchmark specification to make generative scoring failures diagnosable. Each generative dimension now documents: the exact Cypher query context the judge receives, quality criteria the judge evaluates, a scoring rubric, and known failure modes from v19 analysis. The critical finding is the **deterministic-generative disconnect** in query_answerability (9/10 deterministic vs 2/5 generative) caused by judge context truncation - `left(b.description, 60)` truncating specs, missing `value`/`unit` properties, single-query LIMIT 200 missing data. The spec now prescribes multi-query judges for complex dimensions, property inclusion rules (always include value/unit for specs, always use `type(r)` not `r.type` for APOC relationships), and a three-step failure diagnosis protocol: (1) check extraction produced the data, (2) check entity properties are populated, (3) check judge query returns those properties. Added v19 failure analysis section with root cause per dimension and priority fixes for v20 benchmark implementation. Added "Generative Judge Design Principles" section covering context completeness, property inclusion rules, multi-query judges, and failure diagnosis methodology. Documented APOC native relationship type behavior (`type(r)` works, `r.type` is NULL) and SUPPORTS_MODE gotcha (no Mode entity type - modes are Feature/Setting).

65. **Task - Benchmark v20 judge context expansion** (v0.4.18): Implemented benchmark spec fixes - expanded generative judge queries, ran v20 benchmark<br>
    **Result**: Benchmark v20 scored **88% hybrid** (deterministic 61/63=97%, generative 4.0/5.0), up from v19's 84% (3.6/5.0). No pipeline changes - purely judge context expansion. Three changes to `tests/benchmark_multidoc.py`: (1) replaced single-query query_answerability with three-query approach - product/org outgoing relationships with 200-char descriptions (was 60), dedicated specification detail query returning `value`/`unit` properties, and mode/standard coverage query for Feature/Setting entities with "mode" in name plus Standard entities with compliance links. The `run_generative_scoring()` function now supports `queries` (list of `{label, query}` dicts) alongside single `query`, concatenating results with labeled sections. (2) Enriched cross_doc_resolution prompt with entity descriptions (120-char) alongside duplicate counts, plus guidance distinguishing genuine type ambiguity from resolution failures. (3) Increased JSON truncation from 8000 to 12000 chars and max_tokens from 200 to 300. Removed obsolete `docs/benchmarks/benchmark_spec.md` (single-doc spec superseded by multidoc). Dimension-by-dimension: query_answerability jumped **2/5 -> 4/5** (the judge now sees spec values, mode entities, and standard compliance data), all other dimensions held steady (manufacturer 5/5, product 4/5, cross_doc 3/5, spec 4/5). Cross-type duplicates at 52 this run (variance from non-deterministic extraction, was 39 in v19). The v20 result validates the v19 diagnosis: the 84% ceiling was a benchmark measurement problem, not a graph quality problem. Remaining bottleneck is cross_doc_resolution at 3/5 generative - genuine type ambiguity (Component vs Accessory) that the Bayesian model correctly leaves unmerged. Next steps: address residual cross-type duplicates via H5b (post-curing graph consolidation) or H5c (type coercion in extraction prompt) to push toward 90%+.

66. **Task - H5f deferred cross-type dedup** (v0.4.21): Implemented deferred cross-type dedup with evidence accumulation and LLM escalation, plus graceful auth failure handling<br>
    **Result**: Created `kg_builder_cli/extraction/deferred_dedup.py` with `DeferredDedupBuffer` implementing three-zone cross-type resolution - merge (posterior >= 0.6), defer (0.4-0.6 ambiguous zone), block (< 0.4). Buffer accumulates evidence across documents during fluid phase: co-occurrence counts, relationship target overlap (topology Jaccard signal), and description similarity. At curing time `resolve_all()` recomputes posterior with accumulated boosts (topology 0.15, co-occurrence 0.1, description 0.1) and optionally escalates to LLM via instructor+litellm for still-ambiguous pairs. Modified `resolution.py` `_resolve_cross_type()` for three-zone logic with optional `deferred_buffer` parameter. Wired into `FluidAccumulator` - `add_result()` calls `update_evidence()`, `consolidate()` runs `resolve_all()` after entity resolution (Step 6) then applies merge decisions. Added `LLMAuthError` in `extract.py` that detects 403/401/credential errors and propagates immediately instead of silently returning empty results - CLI catches it with clear exit message. Reworked `defaults.py` with comprehensive documentation for every setting (purpose, value rationale, valid range). Removed default provider/model/embedding_model - user must explicitly configure. Enabled `use_embeddings`, `bayesian_resolution`, `deferred_dedup` by default for best extraction. Added `deferred_dedup`, `deferred_dedup_ambiguous_lower`, `deferred_dedup_llm_escalation` to `ExtractConfig`. Updated KGB_DESIGN.md section 6.7 and failure-hypothesis.md with H5f. Bumped to v0.4.21. All 272 tests pass, lint clean. Benchmark v21: **88% hybrid** (61/63 deterministic, 4.0/5.0 generative), 964 entities, 3290 rels, 48 cross-type duplicates (down from 52 in v20, target was <30). Deferred buffer showed minimal impact - only 4 fewer cross-type duplicates.

67. **Task - Rename KGB to KGF and central config** (v0.4.22): Renamed project from Knowledge Graph Builder to Knowledge Graph Forge, created central `config.py` module, restructured config/settings packages<br>
    **Result**: Comprehensive rename across the codebase. Renamed `docs/KGB_DESIGN.md` to `docs/KGF_DESIGN.md` (git mv). Changed CLI command from `kg` to `kgf` in pyproject.toml entrypoint and typer app. Renamed runtime config directory from `.kg-builder/` to `.kgf/` in all code and documentation. Updated display name "Knowledge Graph Builder" to "Knowledge Graph Forge" in README, INSTRUCTIONS, KGF_DESIGN, __init__.py, pyproject.toml description, cli.py help. Updated all `kg ingest`/`kg query`/`kg update`/`kg init` references to `kgf` prefix across README.md, INSTRUCTIONS.md, TASKS.md, KGF_DESIGN.md, DESIGN_ASSUMPTIONS.md, benchmark spec, fact_repository.md. Fixed long-standing `KGB_KGB_DESIGN.md` typo in INSTRUCTIONS.md. Restructured module layout: created `kg_builder_cli/config.py` as central module configuration restoring the copier-data-science template pattern (logger setup with loguru/tqdm, `APP_NAME`/`APP_SHORT`/`APP_VERSION` from importlib.metadata, project paths `PROJ_ROOT`/`DATA_DIR`/etc., KGF runtime path functions with `CONFIG_DIR_NAME` constant). Renamed `kg_builder_cli/config/` package to `kg_builder_cli/settings/` for app settings (YAML loading, defaults, config models) - clear separation between module config (config.py) and app settings (settings/). Removed tracked `.kg-builder/` directory (runtime artifact). Updated `docs/failure-hypothesis.md` with v21 post-mortem: H5f delivered +0 hybrid (52->48 dupes), diagnosed cross-type duplicates as ontological ambiguity not evidential, proposed H5g ontological type merging for v22. Removed `%` from hybrid scores throughout (unbounded score, not percentage). All 272 tests pass, `kgf --help` works.

68. **Task - H5g design documentation** (v0.4.23): Updated `docs/KGF_DESIGN.md` and `docs/failure-hypothesis.md` with H5g ontology-enriched type resolution design<br>
    **Result**: Added three new subsections to Section 5.4 (YAML Ontology Format): Type Hierarchy (H5g) defining `type_hierarchy` with parent-child subsumption for structural disambiguation (Part -> Component/Accessory, Behavior -> Feature/Setting), Resolution Intent and Resolution Guide (H5g) establishing two-layer prose guidance where `resolution_intent` is the user-authored immutable prior and `resolution_guide` is the system-evolved section that accumulates learned disambiguation rules across runs via `resolution_guide_evolution` config flag (default true), and Persisted Type Exemplars (H5g) for auto-flushed concrete disambiguation examples per type. Updated Section 5.5 Buffer Contents with hierarchy, resolution intent, resolution guide, and type exemplar tracking. Updated Buffer Flush to include all new sections with resolution guide evolution loop. Updated Section 6.7 Entity Resolution with hierarchy safety net - sibling types under shared parent auto-merge before Bayesian posterior, multi-facet entities under different parents preserve both labels. Updated Section 9.1 Loading with multi-label `apoc.create.addLabels(n, row.labels)` Cypher replacing embedded type labels. Updated failure-hypothesis.md H5g from "proposed" to "implementing" with unified design combining all three layers (hierarchy, intent/guide, multi-label) plus exemplar injection. Naming convention: `resolution_intent` (immutable prior), `resolution_guide` (evolved rules), `resolution_guide_evolution` (config flag).

69. **Task - Token bucket rate limiter** (v0.4.23): Added optional token bucket rate limiter for LLM API calls to prevent Bedrock rate limiting during ingestion<br>
    **Result**: Created `kg_builder_cli/extraction/rate_limiter.py` with `TokenBucketRateLimiter` class implementing RFC 4697 token bucket algorithm using `threading.Lock` and `time.monotonic()` from stdlib (~45 lines, zero external dependencies). Thread-safe `acquire()` blocks until a token is available; when `rate=0` (default) it's a no-op with zero overhead. Added `RateLimitConfig` model (`requests_per_second: float = 1.0`) to `types/config.py` and optional `rate_limit: Optional[RateLimitConfig] = None` field on `LLMConfig` - when omitted from YAML, no rate limiting applied. Updated `extraction/unstructured.py` to create limiter before ThreadPoolExecutor and wrap `extract_chunk` calls via `_throttled_extract` helper - all futures submitted immediately, each worker blocks on `acquire()` before the LLM call. Added commented-out documentation in `settings/defaults.py`. Updated `docs/KGF_DESIGN.md` Concurrency Model section with rate limiter description and rewrote LLM Retry Strategy section to document the three-layer protection model (token bucket pre-call, litellm 429 backoff, instructor validation retry). Added `rm -f .kgf/ontology.yml` cleanup step to `docs/benchmarks/multidoc_benchmark_spec.md` execution procedure. Created `tests/test_rate_limiter.py` with 5 tests covering no-op (rate=0), rate enforcement timing, thread safety (4 threads x 5 acquires), and burst capacity. All 298 tests pass. Config usage: add `llm.rate_limit.requests_per_second: 1.0` to `.kgf/config.yml`.

70. **Task - Project housekeeping and Docker DNS** (v0.4.23): Gitignore updates, example config, Neo4j Docker DNS setup, benchmark procedure cleanup<br>
    **Result**: Added `/.kgf/`, `/tmp/`, `/logs/` to `.gitignore` to keep runtime artefacts out of the source tree. Created `docs/config.yml.example` with annotated defaults for all configuration sections (llm, extract, ontology_buffer, curing, neo4j) including optional `rate_limit` block. Recreated Neo4j container on `jupyterhub_network` with `--network-alias neo4j` instead of the previous `kg-net` network - Docker's embedded DNS (`127.0.0.11`) now resolves the `neo4j` hostname automatically without `/etc/hosts` workarounds. Removed stale `/etc/hosts` entry (`172.20.0.2 neo4j`) that was overriding Docker DNS and pointing to a dead IP. Removed unused `kg-net` Docker network. Updated `docs/benchmarks/multidoc_benchmark_spec.md`: replaced WSL2 `/etc/hosts` workaround section with proper Docker DNS approach using `--network-alias`, added network discovery step (`docker inspect $(hostname)`), added container recreation instructions, changed benchmark logs to write to `tmp/` instead of `logs/` in project root.

71. **Task - H5g design update and rename** (v0.4.23): Updated KGF_DESIGN.md and failure-hypothesis.md for self-evolving buffer design, renamed project to Knowledge Graph Foundry<br>
    **Result**: Updated seven sections in `docs/KGF_DESIGN.md`: Type Hierarchy (H5g) rewritten from auto-merge to hierarchy-as-Bayesian-signal model where siblings get boosted prior (0.95) instead of bypassing the model; added self-evolving buffer description with encounter-based trigger sequence; Resolution Intent/Guide evolution triggers changed from post-ingestion to inline `_maybe_evolve()` with `GUIDE_EVOLUTION_MIN_ENCOUNTERS` replacing document-count threshold; Entity Resolution (6.7) Layer 3 rewritten to describe hierarchy as prior boost not override; Buffer Contents updated for self-evolving mechanism and `_evolved_guide_keys` dedup set; Buffer Flush updated for inline evolution; added two Design Lessons (Section 21) - "Document-count evolution threshold" and "Hierarchy auto-merge without evidence gates". Updated `docs/failure-hypothesis.md`: added v23 to historical context table (83%, hierarchy completely inert), added v23 post-mortem under H5g documenting three root causes (evolution timing, document-based threshold, auto-merge bypass) and the v24 fix, added v23->v24 priority matrix. Renamed project from Knowledge Graph Forge to Knowledge Graph Foundry across 8 files: README.md, pyproject.toml, docs/config.yml.example, kg_builder_cli/__init__.py, kg_builder_cli/config.py, kg_builder_cli/types/config.py, kg_builder_cli/cli.py, INSTRUCTIONS.md, docs/KGF_DESIGN.md title.

72. **Task - H5g hierarchy fix implementation** (v0.4.23): Implemented code changes for H5g hierarchy resolution fix addressing three root causes from v23 benchmark<br>
    **Result**: Replaced `GUIDE_EVOLUTION_MIN_DOCUMENTS` with `GUIDE_EVOLUTION_MIN_ENCOUNTERS` in `settings/defaults.py` - evolution now triggers on encounter frequency (sum of type_counts // 2) instead of document set cardinality, fixing the single-document multi-encounter blind spot. Added `_maybe_evolve()` method to `OntologyBuffer` that auto-triggers `evolve_type_hierarchy()` and `evolve_resolution_guide()` from within `record_cross_type_stats()` when any type pair reaches the encounter threshold - this moves evolution from post-ingestion only to inline during stats recording. The method aggregates encounters per `(type_a, type_b)` pair across all entity names, matching `evolve_type_hierarchy()`'s aggregation logic. Added `_evolved_guide_keys` set for guide rule dedup so repeated `_maybe_evolve()` calls don't produce duplicate rules. Guide evolution remains gated by `config.resolution_guide_evolution` (default true). In `resolution.py`, replaced the hierarchy Cases 1-2 (auto-merge siblings, auto-merge multi-facet) with hierarchy-boosted Bayesian: sibling types get `prior=0.95`, cross-parent types use standard name-based prior, but ALL go through `_cross_type_posterior()` with the `prior_override` parameter. Four-zone decision: merge at threshold, multi-facet at moderate evidence (>= 0.3), defer, or block. Added detailed logging with prior/posterior values for every hierarchy resolution decision. In `cli.py`, removed the post-ingestion `evolve_type_hierarchy()`/`evolve_resolution_guide()` calls gated by `resolution_guide_evolution`, replaced with `_maybe_evolve()` before flush. Updated 5 existing tests and added 6 new tests covering auto-evolution trigger, single-document encounters, below-threshold blocking, hierarchy-boosted Bayesian (not auto-merge), guide dedup, and evolution idempotency. All 304 tests pass, lint clean.

73. **Task - Unify resolution_intent source** (v0.4.23): Established single source of truth for `resolution_intent` (config.yml) and `resolution_guide` (ontology.yml)<br>
    **Result**: Renamed `OntologyBufferConfig.intent` to `resolution_intent` across config model, defaults, and all callers (cli.py, unstructured.py, generative curing). Removed `_resolution_intent` field from `OntologyBuffer` - it no longer loads from or flushes to ontology YAML. Added `resolution_intent: str = ""` parameter to `buffer.snapshot()` so callers pass the config value at snapshot time, flowing through `OntologyState` to extraction prompts. Removed `_INTENT_PREFIX` and `intent` parameter from `build_extraction_prompt()` - the intent now renders via `_build_resolution_guidance_block()` as "Use case context" (replacing the previous "Type disambiguation rules (domain expert)" label). This eliminates the double-injection where intent appeared both as a prefix and inside the resolution guidance block. Updated `docs/config.yml.example`, `.kgf/config.yml`, and `tmp/.kgf/config.yml` key names. Updated `docs/KGF_DESIGN.md` across 6 sections to document the single-source-of-truth design: `resolution_intent` lives exclusively in `config.yml`, `resolution_guide` lives exclusively in `ontology.yml`, flush no longer writes `resolution_intent`. Updated test fixtures in `test_h5g_hierarchy.py` (removed `resolution_intent` from YAML fixtures, assert NOT present in flushed output, pass intent via `snapshot()` parameter) and `test_prompts.py` (pass intent through `OntologyState` instead of function parameter). All 304 tests pass, lint clean.

74. **Task - Design version bump to v24** (v0.4.23): Updated KGF_DESIGN.md version from v22 to v24, started v24 benchmark run<br>
    **Result**: Bumped `docs/KGF_DESIGN.md` title from v22 to v24 to reflect the H5g hierarchy-boosted Bayesian resolution and resolution_intent/guide single-source-of-truth changes. v24 benchmark ingestion attempted but hit Bedrock 429 rate limits after multiple runs exhausted API quota. Graph wiped for clean re-run, pending rate limit reset.

75. **Task - Non-TTY logging fix** (v0.4.23): Fixed loguru silent output in non-TTY environments (pipes, background jobs, CI)<br>
    **Result**: Root cause: loguru's tqdm.write sink (`lambda msg: tqdm.write(msg, end="", file=sys.stdout)`) buffers indefinitely when stdout is not a TTY because tqdm doesn't flush in non-interactive mode, producing zero log output for piped or backgrounded processes. Fixed `kg_builder_cli/config.py` logger setup to detect TTY mode at startup via `sys.stdout.isatty()`. TTY mode uses tqdm.write sink with `colorize=True` for progress bar compatibility. Non-TTY mode falls back to `sys.stderr` with `colorize=False` which auto-flushes on every write and produces clean uncoloured logs suitable for file capture and CI environments.

76. **Task - Event-driven architecture** (v0.4.24): Implemented typed event system using blinker signals across all pipeline phases, replacing implicit coupling with observable signal dispatch<br>
    **Result**: Created `kg_builder_cli/events/` package with 41 named blinker Signal instances across 10 categories (pipeline phase, extraction, resolution decisions, ontology evolution, stability metrics, curing detection, LLM invocations, blocked/skipped, buffer mutations, loading), ~50 Pydantic event payload models, and three handler layers (default INFO-level for key events, verbose DEBUG-level for full payloads, event accumulator for post-run analysis). Added `--verbose / -v` CLI flag to `kgf ingest`. Refactored `resolve_entities()` to return `ResolutionResult(NamedTuple)` instead of setting `_last_id_map`/`_last_cross_type_stats` module-level attributes - updated all callers (`unstructured.py`, `accumulator.py`) and all tests (`test_resolution.py`, `test_h5g_hierarchy.py`, `test_fluid_accumulator.py`). Wired signal emissions into: `resolution.py` (cross_type_decision per decision, entity_resolution_completed), `accumulator.py` (consolidation_completed), `buffer.py` (ontology_evolved, ontology_flushed), `metrics.py` (stability_metrics_recorded with ~20 metrics), `detector.py` (curing_condition_blocked per sub-condition, curing_check_performed, drift_detected/drift_check_passed), `extract.py` (llm_call_started/completed/failed), `loader.py` (graph_load_started/completed), `cli.py` (ingestion_started/completed). Updated `docs/KGF_DESIGN.md` Section 14 Event Architecture with event bus architecture description, handler registration layers, and curing detection flow example. Added `blinker>=1.7` to dependencies. All 319 tests pass, lint clean.

77. **Task - Streaming event log** (v0.4.24): Replaced batch-at-end event accumulator with streaming JSONL writer and added `--processing-log PATH` for loguru output redirection<br>
    **Result**: Redesigned event accumulator from in-memory list with post-run dump to streaming file writer that appends each event as a JSONL line the moment it's emitted, enabling real-time `tail -f` monitoring during long ingestion runs. Changed `--event-log` from boolean toggle to `Path | None` specifying the output file. Added `--processing-log PATH` which reconfigures loguru at startup to write to file instead of stdout/stderr, replacing shell `| tee` pattern. Removed `_dump_event_log()` function entirely - replaced with `_stream_event()` handler that opens file in append mode per event. Updated `register_event_accumulator()` to accept `Path` parameter and truncate file at run start. Replaced `get_event_log()` list API with `get_event_log_count()` counter. Config fallback `extract.event_log: true` still works with default path `.kgf/events.log`. Updated benchmark spec, design doc, and all tests. Benchmark v25 confirmed 88% hybrid score (60/63 deterministic, 4.0/5.0 generative) - zero regression from event architecture. 446 events streamed across 10 categories: 159 LLM calls, 78 cross-type decisions, 10 stability metrics, 11 entity resolutions. Graph: 1023 entities, 3885 rels, 159 chunks. All 319 tests pass, lint clean.

78. **Task - Buffer signal wiring** (v0.4.24): Wired 9 missing `.send()` calls in `kg_builder_cli/ontology/buffer.py` for blinker event signals<br>
    **Result**: Added signal emissions for all buffer-related events defined in `events/signals.py`: `ontology_signals_accumulated` in `accumulate_from_result()` after accumulation and exemplar update; `buffer_exemplars_updated` per-type at the end of `_update_exemplars()` by tracking which types were modified during the call; `buffer_types_pruned` in `prune_low_frequency_types()` after pruning completes; `buffer_snapshot_taken` in `snapshot()` before the return; `hierarchy_skipped` in `evolve_type_hierarchy()` when pair is below encounter threshold; `hierarchy_pair_qualifying` when pair reaches threshold; `hierarchy_evolved` for both new parent creation (is_new=True) and existing parent child addition (is_new=False); `guide_rule_skipped` at all 4 continue paths in `evolve_resolution_guide()` with distinct reasons (already_generated, below_encounter_threshold, below_dominance_threshold, no_shared_parent); `guide_rule_generated` when a new disambiguation rule is produced. All imports follow the existing lazy pattern (inside methods, not at module top level). All 319 tests pass, lint clean.

79. **Task - Resolution/deferred signal wiring** (v0.4.24): Wired 5 missing `.send()` calls in `resolution.py` and `deferred_dedup.py` for cross-type resolution and deferred dedup signals<br>
    **Result**: In `resolution.py`: added `_compute_lr_values()` helper near `_cross_type_posterior()` that extracts individual likelihood ratios (desc, emb, cooc) from the same computation the posterior function uses internally, added `lr_emb` parameter to `_emit_decision` so all three LR values propagate to `CrossTypeDecision` events instead of hardcoded zeros, updated all 7 `_emit_decision` call sites (4 hierarchy path, 3 non-hierarchy) to compute and pass actual LR values via `_compute_lr_values()`, added `merge_blocked` signal at both blocked paths (hierarchy and non-hierarchy) and `cross_type_pair_deferred` signal at both deferred paths (hierarchy and non-hierarchy). In `deferred_dedup.py`: added module-level imports for `evt_signals` and `etypes`, added `deferred_evidence_updated` emission inside the `update_evidence()` per-pair loop gated by `pair.last_seen_doc == doc_index` so only pairs touched by the current document emit, added `deferred_pair_skipped` in the block branch of `resolve_all()`, added `deferred_resolution_completed` at the end of `resolve_all()` with merge/block/llm_escalation counts. All 319 tests pass, lint clean.

80. **Task - Wire extraction and curing signals** (v0.4.24): Added `.send()` calls for document_extraction_started/completed, curing_triggered, phase_transition, patience_exceeded signals, and fixed ingestion_completed totals<br>
    **Result**: Wired 5 previously-defined-but-unconnected blinker signals into business logic. In `extraction/unstructured.py`: added `doc_index`, `total_docs`, and `phase` parameters to `ingest_document()`, emitting `document_extraction_started` before ThreadPoolExecutor and `document_extraction_completed` before final return. Added `total_docs` field to both `DocumentExtractionStarted` and `DocumentExtractionCompleted` event models in `events/types.py`. In `cli.py`: updated all three `ingest_document()` call sites (`_ingest_direct` with phase="direct", `_ingest_fluid` cured phase with phase="cured", fluid phase with phase="fluid") to pass `doc_index=i`, `total_docs=len(files)`, and `phase`. Added `cure_trigger` tracking variable set to "force_cure", "generative", "metric", "patience", or "safety_net" depending on which curing condition fires. Emitting `curing_triggered` at the top of the `if should_cure:` block, `patience_exceeded` when early stopping fires, and `phase_transition` after `cured = True`. Changed `_ingest_direct` and `_ingest_fluid` return types to `tuple[int, int]` returning `(total_entities, total_rels)` accumulated across all documents, fixing `ingestion_completed` signal which previously hardcoded zeros. All 319 tests pass, lint clean.

81. **Task - Remaining signal wiring and tests** (v0.4.24): Wired remaining signals (accumulator, merge_validation, metrics, loader, validation), expanded test suite, and updated design docs<br>
    **Result**: Completed v25 post-benchmark forensic signal wiring plan. Wired remaining `.send()` calls: `type_enforcement_applied` in `curing/accumulator.py`, `merge_validation_failed` in `curing/merge_validation.py`, `stability_snapshot` (every 5 docs) in `curing/metrics.py`, `graph_resolution_applied` in `loading/loader.py`, `graph_validated` in `loading/validation.py`. Added `[X/Y] processing:` document progression log format to all 3 ingestion paths in `cli.py`. Expanded `tests/test_events.py` from 6 model tests to 30+ covering all event types, added `TestHandlerRegistration` with 13-signal default handler coverage check and verbose/accumulator all-signal coverage tests, added `TestSignalCoverage` with case-insensitive signal-to-model mapping (handles LLM acronym casing), added 11 `TestSignalWiring` tests exercising real business logic with captured signal emissions. Fixed blinker weak reference issue in tests by storing strong handler references in `self._handlers` list. Updated `docs/KGF_DESIGN.md` Event Architecture section with accurate signal counts and handler details. Added Event Architecture verification section to `docs/DESIGN_ASSUMPTIONS.md` with 13 checklist items. All 356 tests pass (37 new), lint clean (0 new issues).

82. **Task - Multipass project sanitation** (v0.4.24): Post-v25 codebase cleanup removing dead code, stale docs, and template artifacts across 4 passes<br>
    **Result**: Pass 1 (dead code): deleted `tests/test_data.py` (copier template example), removed 6 unused conftest fixtures (`sample_config`, `sample_segments`, `sample_chunks`, `sample_relationships`, `sample_ontology`, `near_duplicate_entities`) and their 11 unused imports, keeping only `sample_entities` used by `test_dedup.py`. Removed 8 dead `ExtractConfig` fields (`chunking_strategy`, `extraction_mode`, `evidence_spans`, `source_frequency`, `describe_images`, `vision_model`, `subgraph_splitting`, `rolling_context_window`) from `types/config.py` and corresponding defaults.py entries - confirmed zero references in business logic. Removed dead if/pass block in `type_resolver.py` and dead TODO comment in `defaults.py`. Pass 2 skipped (Strands/TUI deps kept as planned features). Pass 3 (stale docs): fixed `min_frequency_to_emerge` default 2->1 and `generative_patience` 5->0.4 in DESIGN_ASSUMPTIONS.md, bumped KGF_DESIGN.md title v24->v25, added "Unimplemented Design (tracked)" section with 10 checkbox items tracking features in design doc but not yet in code (Strands SDK, TUI, kgf query/update, structured ingestion, query/update pipelines, agent memory, interactive mode). Pass 4 (consistency): verified README.md and INSTRUCTIONS.md have no stale references. Net: -207 lines, 352 tests pass (was 356, -4 from deleted template tests).

83. **Task - Dead code pass 2** (v0.4.25): Second dead code sweep targeting stale defaults, unused regex, config mismatches, and dead dependencies<br>
    **Result**: Removed `_ENV_VAR_PATTERN` regex from `settings/loader.py` (superseded by `_ENV_VAR_DEFAULT_PATTERN` on line 33, zero references). Fixed `min_chao1_coverage` mismatch in `settings/defaults.py` from 0.7 to 0.5 to match the `CuringConfig` Pydantic default changed in journal entry 62. Added two missing `defaults.py` entries for active config fields that had no DEFAULTS key (preventing config.yml override): `hierarchy_resolution: True` in the `extract` section and `resolution_guide_evolution: True` in `ontology_buffer` section. Removed three unused dependencies from `pyproject.toml`: `langchain-text-splitters`, `langchain-neo4j`, and `chonkie` - all with zero imports in the codebase. Kept `embedding_provider` in defaults.py (planned bedrock usage) and copier-data-science template constants in `config.py` (APP_VERSION, PROJ_ROOT, path constants) per user direction. Net: -424 lines (mostly uv.lock shrinkage from removed deps). All 352 tests pass, lint clean.

84. **Task - Benchmark v26 and forensics tooling** (v0.4.25): Ran v26 benchmark confirming dead code pass was regression-free, added implementation delta tracking to benchmark reports, created agentic forensics command<br>
    **Result**: Benchmark v26 scored 88% hybrid (60/63 deterministic, 4.0/5.0 generative) with 1004 entities, 2789 rels, 0 orphans - identical to v25, validating the dependency removal and defaults fixes. Added `_get_changes_since_last_benchmark()` to `tests/benchmark_multidoc.py` which finds the previous benchmark file via glob, looks up the git commit that introduced it via `git log --diff-filter=A`, and appends a "Changes Since vNN" section with the full commit log delta to the benchmark markdown. Created `.claude/commands/forensics.md` - an agentic forensics command (`/forensics`) that runs a four-phase investigation: (1) scope extraction from benchmark report failures, (2) JSONL event log analysis via `jq` with lead-following (blocked pair posteriors, missing entity search, curing timeline), (3) Neo4j graph state queries for failures invisible in events (extraction gaps, graph-load merge misses), (4) classification into four failure classes (extraction gap, resolution miss, graph-load merge miss, benchmark measurement) with distinct fix domains. Updated `docs/benchmarks/multidoc_benchmark_spec.md` to describe forensics as an agent-driven investigation process referencing the command, replacing the previous static jq query list. v26 forensic findings: 15 blocked cross-type pairs at posteriors 0.45-0.60 (top offenders: `integrated heated humidifier` 3 pairs, Section/Interface trio), OSA 4x from name variant normalization miss, SleepStyle modes and iBreeze->Resvent both extraction gaps.

85. **Task - README rewrite** (v0.4.25): Rewrote README.md to lead with plain-language explanation of what KGF does<br>
    **Result**: Replaced feature-adjective-heavy opening with direct statement: "reads your documents and builds a knowledge graph in Neo4j." Added "What It Does" section walking through the 5-step pipeline (parse, chunk, extract, resolve, load) in plain terms. Added "How Ontology Discovery Works" explaining free extraction and curing without jargon. Moved entity resolution details (Bayesian cross-type, hierarchy-boosted, deferred dedup, LLM escalation), curing/convergence stats (JSD, entropy, Chao1), and event system into a "Technical Details" section. Updated technology stack to reflect current deps (removed langchain-text-splitters, langchain-neo4j, chonkie; added blinker). Updated project organization to match current module structure (config.py, settings/, events/).

86. **Task - Strands agent integration design** (v0.4.25): Replaced aspirational agent-centric architecture in KGF_DESIGN.md Section 2 with evidence-based hybrid design<br>
    **Result**: Rewrote Section 2 opening, introduction paragraph, and key facts to describe the hybrid architecture where pipeline functions execute as direct `litellm+instructor` single-shot calls with Strands agents engaged only at specific escalation points. Replaced the generic 4-tool registry (`py-repl`, `neo4j-mcp`, `neo4j-driver`, `file-ops`) and 3-agent model (Ingest/Query/Update agents) with targeted 3-tool registry (`query_graph`, `query_buffer`, `inspect_metrics`) and 3+1 agent model (Curing, Resolution, Query agents). Added four new subsections: LLM Engagement Matrix (7-site decision table with Keep/Convert classification and per-site rationale from benchmark forensics), Agent Conversion Criteria (4 conditions: low volume, external evidence, hardcoded branching, measurable quality gap), Targeted Agents (per-agent tools, structured output, invocation point, what it replaces), and Agent Tool Registry (3 read-only tools wrapping existing functions). Updated architecture diagram from generic CLI->Agent->Tools flow to hybrid Pipeline->Agents(escalation) with dashed escalation edges. Updated module structure section: `tools/` now lists `query_graph.py`, `query_buffer.py`, `inspect_metrics.py`; `agents/` now lists `curing.py`, `resolution.py`, `query.py` with "(escalation only)" qualifier.

87. **Task - Hybrid architecture integrity fixes** (v0.4.25): Fixed downstream sections in KGF_DESIGN.md still using agent-centric language after Section 2 hybrid rewrite<br>
    **Result**: Applied 5 planned fixes plus 8 additional stale references across `docs/KGF_DESIGN.md`. Fix 1: rewrote Section 2.1 opening from "the agent is always present in all modes" to "Strands agents are present at interactive checkpoints and escalation points". Fix 2: Section 3 header now distinguishes `kgf ingest`/`kgf update` (pipeline-direct with agent checkpoints) from `kgf query` (agent-native). Fix 3: Section 3 `kgf ingest` workflow rewritten from "the ingest agent runs the full pipeline" to "the ingest workflow executes the pipeline directly" with agent involvement only at checkpoint/escalation points. Fix 4: replaced all stale tool references - `py-repl` (4 instances) replaced with "programmatic parse" or "pipeline" descriptions, `neo4j-mcp` (5 instances) replaced with `query_graph` tool or direct Neo4J descriptions, `neo4j-driver` (3 instances) replaced with direct pipeline execution, `file-ops` (2 instances) replaced with direct config reading. Fix 5: `kgf update graph` and `kgf update schema` workflows rewritten from agent-centric to pipeline-direct with agent escalation at migration plan approval. Also fixed "ingest agent" and "update agent" references to "ingest pipeline/workflow" and "update workflow". Verification: zero grep hits for `py-repl`, `neo4j-driver`, `neo4j-mcp`, `file-ops`, "ingest agent", "update agent", or "the agent runs the full pipeline".

88. **Task - LLM cassette mock system** (v0.4.25): Built cassette-based LLM recording/replay infrastructure for deterministic testing without live API calls<br>
    **Result**: Created `tests/llm_cassette.py` with three components: `Cassette`/`CassetteCall` data structures for JSON serialization of pre-recorded LLM responses, `ReplayClient` that mimics both instructor call patterns (`client.chat.completions.create()` and `client.create()`) replaying responses in sequence with method validation and exhaustion detection, and `RecordingClient` that wraps a real instructor client to capture calls for cassette creation. The replay client uses a model registry that lazily loads all 6 response model classes (`ExtractionResponse`, `CureProbe`, `CureDecision`, `RecureDecision`, `TypeClusteringResult`, `SchemaSignals`) for Pydantic deserialization. Created 8 pre-recorded cassette files in `tests/fixtures/llm_cassettes/` covering extraction (single chunk CPAP product, multi-chunk AirSense session), curing (cure approved, cure blocked, two-phase with graph query), re-curing (dismiss drift, trigger re-cure), schema signals (CPAP domain), and type clustering (synonym merging). Created `tests/test_llm_cassettes.py` with 16 tests across 5 test classes: infrastructure (load/replay, exhaustion, method mismatch, save/reload round-trip, multi-call), extraction (single chunk with entity/relationship validation, multi-chunk cross-reference, schema signals), curing (cure approved/blocked/two-phase, recure dismissed/triggered), type clustering, and recording client (create and chat.completions round-trips). Added `load_cassette()` helper to `conftest.py`. All 368 tests pass (352 existing + 16 new), zero regressions.

89. **Task - Cassette system hardening** (v0.4.25): Fixed 4 issues in LLM cassette system - inline model registration, import fragility, thread safety, and coverage gaps<br>
    **Result**: Fix 1: registered `_TypeChoice` and `_CrossTypeMergeDecision` as Pydantic models in `tests/llm_cassette.py` mirroring the inline definitions in `type_resolver.py:362` and `deferred_dedup.py:447`, added both to `_ensure_registry()` so cassettes for these call sites deserialize correctly. Fix 2: created `tests/__init__.py` to make tests a proper package, replaced all `sys.path.insert()` hacks in `test_llm_cassettes.py` and `conftest.py` with clean `from tests.llm_cassette import ...` imports. Fix 3: added `threading.Lock` to `ReplayClient._next_response()` protecting `_index` and `call_log` from race conditions when cassette clients are shared across `ThreadPoolExecutor` threads in `extract_chunk`. Fix 4: created 2 new cassette files (`type_resolver_escalation.json`, `deferred_dedup_escalation.json`) and 3 test cases in `TestInlineModelCassettes` class validating both inline models replay correctly and are resolvable in the registry. Updated `docs/KGF_DESIGN.md` Section 17 to document inline model coverage and thread-safe replay. All 371 tests pass (368 existing + 3 new).

90. **Task - LLM token tracking** (v0.4.25): Wired token usage extraction across all 8 LLM call sites and added event signal emission<br>
    **Result**: Previously `LLMCallCompleted.token_count` was always `None` and only `extract_chunk` emitted signals - 7 of 8 call sites were silent. Created `extract_usage()` helper in `kg_builder_cli/extraction/extract.py` that reads `response._raw_response.usage` from instructor responses to capture `prompt_tokens`, `completion_tokens`, and `total_tokens`. Added `prompt_tokens` and `completion_tokens` fields to `LLMCallCompleted` in `events/types.py`. Wired signal emission (`llm_call_started` + `llm_call_completed` with token counts) into all 8 call sites: chunk extraction (`extract.py`), schema signals (`schema_signals.py`), type resolver escalation (`type_resolver.py`), deferred dedup escalation (`deferred_dedup.py`), curing probe and decision (`generative.py` phase 1 and 2), recure decision (`generative.py`), and type clustering (`type_clustering.py`). Added `_on_llm_call_completed` handler in `events/handlers.py` that logs call type, duration, and token breakdown (`Xp + Yc = Zt`). Registered handler in `register_default_handlers()`. All 371 tests pass.

91. **Task - KGF_DESIGN pipeline-first tightening** (v0.4.25): Applied design critique fixes to `docs/KGF_DESIGN.md` addressing residual agent-centric language, incomplete batch logging, and missing no-mutation mandate<br>
    **Result**: Applied 8 edits across 3 categories in `docs/KGF_DESIGN.md`. Fix 1 (pipeline-first language): rewrote 6 lines where agent-centric phrasing implied agents orchestrate or pause the pipeline - line 25 changed "agents are present at" to "agents are invoked at ... returning structured decisions to the pipeline", line 27 changed "the agent pauses" to "the pipeline pauses at defined checkpoints where the agent presents", line 28 changed "the agent makes all decisions" to "the pipeline proceeds through interactive checkpoints" with agent returning decisions, line 47 changed "the agent orchestrates their sequencing" to "the pipeline orchestrates their sequencing" with agents handling only checkpoint/escalation decisions, line 256 changed "rendering a cure/block decision" to "returning a `CureDecision` to the pipeline", line 316 added "and escalation points" to match line 25 phrasing. Fix 2 (escalation logging): added 2 new bullet points to the batch decision logging section covering escalation invocations (call_type, model, duration, token counts, structured decision, reasoning) and escalation failures (error type, message), reflecting the `LLMCallStarted`/`LLMCallCompleted`/`LLMCallFailed` signals already emitted by the implementation. Fix 3 (no-mutation mandate): appended explicit sentence to Agent Tool Registry stating "Escalation agents must not mutate pipeline state - they receive read-only context and return structured decisions (`CureDecision`, `MergeDecision`, `TypeChoice`) that the calling pipeline function applies." Verification: zero grep hits for "the agent applies", "the agent pauses", "the agent makes all decisions" confirming all agent-centric language eliminated from pipeline sections.

92. **Task - KGF theme swatch creation** (v0.4.25): Created SVG theme swatch for KGF design doc infographics derived from Stellars-Tech JupyterHub logo brand colours<br>
    **Result**: Created `docs/images/theme_swatch.svg` with three-section validation layout (transparent, light #f5f7fa, dark #1e1e1e) following the svg-infographics skill standards. Derived palette from `stellars-tech_jupyterhub_logo_basic.svg` brand colours: accent-1 `#0096d1` (logo cyan hub icon), accent-2 `#da8230` (logo orange connector), with fg grades using the teal family from logo text fill `#4099b3`. Reversed fg numbering convention so fg-1 is the most contrastive/visible shade and fg-4 is the least (but still readable at 9px). Light-mode fg values darkened through three iterations to ensure contrast against near-white backgrounds: fg-1=`#1a5a6e`, fg-2=`#24697e`, fg-3=`#2e808e`, fg-4=`#3a929e`. Dark-mode fg values via CSS `prefers-color-scheme`: fg-1=`#b8e4f0`, fg-2=`#8fd4e6`, fg-3=`#5aafc5`, fg-4=`#4099b3`. Swatch demonstrates cards, stat banners, coverage bars, arrow styles (pipeline/escalation/decision-return), and graph node icon strokes on all three backgrounds. Also updated workspace skill `svg-infographics/SKILL.md` to document the reversed fg grade ordering convention (most contrastive first).

93. **Task - KGF design infographics creation and validation** (v0.4.25): Created 3 SVG infographics for KGF_DESIGN.md and validated through iterative WCAG AA contrast fixing<br>
    **Result**: Created three SVG infographics in `docs/images/`: `pipeline_flow.svg` (6-stage horizontal pipeline Parse->Chunk->Extract->Dedup->Resolve->Load->Neo4J with 3 orange dashed escalation branches to Curing, Resolution, and Deferred Dedup agent cards), `hybrid_architecture.svg` (3-column layout showing input sources, pipeline stages with hot-path indicator, and agent engagement model with interactive checkpoints, escalation points, and tool registry), and `llm_call_sites.svg` (4x2 card grid showing all 8 LLM call sites with KEEP/CONVERT classification, response models, and volume indicators). Initial WCAG AA validation via `check_contrast.py` revealed 22-44 failures per file from three root causes: fg-3/fg-4 too light on card fills, orange/cyan inline text fills below 4.5:1 on tinted backgrounds, and no dark-mode adaptation for accent text. Fixed through 3 rounds: (1) darkened CSS fg-3 from `#2e808e` to `#236a74` and fg-4 from `#3a929e` to `#2b7680`, (2) added `.accent-1` (#005f7a light / #5cc8e0 dark) and `.accent-2` (#7a4a15 light / #d4a04a dark) CSS classes replacing all inline `fill="#007a9e"` and `fill="#a86520"` on text elements, (3) changed section headers and Neo4J label to CSS classes (`fg-1`, `fg-2`) instead of fixed inline fills that failed in one mode. Final contrast check: `llm_call_sites.svg` 0 failures, `pipeline_flow.svg` and `hybrid_architecture.svg` 4-6 false positives from checker not handling `fill-opacity` (reports 6% opacity fills as solid #0096d1). Overlap checker shows structural issues from hidden guide-grid elements and column connectors (by design). Alignment checker confirms main layouts correct. Updated `theme_swatch.svg` with final fg-3/fg-4/accent values to match infographic palette.

94. **Task - SVG design system standardization** (v0.4.25): Standardized card shape, arrow conventions, and corner radius across all 4 SVG infographics to match theme swatch reference design<br>
    **Result**: Converted all card shapes across `pipeline_flow.svg` (6 pipeline + 3 escalation + 1 container = 10), `hybrid_architecture.svg` (3 input + 3 agent = 6), and `llm_call_sites.svg` (8 cards) from `<rect rx="6">` to the swatch card pattern: flat top edge with rounded bottom corners only using `<path>` with Q curves at r=3 (reduced 75% from original r=10), separate fill and stroke paths, and 5px accent bar at top with opacity 0.6. Replaced all diagonal arrow connectors with chamfered L-shaped routing using 4px diagonal segments at each 90-degree bend instead of sharp corners. Pipeline->escalation connectors in `pipeline_flow.svg` use solid lines (not dashed), with Dedup->Resolution as straight vertical (aligned centers) and Extract->Curing / Resolve->Deferred as chamfered L-shapes. Architecture SVG arrows between columns also chamfered with staggered vertical midpoints. Escalation cards wrapped in a muted container (flat-top-rounded-bottom, fill opacity 0.02, stroke opacity 0.25) with label moved below. Removed markdown h1 header from `KGF_DESIGN.md` since banner SVG serves as document header. Updated `header_banner.svg` version to v28. Added comprehensive design notes to `theme_swatch.svg` header comment documenting card path template, arrow conventions, chamfer pattern, and container styling for future SVG work.

95. **Task - Arrow and matrix refinements** (v0.4.25): Fixed arrow stem protrusion, updated swatch spacing, cleaned up LLM engagement matrix in design doc and SVG<br>
    **Result**: Fixed arrow stems across all SVGs so path ends at back of arrowhead polygon (not tip) - affected L-route connectors in `pipeline_flow.svg` (2 chamfered paths V116->V112), `hybrid_architecture.svg` (3 paths H210->H206, H548->H544), and `theme_swatch.svg` (V270->V266). Updated `theme_swatch.svg` dark strip arrows from old dashed "decision return" style to chamfered L-route matching light strip, standardized stroke-width to 1.2 and head size to 6px/4px, closed 16px gap between light and dark strips, added 12px spacing between arrow samples and ICON STROKES label. Split the LLM Engagement Matrix table in `KGF_DESIGN.md` into two tables: a clean decision matrix with explicit `LLM Call` / `Strands Agent` columns (Yes/No) with volume, and a separate rationale table with module paths. Updated `llm_call_sites.svg` to replace source file paths with coloured `LLM CALL` (accent-1 cyan) and `STRANDS AGENT` (accent-2 orange) labels on each card, and updated legend to match with colour-coded text. All updated SVGs copied to svg-infographics skill examples.

96. **Task - 6 KGF design infographics**: Created 6 SVG infographics for KGF_DESIGN.md sections, embedded in document, created svg-infographics skill<br>
    **Result**: Created 6 new SVGs in `docs/images/` following the Stellars-Tech design system: `bayesian_resolution.svg` (formula card + 4 evidence signal cards + 3-zone posterior threshold bar), `curing_decision_flow.svg` (6-level vertical priority chain with YES/NO branching to CURE outcome), `stability_metrics.svg` (2x5 metric card grid grouped by Information Theory, Ecology, Linguistics domains), `deferred_dedup.svg` (left card initial assessment -> centre timeline with 3 evidence boost rounds -> right card resolution), `type_hierarchy.svg` (3-level tree with sibling boost annotation + self-evolution panel showing encounter threshold mechanism), and `confidence_propagation.svg` (6 pipeline stages in S-shaped flow showing confidence transformation rules per stage). Added `.semantic-block` and `.semantic-merge` CSS classes for red/green semantic colours with proper light/dark variants - eliminates all inline `fill` attributes on text elements. All text uses CSS classes exclusively (fg-1 through fg-4, accent-1, accent-2, semantic-block, semantic-merge). Embedded all 6 SVGs in `docs/KGF_DESIGN.md` at their respective sections (type hierarchy after line 960, curing after config block, stability metrics after metrics table, bayesian resolution after module structure, deferred dedup after configuration, confidence propagation after stage 6). Created `~/.claude/skills/svg-infographics/` with `SKILL.md` containing full design system specification including mandatory CSS class rules, card/arrow/container conventions, and validation checklist. Copied all 6 SVGs to skill examples directory. Validation passed: all 6 SVGs compliant with design system (CSS classes on all text, no inline fills, no opacity on text, correct card shapes, proper arrow conventions, viewBox without explicit dimensions).

97. **Task - SVG visual review fixes**: Fixed layout, spacing, connectivity, and colour consistency across all SVG infographics through multiple review passes<br>
    **Result**: Applied iterative fixes from visual review across all infographics. `bayesian_resolution.svg` - fixed broken embedding arrow, extended evidence cards, rewrote Bayesian equation as clean single line. `confidence_propagation.svg` - extended bottom row cards, added Stage 6 -> Neo4j connecting arrow, rebuilt taller/dimmer Neo4j cylinder with bold `fg-1` text, moved legend beside cylinder, made all 6 arrows much thicker (stroke-width 2.5, opacity 0.7-0.8, 10x6 heads). `curing_decision_flow.svg` - replaced individual YES arrows with manifold bus pattern (vertical trunk at x=480, horizontal taps as simple T-junctions, single output arrow to CURE card), removed circle/ball junction markers, fixed trunk overshoot artefact at top. `deferred_dedup.svg` - extended cards for pill badge spacing. `type_hierarchy.svg` - simplified Component->exemplar connectors to straight lines, angular HEPA Filter connector routed vertically then horizontally to avoid sibling boost text, shifted all tree content down 15px for breathing room from container top, all connectors strengthened (stroke-width 1.5, opacity 0.5). `stability_metrics.svg` - fixed accent bar heights. `theme_swatch.svg` - converted palette reference section from hardcoded dark-mode fills to CSS classes for proper `prefers-color-scheme` detection, fixing unreadable text on light backgrounds. All legend text across all 9 SVGs updated from `fg-4` to matching accent classes (cyan swatch -> `accent-1`, orange -> `accent-2`). Updated svg-infographics skill SKILL.md with legend colour convention and validation checklist item. Copied all SVGs to skill examples directory. Fixed 6 infographic `<img>` paths in `docs/KGF_DESIGN.md` from `docs/images/` to `images/` (relative to markdown file location).

98. **Task - CLI multi-input and pipeline modes** (v0.4.25): Added `--input`/`-i` repeatable option and `--structured`/`--unstructured` pipeline mode flags to `kgf ingest`, moved supported extensions to `config.py`, added README SVG graphics<br>
    **Result**: Three CLI changes to `kg_builder_cli/cli.py`. (1) Added `--input`/`-i` as a repeatable `typer.Option` accepting files or directories - merged with the now-optional positional `SOURCE` argument so both `kgf ingest data/raw/` and `kgf ingest -i dir1/ -i dir2/ -i file.pdf` work. (2) Added mutually exclusive `--structured` and `--unstructured` flags controlling pipeline mode - unstructured is default when neither specified, structured selects JSON/JSONL/CSV/XLSX file types. Validation exits with error if both flags are set. (3) Added startup log output announcing pipeline mode, model, neo4j URI, chunk settings, embedding/bayesian flags, and rate limit config. Moved file extension constants to `kg_builder_cli/config.py` as `UNSTRUCTURED_EXTENSIONS` ({.pdf, .txt, .md, .docx}), `STRUCTURED_EXTENSIONS` ({.json, .jsonl, .csv, .xlsx}), and `SUPPORTED_EXTENSIONS` (union) - CLI imports and filters by mode-appropriate set. Created `tests/test_cli_input.py` with 12 tests covering extension constants (correct sets, union, no overlap), no-input error, mutual exclusion error, unstructured/structured file type filtering from mixed directories, multi-source merging, single file input, and missing path handling. All 383 tests pass. Updated all three docs: `docs/benchmarks/multidoc_benchmark_spec.md` (execution commands, ingestion modes section with both flags), `docs/KGF_DESIGN.md` (synopsis, options table with both flags, input detection workflow), `README.md` (quick start examples, supported formats section split by mode). Created `docs/images/readme_banner.svg` - compact 90px header banner with title, tagline, and graph-node motif (no version or "Design Document" text). Added 4 SVGs to `README.md`: readme_banner at top, `pipeline_flow.svg` after pipeline description, `hybrid_architecture.svg` at Technical Details, `curing_decision_flow.svg` before curing convergence details.

99. **Task - References and design concepts** (v0.4.25): Analyzed PheKnowLator and graph_builder repos, organized references, renamed DESIGN_ASSUMPTIONS to DESIGN_CONCEPTS with new future concepts<br>
    **Result**: Cloned and analyzed two external repositories for KGF relevance. PheKnowLator (biomedical KG from structured data + OWL ontologies) - deterministic entity mapping, no LLM extraction, relevant only for ontology validation patterns (punning detection, connectivity verification). graph_builder (tabular extraction from PDFs/CSVs) - simple fuzzy matching, relevant for multi-stage assembly pipeline pattern and collection-aware entity management. Neither has anything fundamentally better than KGF for LLM-driven graph construction. Created `references/other-projects/PheKnowLator-insights.md` and `references/other-projects/graph_builder-insights.md`. Reorganized `references/` into three subfolders: `articles/` (13 Medium posts and guides), `papers/ontology/` (5 academic papers with summaries), `other-projects/` (3 project analyses). Renamed `docs/DESIGN_ASSUMPTIONS.md` to `docs/DESIGN_CONCEPTS.md` with updated header. Added four new concepts to Potential Enhancements section: (1) post-curing ontology validation from PheKnowLator analysis, (2) session state persistence and resume (`--resume`) with 6-scenario matrix covering interrupted fluid/cured/re-curing phases, 4 recovery strategies (local file, graph node, hybrid, graph-only), and conflict detection design, (3) LLM observability and cost tracking via litellm callbacks with per-call token/latency metrics and benchmark cost summaries, (4) multi-pass extraction with convergence signal measuring inter-pass agreement across multiple extraction runs over the same chunks.

100. **Task - Fix event log NaN/timestamps/doc_index**: Fixed JSONL event log breaking JupyterLab viewer due to NaN serialization, added timestamps and doc_index to LLM call events<br>
    **Result**: Three files modified. In `kg_builder_cli/events/handlers.py`: added `_sanitize_nans()` recursive function that replaces `float('nan')` and `float('inf')` with `None` before JSON serialization - the root cause was `json.dumps()` defaulting to `allow_nan=True` which emits bare `NaN` literals (invalid JSON per RFC 8259). Also injected ISO 8601 UTC `timestamp` field into every event log line using `datetime.now(timezone.utc).isoformat()`. In `kg_builder_cli/extraction/extract.py`: added `doc_index: int | None = None` parameter to `extract_chunk()` and wired it to all five event emissions (`LLMCallStarted`, `LLMCallCompleted`, two `LLMCallFailed` paths) replacing hardcoded `None`. In `kg_builder_cli/extraction/unstructured.py`: passed `doc_index=doc_index` from `ingest_document()` context through `_throttled_extract()` to `extract_chunk()`. All 383 tests pass, lint clean.

101. **Task - Extract foundational concepts**: Analyzed `docs/research/Document concepts.md` and extracted 13 foundational concept documents into `docs/research/`<br>
    **Result**: Decomposed a long research conversation into 13 self-contained concept documents in `docs/research/concept-*.md`. Concepts span four domains: ontology lifecycle (curing/stabilization), entity resolution (Bayesian posterior, multi-channel fusion, adaptive guides, positive evidence accumulation), pipeline control (FSM governance, graph metanode control plane), and decision theory (two-layer architecture, Bayesian decision calibration, contextual adjudication trigger, epistemic loop prevention). Each document explains the concept, its rationale, relationship to existing approaches, current implementation status, and classification of novelty. Also includes `concept-architectural-innovation-classification.md` capturing the meta-analysis of KGF's position in the ecosystem (architecturally innovative through integration, not algorithmically groundbreaking).

102. **Task - Pipeline lifecycle FSM section**: Added formal FSM section to KGF_DESIGN.md governing graph lifecycle from cradle to grave<br>
    **Result**: Inserted new Section 14 "Pipeline Lifecycle" into `docs/KGF_DESIGN.md` with 7 subsections (14.1-14.7) covering a 6-state FSM: EMPTY, INITIALIZING, CURING, STABLE, RECURING, FAILED. The design evolved through extensive simulation and deliberation from an initial 10-state model to 6 states by recognizing that fluid/direct are extraction mechanisms (not lifecycle states), CURING means ontology calibration (not just type discovery), and STABLE serves as both active-run and idle-between-runs state with `run_id` tracking activity. Key sections: state table with entry/exit conditions, full transition table with triggers/conditions/actions/events, 6-scenario entry matrix covering all graph-state/seed/strict-mode combinations, ontology prior conflict rules (graph ontology always wins in strict mode), `(:KGFControl)` metanode design with `fsm_state`, `run_id`, `ontology_source`, `extraction_mechanism`, `consolidation_started_at` and related `(:KGFRun)` and `(:KGFTransition)` audit nodes, `transitions` library implementation sketch with guards and callbacks, and event mapping cross-reference to Section 15 (Observability). Renumbered sections 14-21 to 15-22, added `transitions` to Dependencies (Section 19), updated cross-references in Sections 3 and 5.8, updated Mode Compatibility Matrix to allow `--fluid` + `--ontology` (seed as prior), updated `docs/research/concept-pipeline-governance.md` with formalization status note, and rewrote `README.md` Pipeline Lifecycle section with the FSM diagram and STABLE/CURING semantics.

103. **Task - FSM generative simulations**: Created 3 generative simulation documents validating the 6-state pipeline lifecycle FSM<br>
    **Result**: Created `docs/simulations/` with three reasoning-based simulation documents that trace the FSM through distinct scenarios. `sim-first-run-fluid-discovery.md` traces a 10-doc medical device corpus through EMPTY -> INITIALIZING -> CURING -> STABLE with fluid extraction, observing 3 transitions for a complete first run and validating that CURING encompassed both discovery and consolidation without needing separate states. `sim-multi-run-drift-recuring.md` traces 3 runs against the same graph - Run 1 establishes ontology (6 types), Run 2 adds same-domain docs (STABLE -> INITIALIZING -> STABLE, 2 transitions), Run 3 introduces adjacent-domain docs triggering drift and the full STABLE -> RECURING -> CURING -> STABLE cycle (5 transitions, ontology expands to 9 types). Key observation: RECURING as a deliberation state enables audit trail for both authorize and dismiss paths. `sim-strict-seed-with-crash-recovery.md` validates that strict-seed runs enter CURING (not STABLE) because calibration is needed even with prescribed types, and demonstrates crash recovery from `consolidation_started_at` metanode property - proving graph-as-control-plane works without local filesystem state.

104. **Task - FSM implementation and CLI integration** (v0.4.26): Implemented the 6-state pipeline lifecycle FSM as a new `kg_builder_cli/fsm/` package and integrated it into the ingestion pipeline as a backward-compatible observability overlay (see also entry 105 for completion)<br>
    **Result**: Built the complete FSM module across four files. `fsm/states.py` defines the 6 states (EMPTY, INITIALIZING, CURING, STABLE, RECURING, FAILED) and 8 transitions as `transitions` library-compatible dicts, plus a `GraphState` constants class providing type-safe state references throughout the codebase. `fsm/context.py` implements `PipelineContext` as a `@dataclass` carrying all KGFControl metanode properties (`graph_id`, `run_id`, `run_count`, `ontology_source`, `extraction_mechanism`, `consolidation_started_at`, `ontology_type_count`, `ontology_hash`, `created_at`, `last_completed_at`, `last_error`) alongside runtime flags (`needs_calibration`, `drift_confirmed`). The `create_fsm()` factory attaches the `transitions` Machine via mixin with `auto_transitions=False` to avoid naming collisions with existing `is_<state>()` methods (renamed to `in_curing()`/`in_stable()`), then wraps each trigger method to capture `_prev_state` before transition - enabling `on_enter_*` callbacks to know the source state and emit accurate `phase_transition` blinker signals. Guard conditions are simple boolean properties set by `cli.py` rather than reading detector state directly - when a guard returns False, the `transitions` library silently skips the transition (no MachineError), which is intentional as guards are safety nets not primary flow control. `fsm/metanode.py` provides full CRUD for the `(:KGFControl)` metanode via `create_control_metanode()` (idempotent MERGE on `graph_id`), `read_control_metanode()`, `update_control_metanode()`, plus `write_run_node()` for `(:KGFRun)` nodes linked to KGFControl via `[:HAS_RUN]`, `write_transition_node()` for `(:KGFTransition)` audit trail linked via `[:HAS_TRANSITION]`, and `detect_graph_state()` implementing the Section 14.3 detection algorithm (check for metanode, then entity count, then empty). CLI integration in `cli.py` adds two helper functions: `_init_fsm(config, curing_enabled, has_seed)` which detects graph state via `detect_graph_state()`, creates `PipelineContext` with appropriate initial state (EMPTY for new graphs, STABLE for existing), transitions through INITIALIZING to CURING or STABLE based on detection results and config, writes the KGFControl metanode and KGFRun node, and returns None on any failure so the pipeline proceeds without FSM tracking; and `_update_metanode_safe(config, ctx)` which writes the full FSM context to the KGFControl metanode with all exceptions swallowed. The FSM context is passed as an optional keyword argument `fsm_ctx=None` to `_ingest_fluid()`, with all FSM operations guarded by `if fsm_ctx:` checks. Three hook points are wired: (1) ingestion start - `_init_fsm()` creates context and writes metanode, (2) consolidation - `mark_consolidation_start()` sets the crash recovery timestamp before type clustering begins, `mark_consolidation_end()` + `stabilize()` fires the CURING -> STABLE transition after successful flush, (3) run completion - `complete_run()` sets `run_id` to null, increments `run_count`, and updates the metanode. The existing boolean `cured` flag and phase string tracking remain unchanged - the FSM runs alongside them as an observability layer, not a control-flow replacement. Updated `docs/KGF_DESIGN.md` Section 14.6 to reflect the actual implementation: documented the `GraphState` constants class, `PipelineContext` dataclass structure, `create_fsm()` factory with `_prev_state` wrapping, guard behavior (silent no-op on failure), state entry callbacks (emit signals only, don't drive pipeline logic), and the full integration pattern including the backward-compatible overlay design. Created `tests/test_fsm.py` with 25 tests covering all state transitions (11), guard conditions (3), illegal transitions (4), full lifecycle scenarios matching the 3 simulation documents (4), and helper methods (5). Created `tests/test_metanode.py` with 12 tests covering metanode CRUD and graph state detection with mocked Neo4j. Added `transitions` to `pyproject.toml` dependencies. All 420 tests pass (383 existing + 37 new), lint clean.

105. **Task - FSM drift hooks, audit trail, and benchmark** (v0.4.28): Completed remaining FSM Phase 2 items (drift detection hooks, KGFTransition audit trail), fixed import bug, added `transitions` dependency, ran v27 benchmark<br>
    **Result**: Wired drift detection FSM hooks into `cli.py` at three points in the cured-phase drift detection block (lines 493-560): when `detector.check_drift()` returns True, sets `fsm_ctx.drift_confirmed = True` and calls `fsm_ctx.detect_drift()` (STABLE -> RECURING); when LLM or heuristic decides re-cure, calls `fsm_ctx.authorize_revision()` (RECURING -> CURING) with `needs_calibration = True`; when drift is dismissed, calls `fsm_ctx.dismiss_drift()` (RECURING -> STABLE) and resets `drift_confirmed = False`. All three paths update metanode via `_update_metanode_safe()`. Added KGFTransition audit trail to `_emit_transition()` in `fsm/context.py` - after emitting the blinker signal, the callback now writes a `(:KGFTransition)` node linked to `(:KGFControl)` via `[:HAS_TRANSITION]` with `from_state`, `to_state`, `trigger`, `run_id`, `timestamp`. Added `_neo4j_uri`, `_neo4j_user`, `_neo4j_password` fields to `PipelineContext` (wired in `_init_fsm()` after context creation), Neo4j write is fire-and-forget with try/except. Fixed missing `detect_graph_state` export from `fsm/__init__.py` that caused FSM to silently degrade to None on the v27 run. Added `transitions>=0.9` to `pyproject.toml` runtime dependencies - `make install` had been removing the package because it was only installed manually. Ran v27 benchmark from `tmp/` per `multidoc_benchmark_spec.md` procedure (concurrency=1, `bolt://neo4j:7687`). Ingestion took 57 minutes, cured at doc 4 with 12 types and 395 entities. Final graph: 1036 entities, 3880 rels, 159 chunks, 10 docs. Benchmark scored **86% hybrid** (60/63 deterministic = 95%, 3.8/5.0 generative) - within -2% of v20 baseline (88%), well within normal extraction variance. Same 3 persistent failures: OSA not consolidated (4 copies), cross-type duplicates at 34 (threshold <20), SleepStyle modes unlinked. FSM did not execute this specific run due to the import bug (gracefully degraded), confirming no regression from FSM code changes. Import fix and dependency addition require a follow-up run to verify FSM metanode creation and KGFTransition audit trail in Neo4j.

106. **Task - FSM lifecycle research goal**: Created SMART research goal for graph-resident FSM lifecycle management investigation<br>
    **Result**: Created `docs/research/goal-fsm-lifecycle.md` with full SMART research goal covering three novel angles not addressed in existing literature: graph-authoritative schema management (ontology state persisted as graph metanodes rather than filesystem artifacts), cross-run Bayesian calibration continuity (per-type posterior persistence via `(:KGFTypeCalibration)` nodes), and crash recovery for multi-phase LLM extraction pipelines (three interruption scenarios with distinct recovery strategies). The novel angle argues that 2024-2025 LLM KG builders (GraphRAG, LightRAG, Graphusion) solved extraction but left lifecycle governance wide open - no published system addresses ontology evolution persistence, cross-run calibration continuity, or partial-consolidation crash recovery. Distinguishes from traditional ontology evolution research (Stojanovic, Flouris) which addresses hand-crafted schemas, not LLM-emergent ontologies governed by statistical signals. Benchmark uses existing KGF MultiDoc v27 baseline (86% hybrid, 60/63 deterministic, 3.8/5.0 generative) with a 3-run sequential evaluation protocol measuring ontology reconstruction fidelity, resolution quality delta from calibration persistence, and crash recovery correctness across pre/mid/post-consolidation scenarios. 4-week timeline aligned with existing FSM infrastructure.

107. **Task - FSM lifecycle experiment plan**: Designed experiment YAML for graph-resident FSM lifecycle management hypotheses<br>
    **Result**: Created `docs/research/experiment-fsm-lifecycle.yaml` with full experiment plan testing three hypotheses from the research proposal. H1 (dual-authority schema): 3 conditions - `file_only_schema` (current stateless behavior, represents all existing LLM KG builders), `graph_only_schema` (pure graph authority testing contrarian's noise accumulation concern), and `dual_authority_schema` (split authority - file for definitions, graph for operational state) with consumed prior dedup ablation. H2 (idempotent crash recovery): 2 conditions - `wipe_and_retry_recovery` (destructive baseline) and `fingerprint_idempotent_recovery` (SHA256 content-addressable MERGE keys eliminating crash recovery taxonomy) tested at 3 crash points (25/50/75% consolidation) with diagnostic run_id ablation. H3 (distribution-aware calibration): 3 conditions - `fresh_calibration` (current fixed defaults), `naive_calibration_persistence` (contrarian test case), and `decay_weighted_calibration` (exponential decay with topology sanity check) with topology-sanity and no-decay ablations. Two regime factors: corpus split (homogeneous/heterogeneous) and crash point (25/50/75%). Total ~225 batch runs + 45 recovery tests across 5 seeds per condition. Extraction caching strategy reduces API cost ~60% for conditions differing only in schema management. Statistical plan uses Wilcoxon signed-rank, bootstrap CIs, and Cohen's d. Primary metric: recovery entity divergence (minimize). Estimated 60-80 hours sequential, \$80-150 API cost.

108. **Task - FSC-1/2/6 implementation**: Implemented control plane naming migration, graph-authoritative schema persistence, and crash recovery with document resume<br>
    **Result**: Three FSM state considerations implemented across 10 files with 429 tests passing, lint clean. **FSC-1** migrated all 7 Cypher queries in `fsm/metanode.py` to dual labels (`:KGFControl:KGFState`, `:KGFControl:KGFRun`, `:KGFControl:KGFTransition`) and added `WHERE NOT n:KGFControl` exclusions to 11 domain queries across `loading/validation.py` (5 queries for orphan detection, entity count, relationship count, type distribution, types with rels), `curing/graph_query.py` (4 queries for entity counts, relationship patterns, entity search), `loading/loader.py` (1 query for `resolve_against_graph()`), and `fsm/metanode.py` (1 query for `detect_graph_state()`). This prevents control plane nodes from contaminating ontology discovery, resolution, validation metrics, and curing decisions. **FSC-6** added `run_id` parameter to `_create_document_node()` and `_create_entity_nodes()` in `loader.py`, threaded through all `load_extraction()` and `load_doc_chunks()` calls in `cli.py`. Implemented three crash recovery scenarios in `_init_fsm()`: pre-consolidation (force re-cure), mid-consolidation (wipe entities by stale `run_id`, force re-cure), post-consolidation (query loaded `(:Document)` nodes, skip already-loaded docs). Added `loaded_doc_names: set[str]` to `PipelineContext`, document skip logic in both `_ingest_fluid()` and `_ingest_direct()`. Stale consolidation markers and run_ids cleared after detection. **FSC-2** added `compute_hash()` to `OntologyBuffer` (deterministic SHA256 of sorted confirmed types, 16 hex chars), `from_graph()` classmethod reconstructing buffer from 7 graph queries (entity frequencies, rel types, KGFOntologyType defs, IS_A hierarchy, KGFResolutionGuide, entity exemplars, KGFTypeCalibration), three new `metanode.py` functions (`write_ontology_types()`, `write_resolution_guide()`, `write_type_calibration()`), `_persist_schema_safe()` helper in `cli.py` orchestrating writes at both stabilize points. Seed dedup compares seed hash against metanode's `ontology_hash`. Cache-first pattern loads `ontology.yml` if hash matches, falls back to `from_graph()` on mismatch. Updated KGF_DESIGN.md Section 14.5 with dual-label convention, 6-node control plane inventory, schema persistence design, crash recovery matrix, and document/entity tagging. Updated FSC-1/2/6 tracking tables in `fsm-states-considerations.md`. Added 9 new tests (3 write function test classes, 3 `compute_hash()` tests).

109. **Task - FSC-8 cross-run fluid persistence**: Implemented cross-run fluid phase persistence allowing fluid phase to survive process restarts without re-extraction<br>
    **Result**: Added graph-resident extraction cache using two new control plane node types: `(:KGFControl:KGFFluidResult)` caching per-document `ExtractionResult` (embeddings and chunks excluded) and `(:KGFControl:KGFFluidState)` caching serialized `CuringDetector`, `StabilityMetrics`, and `DeferredDedupBuffer` state. Payloads use versioned envelope pattern (`{"v": 1, "data": ...}`) with `zlib` compression and `base64` encoding via `_compress()`/`_decompress()` helpers, with `default=str` for datetime serialization. Added `to_dict()`/`from_dict()` serialization methods to `CuringDetector` (6 fields including coverage_history, new_types_history as sorted lists), `StabilityMetrics` (5 fields including prev_probs and occurrence histories), and `DeferredDedupBuffer` (pipe-delimited keys, full DeferredPair reconstruction). Created 6 new `metanode.py` functions (`write_fluid_result`, `read_fluid_results`, `delete_fluid_results`, `write_fluid_state`, `read_fluid_state`, `delete_fluid_state`) plus `check_fluid_cache_exists`. In `cli.py`, moved `load_doc_chunks()` from batch-at-consolidation to per-document during fluid phase, added 7 safe helper functions (`_write_fluid_result_safe`, `_write_fluid_state_safe`, `_read_fluid_results_safe`, `_read_fluid_state_safe`, `_delete_fluid_cache_safe`, `_check_fluid_cache_exists_safe`). Resume logic before the file loop reads cached results and rebuilds accumulator, detector, metrics_tracker, and deferred buffer. Cache cleanup at 6 trigger points: curing completion, end-of-corpus flush, drift re-entry, mid-consolidation crash recovery, version mismatch, and run completion safety net. Updated `_init_fsm()` crash recovery scenario 1 to split into 1a (fluid cache exists -> resume) and 1b (no cache -> force re-cure), scenario 2 now also cleans up stale fluid cache. Updated `docs/KGF_DESIGN.md` Section 14.5 with 2 new nodes in control plane inventory, cross-run fluid persistence paragraph, and expanded crash recovery matrix (4 scenarios). Added FSC-8 entry in `docs/research/fsm-states-considerations.md`. Created 18 new tests covering compression round-trips, version mismatch, write/read/delete for both node types, and serialization round-trips for all 4 model types. 447 tests pass, lint clean.

110. **Task - Calibrated multi-channel Bayesian**: Implemented three-phase calibration system for Bayesian type assignment and cross-type resolution<br>
    **Result**: Implemented all three phases (C: observation collection, A: isotonic calibration, B: multi-channel prior) across 13 files. **Phase C**: Created `kg_builder_cli/curing/observation.py` with `CrossTypeObservation` and `TypeAssignmentObservation` dataclasses plus `ObservationCollector` class providing `record_cross_type()`, `record_type_assignment()`, `derive_ground_truth(type_mapping)` (uses type clustering outcome as ground truth - merged pairs mapping to same cluster are correct, blocked pairs mapping to different clusters are correct), `derive_type_assignment_truth()`, and `aggregate_calibration()` (produces per-type mean_posterior/observation_count/remap_count for KGFTypeCalibration nodes). Wired collector into `_resolve_cross_type()` in `resolution.py` by extending the existing `_emit_decision` helper to also call `_record_obs`, and into `BayesianTypeResolver.resolve()` in `type_resolver.py` recording after each type assignment decision. Threaded collector through `ingest_document()` in `unstructured.py` and both fluid/cured phases in `cli.py._ingest_fluid()`. **Phase A**: Created `kg_builder_cli/curing/calibration.py` with `PosteriorCalibrator` class providing `fit()` (sklearn isotonic regression, minimum 20 samples, graceful None return when sklearn absent or insufficient data), `calibrate()` (binary search + linear interpolation on persisted curve), `to_points()`/`from_json()`/`to_json()` serialization. Added `write_calibration_curve`, `read_calibration_curve`, `delete_calibration_curves` to `fsm/metanode.py` persisting `(:KGFControl:KGFCalibrationCurve)` nodes with `x_points`/`y_points` (JSON arrays), `n_samples`, `model_type`, linked via `HAS_CALIBRATION_CURVE`. Added `_load_calibrator_safe()`, `_fit_and_persist_calibration()`, `_persist_calibration_curve_safe()` helpers in `cli.py`. Applied calibrator to raw posteriors via `_maybe_calibrate()` in `_resolve_cross_type()` and via `self._calibrator.calibrate()` in `BayesianTypeResolver.resolve()`. **Phase B**: Created `kg_builder_cli/curing/type_metrics.py` with `TypeMetricsCollector` computing 19 metrics across 6 categories: frequency-derived (F1-F3: log-smooth, rank percentile, CV), distribution shape (D1-D3: description entropy, name diversity, top-3 concentration), embedding space (E1-E4: cohesion, separation, silhouette, centroid distance - NaN when no embeddings), graph topology (G1-G3: rel type diversity, co-occurrence Jaccard, reciprocity), temporal (T1-T3: discovery order, TAR at discovery, cross-type rate), cross-run calibration (C1-C3: historical mean posterior, remap rate, frequency delta - NaN on run 1). Added `_build_multi_channel_prior()` to `BayesianTypeResolver` with min-max normalization, negative-direction inversion, NaN-neutral weighting, uniform weights for Phase B.1. Added `TypeMetricsComputed` event type and `type_metrics_computed` signal. Updated `_persist_schema_safe()` to accept collector and use real observation data for KGFTypeCalibration instead of scaffold zeros. Updated `docs/KGF_DESIGN.md` Section 14.7 documenting observation collection, isotonic calibration lifecycle, multi-channel prior with 19-metric table, and KGFCalibrationCurve control plane node. Created `tests/test_observation.py` (15 tests), `tests/test_calibration.py` (11 tests), `tests/test_type_metrics.py` (18 tests). 493 tests pass, lint clean.

111. **Task - Calibration correlation tracking**: Added Spearman rank correlation for type metrics, fixed metric lifecycle, added calibration events<br>
    **Result**: Added Spearman rank correlation computation between 19 type metrics and per-type resolution correctness rate. Created `compute_type_correctness_rates()` on `ObservationCollector` deriving per-type accuracy from cross-type ground truth, and `compute_spearman_correlations()` standalone function with pure-Python rank computation (no scipy dependency, minimum 5 types, NaN-aware per-metric filtering). Added 4 new event types (`CalibrationGroundTruth`, `CalibrationFitted`, `MetricCorrelationComputed`) with signals and default INFO handlers in `handlers.py`. Updated `TypeMetricsComputed` with `doc_index` and `trigger` fields. Changed `_fit_and_persist_calibration()` to return computed `type_metrics` dict, captured at both consolidation paths (curing trigger and end-of-pipeline flush), stored as `cured_type_metrics` in `_ingest_fluid()`. Threaded `type_metrics` parameter through `ingest_document()` -> `BayesianTypeResolver` constructor, where it triggers `_build_multi_channel_prior()` instead of frequency-only `_build_prior()`. This fixes the lifecycle: metrics computed at curing time are now actually used as the prior during cured-phase resolution. During fluid phase, frequency-only priors remain (correct - insufficient data). Updated `docs/KGF_DESIGN.md` Section 14.7 with metric lifecycle, Spearman correlation description, and 4 calibration events. Added 12 new tests: `TestCorrectnessRates` (3), `TestRank` (4), `TestSpearmanCorrelations` (5). 505 tests pass, lint clean.

112. **Task - Suppress Neo4j notification warnings**: Fixed noisy GqlStatusObject warnings on fresh graphs and entity-type frequency filtering bug<br>
    **Result**: Neo4j 5 emits verbose stderr warnings when MATCH queries reference labels/properties that don't exist yet on a fresh graph (KGFCalibrationCurve, KGFFluidResult, etc.). Created `_create_driver_quiet(config)` helper using `notifications_min_severity=NotificationMinimumSeverity.OFF` on the Neo4j driver, replacing 14 inline `GraphDatabase.driver()` calls across all `_safe` helper functions in `cli.py` (`_update_metanode_safe`, `_persist_schema_safe`, `_write_fluid_result_safe`, `_write_fluid_state_safe`, `_read_fluid_results_safe`, `_read_fluid_state_safe`, `_delete_fluid_cache_safe`, `_check_fluid_cache_exists_safe`, `_load_calibrator_safe`, `_persist_calibration_curve_safe`) and 5 crash recovery driver instantiations in `_init_fsm`. Added logical guard to `_load_calibrator_safe` skipping calibration curve load when `run_count < 1` (no prior completed run could have produced a curve). Fixed `_compute_type_metrics_safe` to use `buffer.entity_type_frequencies()` instead of `buffer.frequencies()` which was including relationship type names (COMPLIES_WITH, HAS_COMPONENT, etc.) as pseudo-entity-types in the metrics - producing 16 types with 8 having all-zero/NaN metrics instead of the correct 8 entity types. Smoke test on fresh graph confirmed: zero Neo4j warnings on stderr, 8 entity types in type-metrics-computed event, all 19 metrics populated correctly per type. 505 tests pass, lint clean.

113. **Task - Remove trivial tests**: Pruned 43 tests that tested Pydantic field storage, hardcoded constants, or mathematical guarantees rather than application logic<br>
    **Result**: Deleted `TestEventModels` class (25 tests) from `test_events.py` - every test constructed a Pydantic model and asserted one field back, testing Pydantic not app logic. Deleted entire `test_graph_quality.py` (8 tests) - same pattern for Entity, PropertyDef, TypeDef, ExtractionResult models plus a hardcoded index count assertion. Deleted `TestExtensionConstants` (4 tests) from `test_cli_input.py` - pure constant comparison assertions, cleaned unused top-level imports. Deleted `test_empty_curve_passthrough` and `test_single_point` (2 tests) from `test_calibration.py` - trivial boundary cases. Deleted `test_generate_embeddings_empty_list` (1 test) from `test_embeddings.py` - tests that `[]` returns `[]`. Deleted `test_similarity_scores_bounded` (1 test) from `test_exemplar_index.py` - asserts cosine similarity is between -1 and 1, a mathematical guarantee. Test count reduced from 505 to 462 (460 passed, 2 skipped from pre-existing sklearn absence). Lint clean.

114. **Task - Dead code removal and quality fixes**: Pruned unused code and added logging to silent exceptions across 10 files<br>
    **Result**: Removed unused `buffer` parameter from `query_fluid()` in `graph_query.py` and propagated removal through `llm_should_cure()` in `generative.py` and call site in `cli.py`, cleaning up `OntologyBuffer` TYPE_CHECKING imports from both files. Removed `delete_calibration_curves()` function from `fsm/metanode.py` (defined but never called) and its import/export from `fsm/__init__.py`. Removed `missing_relationships` field from `ValidationReport` in `types/loading.py` (always `[]`, never populated) and its `missing_relationships=[]` at call site in `validation.py`, updated 2 references in `docs/KGF_DESIGN.md` (interface table and JSON example). Added `logger.trace()` to silent `except Exception: pass` in `extract.py:extract_usage()` for token usage parsing failures. Added `logger.debug()` with type name and raw value to `except (json.JSONDecodeError, TypeError): pass` in `buffer.py:from_graph()` for malformed property JSON. Added research note to `docs/research/fsm-states-considerations.md` FSC-5 about control plane cleanup on `authorize_revision` and future controller+model for tracking provenance state. 462 tests pass, lint clean.

115. **Task - FSC-3 and FSC-9 research updates**: Updated FSC-3 implementation status and created FSC-9 documenting CURING semantic breadth gap<br>
    **Result**: Updated FSC-3 (Evidence Collection in STABLE) to reflect that per-type calibration state persistence is now partially resolved via `(:KGFControl:KGFTypeCalibration)` nodes and isotonic calibration curves, narrowing the remaining gap to per-entity posterior provenance (system tracks aggregate type-level stats but cannot trace individual entity assignment confidence for selective re-evaluation during RECURING). Created FSC-9 (CURING Semantic Breadth) capturing the design insight that fluid/direct are extraction mechanisms not lifecycle states, and that CURING encompasses all ontology calibration beyond type discovery. Documented the gap between structural correctness (FSM correctly routes both mechanisms through CURING, `extraction_mechanism` is a metanode property not a state) and the cure trigger (still relies on type-distribution signals only - JSD, Chao1, type accumulation rate). Proposed four calibration convergence signals for a composite cure readiness score: posterior convergence (mean variance across types), resolution guide saturation (new rules per document), deferred dedup resolution rate (fraction of pairs with sufficient evidence), and intra-CURING remap rate (LLM-resolver convergence). Removed stale `docs/research/untitled.txt` scratch notes.

116. **Task - v28 benchmark and calibration update analysis**: Ran v28 benchmark (90% hybrid, new high watermark) and designed adaptive calibration update granularity options<br>
    **Result**: Executed full benchmark procedure per `multidoc_benchmark_spec.md` - wipe, ingest 10-doc CPAP corpus (51 min, cured at doc 4 with 12 types), benchmark. v28 scored **90% hybrid** (60/63 det = 95%, 4.2/5.0 gen), up from v27's 86% and v20's previous high of 88%. Key improvement: cross_doc_resolution generative jumped 3/5 -> 4/5 as judge now recognizes cross-type duplicates as legitimate multi-type entities (e.g. heater as Component vs Setting). FSM fully active this run (EMPTY -> INITIALIZING -> CURING -> STABLE), first verified run with KGFTransition audit trail and KGFTypeCalibration nodes persisted. Calibration: 25 ground truth pairs, 44% accuracy, flat isotonic curve (insufficient discrimination). Spearman correlations on 8 types: rel_type_diversity (+0.58) and hist_mean_posterior (-0.57) strongest signals. Identified two calibration gaps in FSC-9: (1) isotonic curve not hot-loaded for cured-phase docs in same run, (2) multi-channel prior frozen at consolidation instead of updating continuously. Designed three adaptive update granularity options avoiding hardcoded thresholds: Option A (data-driven triggers using sqrt(N_existing) for prior updates, prediction error for calibration refits), Option B (document-aligned with Welford EMA), Option C (observation-stream with confidence-blended weights). Recommended hybrid of A+C. Feedback loop mitigation via 1-sigma cap on per-update prior shift. All written to `docs/research/fsm-states-considerations.md` FSC-9.

117. **Task - Adaptive calibration implementation**: Implemented Option A adaptive calibration with hot-load, continuous prior reshaping, and sqrt(N) trigger schedule<br>
    **Result**: Created `kg_builder_cli/curing/adaptive.py` with two classes: `AdaptivePriorState` tracks per-type posterior EMA with alpha=2/(n+1), Welford online variance for sigma-cap feedback mitigation, and sqrt(N) update trigger schedule derived from 1/sqrt(n) sampling error rationale - no hardcoded thresholds, all parameters derived from observation counts and running statistics. `CalibrationHotLoader` wraps `PosteriorCalibrator | None` with interior mutability so the calibrator fitted at consolidation replaces the run-start calibrator for all subsequent cured-phase documents in the same run. Modified `ObservationCollector` in `curing/observation.py` with `on_observation: Callable[[str, float], None] | None` callback that fires after every `record_cross_type()` (twice, for type_a and type_b) and `record_type_assignment()` (once, for type_after with max posterior). Modified `BayesianTypeResolver` in `extraction/type_resolver.py` to accept `adaptive_state` parameter - when present, `resolve()` uses `adaptive_state.get_adjusted_priors()` instead of the frozen `self._prior`, falling back to frozen when adaptive returns empty. Threaded `adaptive_state` through `ingest_document()` in `extraction/unstructured.py` to `BayesianTypeResolver` constructor. Modified `_fit_and_persist_calibration()` in `cli.py` to return `(type_metrics, fitted_calibrator)` tuple instead of just type_metrics. In `_ingest_fluid()` curing path: after `_fit_and_persist_calibration`, hot-loads the fitted calibrator into `CalibrationHotLoader`, creates `AdaptivePriorState.from_type_metrics()` seeded from cured-phase frequencies, wires `collector.on_observation = adaptive_state.record_observation` so every observation during cured phase feeds back into the adaptive prior. Cured-phase `ingest_document()` calls pass `adaptive_state=adaptive_state`. Added 2 new event types (`AdaptivePriorUpdated`, `CalibrationHotLoaded`) in `events/types.py`, 2 new signals in `events/signals.py`, 2 new handlers in `events/handlers.py` registered in `register_default_handlers()`. `AdaptivePriorState.record_observation()` emits `adaptive-prior-updated` when the sqrt(N) trigger fires, including sigma_cap_applied flag. Added Section 14.7.1 Adaptive Calibration to `docs/KGF_DESIGN.md` covering hot-load, sqrt(N) trigger rationale, continuous EMA reshaping, Welford variance, sigma cap, and batch operation boundaries. Updated CLAUDE.md with planning workflow rule (every plan must include acceptance criteria). Created `tests/test_adaptive.py` with 26 tests across 10 test classes covering sqrt(N) trigger boundaries, EMA convergence, sigma cap, normalization (sum to 1.0 at 1/10/100/1000 observations across 12 types), factory initialization, cold start, new type incorporation, sparse type bounding, feedback loop resistance, performance (10K+ observations < 1ms/call), hot-loader swap/passthrough, and observation callback firing. 488 tests pass (462 existing + 26 new), lint clean.

118. **Task - TrustGraph reference and enhancements doc**: Added TrustGraph project insights and created prioritized enhancement document for KGF<br>
    **Result**: Cloned `trustgraph-ai/trustgraph` to `/tmp/trustgraph` and performed deep analysis of its extraction pipeline, ontology handling, entity normalization, provenance model, and Context Cores architecture. Created `references/projects/trustgraph-insights.md` documenting patterns relevant to KGF: OntoRAG per-chunk ontology subsetting via embedding similarity (send only relevant type subset to each chunk), type-aware deterministic entity URIs with "80% rule" (deterministic handles most cases, Bayesian for the rest), PROV-O provenance chain (Document -> Chunk -> Activity -> Triples with LLM model and ontology version tracking), domain/range constraints in extraction prompts, separate definition extraction pass for better embeddings, and Context Cores as versioned knowledge bundles. Created `docs/enhancements.md` with 13 prioritized enhancements filtered to those that actually matter for graph quality and usability. P0: E01 (unify resolution_intent/guide dual-source) and E02 (post-curing graph consolidation H5b, estimated +2-4 hybrid points). P1: E03 (type coercion in prompt H5c), E04 (synonym resolution H5g-D targeting 36 tubing and 38 filter variants), E05 (multi-label entities H5g Layer 3), E06 (expand guide scope to relationship patterns and merge/split rules), E07 (OntoRAG per-chunk subsetting from TrustGraph). P2: E08 (pre-scan resolution map), E09 (SleepStyle mode fix H10), E10 (judge improvement H9), E11 (provenance chain). P3: E12 (MCP server), E13 (context cores). Includes dependency graph and recommended execution order with cumulative impact estimate of +4-8 hybrid points from Track A alone.

119. **Task - Module rename to kgf and packaging updates** (v0.4.30): Renamed main Python module from `kg_builder_cli` to `kgf`, added packaging infrastructure for PyPI publication<br>
    **Result**: Renamed `kg_builder_cli/` directory to `kgf/` via `git mv` to preserve history. Replaced `kg_builder_cli` with `kgf` across 85 Python files (imports and references) via `sed` on all .py files under `kgf/` and `tests/` excluding `.ipynb_checkpoints/` and `__pycache__/`. Updated `pyproject.toml`: package name `kg_builder_cli` -> `kgf`, `[project.scripts]` entry point to `kgf = "kgf.cli:main"`, `[tool.setuptools.packages.find]` include pattern to `kgf*`, `[tool.ruff]` src/include/known-first-party all updated to `kgf`. Added new `[project.urls]` section pointing to `https://github.com/stellarshenson/knowledge-graph-foundry` for Homepage/Repository/Issues (using the existing GitHub remote alongside GitLab origin). Updated `Makefile`: `PROJECT_NAME` to `knowledge-graph-foundry`, `MODULE_NAME` to `kgf`, `data` target path to `kgf/dataset.py`. Added new `publish` target after `build`: depends on `build`, auto-installs twine if missing via `uv pip install twine`, runs `python -m twine check dist/*` for validation, then `python -m twine upload dist/*` for PyPI upload. The `install` target already had `increment_version_number` as dependency - version auto-bumped from 0.4.29 to 0.4.30 during `make install` rebuild. Initial pyproject.toml edit placed `[project.urls]` between `classifiers` and `dependencies` which would have caused `dependencies` to be swallowed by the `[project.urls]` table - caught during review and relocated to after `dependencies` and before `[project.optional-dependencies]`. First PyPI publication attempt as `kgf` failed with 403 Forbidden because the distribution name was already claimed by another user (Naresh khuriwal) on pypi.org, confirmed via `curl -sI https://pypi.org/pypi/kgf/json` returning 200 with existing owner. Changed PyPI distribution name to `knowledge-graph-foundry` (hyphens) after confirming availability via 404 check, successfully published v0.4.32 at https://pypi.org/project/knowledge-graph-foundry/0.4.32/, then republished at v0.4.33 on user request. User then requested renaming the Python module directory itself to match the distribution, so performed a second rename `kgf/` -> `knowledge_graph_foundry/` via `git mv` with word-boundary sed (`\bkgf\b`) across all Python files in module and tests to avoid matching inside longer identifiers. Updated `pyproject.toml` `[project.scripts]` to `kgf = "knowledge_graph_foundry.cli:main"` (CLI command stays `kgf`), `[tool.setuptools.packages.find]` include pattern to `knowledge_graph_foundry*`, `[tool.ruff]` src/include/known-first-party updated. Updated `Makefile` `MODULE_NAME` to `knowledge_graph_foundry` and `data` target path. Established three-tier naming convention: distribution name `knowledge-graph-foundry` (hyphens, PyPI), Python module `knowledge_graph_foundry` (underscores, imports), CLI command `kgf` (short, unchanged). Version bumped to 0.4.34, not yet published. All 488 tests pass after both renames, lint clean. Saved durable feedback memory `feedback_use_journal_skill.md` requiring all journal edits to go through journal:create/update/archive skills after a prior direct edit displaced entry 117's Result body to the end of entry 119.

120. **Task [Extended] - R01 verdicts, retrieval-first SOTA round, R02/R03 implementation** (v0.1.2): Measured the R1-R8 rebuild, ran a six-thread external research round, pre-registered and implemented the R02/R03 retrieval-first batches with probe-set ablations<br>
    **Result**: R01 CPAP rebuild (26 docs, 66 min) recorded in `docs/experiments/kgf-redesign-experiments.md`: recall roughly tripled (4601 entities, 6717 rels), orphan_rate halved to 0.059, duplicate density DOWN to 0.002, but 65 entity types and 334 relationship types exposed ungoverned type surface - H2/H3/H4/H6/H7 Promoted, H5/H8 Kept with caveats. Six Opus research threads (graph shape, traversal, faithfulness, maintenance, topology+agent navigation, millions-scale) synthesized into `docs/sota-decision.md` R02 section; 20 papers archived to `references/papers/` per new project rule. Key findings: no SOTA system promotes values to types, NodeRAG-style propositions are the weak-reader lever, avg_degree 2.49 sits near failing systems. Implemented R02: values-as-properties extraction constraint + deterministic `value_likeness` demotion guard (`ontology/clustering.py`), deterministic proposition nodes (`graph/propositions.py`, 8508 backfilled, faithful by construction, zero LLM), Chunk/Document provenance nodes + passages in the PPR projection, kNN SIMILAR_TO densification. R03: comparison decomposition, head+tail context ordering, per-claim citations, structural abstention gate. Also: DEF-1 embedding cache fixed (per-mention re-embedding eliminated), kill-resume proven with fingerprint-keyed skip (revised files re-ingest), five-backend engine matrix test-pinned (Bedrock/Anthropic/OpenAI/vLLM/llama.cpp), 28-probe gold set built. Measured ablations: propositions +10.6% evidence recall and lift Haiku to strong-reader parity (0.750); densification created 5640 edges but zero retrieval delta (kNN redundant with vector seeding - honest negative); top-score-only abstention refuted (unanswerable scores 0.79-0.86 overlap answerable 0.74-0.95 - similarity measures topical proximity, not answerability). H10 rebuild on second Neo4j pending final verdicts. Suite 272 green.

121. **Task [Extended] - R04 audit arc to 28/28, DEF-3 curing gate, R06 contrarian slate** (v0.1.3): Closed the CPAP probe set via fidelity+identity audits, replaced the curing gate after live premature-cure, pre-registered contrarian round<br>
    **Result**: R04-H22 fidelity audit: verbatim quote propositions (`graph/propositions.py` `generate_quote_propositions`, pure text-side scan - NOT provenance-anchored, so the audit cannot inherit extraction's provenance failures) plus trigram-Jaccard diversity filter on proposition retrieval (measured separation: clones 0.42-0.79 vs distinct 0.02-0.12, threshold 0.4) took probes 26/28 -> 27/28; iteration 3 exposed reader over-attribution (P25 false-answered from another device's unscoped entity) fixed with an attribution rule in the reader prompt. R04-H21 identity audit: four deterministic alias detectors (`graph/aliases.py` - explicit assertion, deictic via document-primary entity, model-code cluster, normalized name) needed four iterations, each killing a measured false-alias class (measurement tokens 15mm/2.5kg, generic primaries, filename-signal, extraction-provenance membership after 'SleepStyle 200 Series' appeared in ZERO chunks of its own manual) - P09 closed via SAME_AS alias-cluster render, 28/28. DEF-3: wave-1 plateau gate cured 481-doc stream at doc 4; two rejected fixes (backlog-proportional floor - corpus size unknowable on unbounded stream; count floor - hardcoded scale constant, both user-rejected) led to Good-Turing missing-mass UCB gate (`ontology/curing.py`: n1/N + z*sqrt(n1+1)/N <= 0.05, only dimensionless constants), validated by H32 replay (98.55% forward observation-mass coverage vs plateau's 85.14%) then live-bounded by wave 1b curing legitimately at doc 7 - no within-sample gate anticipates content shift, hence H33 pre-registered: cure must be reversible. R06 contrarian slate H27-H33 registered with six-source literature sweep, 9 paper pairs added to `references/papers/`. Bedrock Sonnet daily quota exhausted after five scoreboards; final 28/28 rescored offline deterministically from saved answers. 296 tests green.

122. **Task - R07 contrarian slate 2, three completion-free verdicts** (v0.1.3): Pre-registered 16 hypotheses (H34-H49) attacking surviving assumptions, ran the three that need no completions<br>
    **Result**: Trigger: user question "do embeddings help find the first node?" - P09 forensic showed the vector index (sole graph entry) failing where structure rescued. Registered R07 in `docs/experiments/kgf-redesign-experiments.md`: entry point (H34/H40/H41), graph's right to exist (H37/H38/H42/H46/H48), lifecycle (H35/H47/H49), process (H36/H39/H43/H44/H45). H34 confirmed (`probe_eval_r07h34.ipynb`): full-pipeline evidence recall 1.0 but only 63.6% of golds enter via direct vector seeds; pure-vector ablation loses 33.3%; matcher needed three iterations (unit variants 28 dB(A)/28 dBA, unit-in-key values dimensions_mm). H37 confirmed (`probe_eval_r07h37.ipynb`): PPR 99.4% contained in seeds+1hop, ranks OUT gold-bearing neighbours on 20 probes - lossy filter, not expander, at <5k entities. H47 interim (`entity_growth_h47.ipynb`): Heaps exponent b=0.803 (saturating) on wave-1b 130-doc prefix, final-window b=0.966 flagged for wave-end re-fit (single cluster c34, so not a content boundary). H34's miss-list promotes H40/H41 to scheduled. matplotlib added to dev extras.

123. **Task - R07-H50 CFAR formal refutation on drift stream** (v0.1.3): User requested formal trial of the CFAR criterion the H33 note rejected by argument<br>
    **Result**: Registered H50 as steelman (including m-of-n binary integration, the sustained-target variant radar practice deploys) and raced five detectors at MATCHED false-alarm budget (ARL0 ~500, bisection-calibrated) in `drift_cfar_h50.ipynb` - Binomial noise over realized entities-per-doc distribution, real wave-1b remap series as clean check. CFAR REFUTED at all noise floors: ramp miss rates CUSUM 0.00 vs CFAR 0.23-0.99; self-masking confirmed (detects at onset or never - reference window absorbs the shift). Iteration 1's failures were themselves evidence: censored ARL0 band let never-alarms pass as calibrated; eps floor turned OS-CFAR into a fixed threshold; trailing-median CUSUM self-masked like CFAR (fixed with quiescent-only adaptation). Surprises: production all-3-consecutive MATCHED CUSUM at delta=0.30 (pre-registered clause refuted, criterion rehabilitated for large shifts); plain-rate CUSUM false-alarmed on the real single spike doc - entity-count weighting needed regardless of detector family.

124. **Task - R08 slate: 5 contrarian + 5 conformist hypotheses, H53 adjudicated** (v0.1.3): User requested 5 more contrarian and 5 conformist deep-dig hypotheses; registered as R08 (H51-H60), first entry run same-day<br>
    **Result**: Contrarian camp attacks layers no round touched: H51 PDF-parse fidelity (grounded in the H21 zero-chunks forensic), H52 merge-nothing federation (every defect class exists only because ingest merges), H53 BM25 vs the embedding bill, H54 Bayesian-resolution-as-numerology (grounded in measured 44% calibration accuracy), H55 purpose-as-placebo (audits the project's own use-case doctrine). Conformist camp deepens promoted results: H56 proposition coverage (best channel per H34), H57 Good-Turing UCB for relationship/property inventories (H23's statistical footing), H58 collective-evidence alias detector (Bhattacharya-Getoor), H59 binomial-LLR CUSUM (closes both H50 gaps), H60 Less-is-More pruning. H53 executed in `probe_eval_r08h53.ipynb` with budget controls: BM25 0.583 vs vector 0.667 at k=8 (12.5% gap - dead-zone inconclusive); RRF@8 0.646 and union@16 0.750 both lose to vector-alone@16 0.854 - hybrid refuted as fusion effect. Discovery: doubling top_k is the cheapest seed-gap fix (+28% relative pure-seed recall); H40/H41 must now beat vector@16.

125. **Task - R09 slate: graph topology round, 20 hypotheses** (v0.1.3): User requested 20 hypotheses on rebuild policy, communities, graph-theoretic markers and their impact on traversals; registered as R09 (H61-H80)<br>
    **Result**: Audit first: Leiden already writes `communityId` at optimize (`graphrag.py:52`) but nothing reads it at query time; Heaps (H47) is the only macro marker; the rebuild trigger is JSD over type frequencies - blind to topology. Registered four themes with a panel doctrine (a metric earns its place only if it predicts probes, fires earlier than existing triggers, or improves render decisions at matched budget - the H37 lesson generalized): structural markers H61-H67 (hub truncation, neighbor-Jaccard twins, dark-matter census, betweenness bottlenecks, assortativity trend, k-core fingerprint, hop-distance law flagship), communities H68-H72 (provenance-artifact NMI test, diversified seeding vs the 0.854 bar, conductance merge gate, context-cores retest, closure densification), rebuild H73-H76 (rebuild-equals-maintenance, composite trigger, partial rebuild, order invariance), coupling H77-H80 (spectral early warning, degree-aware render budgets, entropy-ranked rendering from the 0.58 correlate, sparsification safety line). 14 of 20 runnable without completions; H72/H80 form a deliberate densify-vs-prune tension pair.

126. **Task [Extended] - R10 research slate + R11 weak-flank slate + papers skill** (v0.1.3): User requested deep research (graph theory, GNNs, ontology optimization, improvement gradient) via subagents, 20 research-grounded hypotheses, then 20 more targeting the system's weak flanks; also a global papers skill codifying the digest format<br>
    **Result**: Four Opus research scouts swept curvature/spectral mathematics, GNNs for KG quality, information-theoretic optima, and ontology optimization - 24 verified papers, ~30 hypothesis kernels. Registered R10 (H81-H100) answering the user's optimum/gradient question with a two-axis doctrine: ingest saturation (Good-Turing/Heaps, already instrumented) vs task utility - a foundry potential Phi(G) = probe-evidence sufficiency minus lambda x tokens whose per-edit gain is the improvement gradient; if empirically submodular (RPQ view-selection precedent proves the class), a swap-stable fixed point certifies "no single edit improves" (Krause-Golovin). Themes: gradient (H81-H86), curvature detectors repurposed from over-squashing theory to defect detection (H87-H91: AFRC on false SAME_AS, effective-resistance audit, inter-community curvature bound on the 47 duplicates, Betti-curve drift monitor, super-hub autopsy), learned quality at honest scale (H92-H97: ULTRA zero-shot plausibility, LLM in-context clustering vs the 44%-calibrated resolver, structure-vs-text null, differentiable edit ranker, chain variance), ontology optima (H98-H100: granularity operators - the literature has NO published granularity objective, latent hierarchy via box-geometry repurposing, ROMEO-style metric profiles). Registered R11 (H101-H120) on the named weak flanks: identity layer (H101 200+-pair benchmark - every resolver metric rests on 25 pairs, H102 decision-theoretic thresholds, H103 invariant vetoes, H104 split-remerge instability signal, H105 sibling stress set, H106 ensemble arbitration, H107 root-cause partition of the 47), retrieval correctness (H108 attached-proposition render fix for the 6-gold gap, H109 channel-knockout fragility matrix, H110 paraphrase stress, H111 calibrated abstention, H112 ordering null), the goal-critical SOTA comparison (H113 head-to-head vs GraphRAG/LightRAG/HippoRAG-2/vector/BM25, H114 public benchmark, H115 amortization frontier, H116 ablation ladder, H117 corruption robustness), persistent failures (H118 mode-family class repair, H119 extraction determinism, H120 ship H59 detector). Created global `papers` skill (~/.claude/skills/papers/SKILL.md) codifying the user's AUTOMEM-example digest format; library agent building all 24 paper digests into references/papers/.

127. **Task - R11 execution: H108 refuted, H107 confirmed, identity flank re-mapped** (v0.1.3): Autonomous execution of the weak-flank round's completion-free entries after user delegated sequencing ("You decide what to run and when")<br>
    **Result**: H108 (attached-proposition render fix) REFUTED through three configurations in `probe_render_r11h108.ipynb` - the "6 PROP-ATTACHED golds" premise dissolved under rank forensics: only 2 have real carriers (global ranks 16/22, recoverable only at +20-32% tokens), the other 4 were H61 placement-loop mislabels plus fuzzy-matcher degeneracy on short golds (value_tokens("SD card: > 1 year") reduces to ["1"]). Architectural conclusion: the global proposition channel IS the correct mechanism, not redundancy - no render change ships; H61/H56 corrected in ledger. Identity forensics in `identity_forensics_r11.ipynb`: class-shape discovery - ZERO cross-type duplicate pairs at cosine>=0.9 on the rebuilt graph (the "47" record is stale); real class = 66 same/overlapping-type pairs partitioned 71% extraction variance / 29% resolver-visible miss - H107 CONFIRMED (bar 0.60), promoting H119 (extraction canonicalization) to primary identity lever. H103 invariants interim-refuted as signal-starved (manufacturer coverage 12%, veto coverage 0.17); 8 suspicious spec-conflict fires on model_code SAME_AS merges (73 of 127 edges, the false-merge surface: AirFit P10 mask <-> Transcend P10 battery) routed to H101 adjudication. Memory updated.

128. **Task [Extended] - Executor-fleet burn-down: 26 verdicts in one morning, SOTA rebuild launched** (v0.1.3): User froze registrations and directed overnight backlog clearing with SOTA-promotion tracking; after an idle-night failure (logged to memory as the always-a-job-in-flight rule), stood up the unattended machine<br>
    **Result**: Architecture: chained background jobs + parallel Opus executor agents, verdicts recorded centrally section-scoped (after repairing five mis-anchored fills the count audit exposed - H33 and the topology quartet had landed in wrong entries; fixed, process hardened). Adjudicated this morning: H59 REFUTED (the doc-75 spike is a genuine full-remap event, not a small-denominator artifact - evidence weighting cannot give spike immunity, only persistence can; production all-3-consecutive detector doubly validated with H50 and retained for the SOTA config) and H120 moot. R09 batch (11): H68 communities=provenance CONFIRMED (NMI 0.614), H84 thin sufficient subgraph CONFIRMED strongly (10% of edges hold 100% recall), H77 weak spectral CONFIRMED; H62/H64/H65/H66/H69/H70/H72 REFUTED (the sparsity/on-seed regime starves every structural detector; H70 anti-informative - false merges live INSIDE communities), H80 marginal (20% zero-loss pruning capacity). R12 bake-off (7): Titan cosine UNBEATEN by cross-encoders (-0.04), local bi-encoders (-0.19), instruction-7B (-0.34), NLI (-0.26), field split (~0), ensemble (+0.004), node2vec (below chance, structure ANTI-correlated with identity) - multi-directional confirmation that calibration (H129), not the scorer, is the identity fix; one new signal shipped-able: NLI contradiction as defer-band sibling veto (90% recall). R13 transport (6): H134 CONFIRMED caveated (neighborhood transport works exactly where literal overlap is blind); H132/H133/H135/H136/H137 REFUTED mostly on their own registered refuters; H135 note - one-to-one assignment broke 5/6 false merges, capacitated variant queued. Tally 65/143. In flight: agent 4 (H51/H54/H56/H57/H101 keystone benchmark on the local model), SOTA chain (config-sota.yml = vector@16 delta; rebuild at 9/26 docs, then optimize, then wave 2 auto-launch). Promotions ledger docs/sota-promotions.md maintained throughout.

129. **Task - R10 potentials family: five verdicts on the two foundry potentials** (v0.1.3): Executed the pre-registered R10 gradient batch H81/H82/H83/H85/H86 as deterministic in-memory edit-replay over the read-only neo4j2 CPAP graph, using H84's ablation pattern and the H61/H34 fuzzy-evidence matcher<br>
    **Result**: Built/executed `potentials_r10.ipynb` (report `potentials-r10-20260707-120032.json`); reproduced 0.697 baseline exactly. H81 PARTIALLY CONFIRMED - `Phi_task` = sufficiency minus lambda x tokens; all 10 missing golds reachable (ceiling 1.0), cost-benefit greedy hits 0.879 within +10% tokens but crosses 0.9 only at +10.9% (clause a fails by a hair), while monotone decay (b) and the swap-stable fixed point at 0% supermodular pairs (c) hold - the gradient-plus-certificate machinery is validated. H82 CONFIRMED - star-forest MDL saves 31751 bits; materialize dMDL=0 vs dark-removal +16 = non-empty divergence set. H83 CONFIRMED - sign-pair aligns 100%; corrected PROP-ATTACHED premise from 6 to 2 real. H85 CONFIRMED - missing-mass vs task-gradient r=-0.18, axes independent. H86 REFUTED - recalled evidence 91% on-seed, no view cuts cost at equal recall (registered honest-risk outcome). Recorded all five verdicts in `kgf-redesign-experiments.md`. Discipline call: rejected a fragment materialization that spuriously cleared +10% by exploiting matcher degeneracy on 17/33 short-numeric golds.

130. **Task - Three-arm benchmark: SOTA config vs baseline** (v0.1.3): Measured the promoted SOTA retrieval configuration against the recorded baseline on one comparable metric - pure-seed evidence recall over the 24 gold CPAP probes (H34/H53 convention), three arms isolating the pure lever from the full-stack rebuild<br>
    **Result**: Built/executed `notebooks/three_arm_benchmark.ipynb` (report `reports/three-arm-benchmark-20260707T123450Z.json`), READ-ONLY on both graphs, Bedrock Titan query embeddings only, no LLM judge. Arm1 baseline `neo4j2` (2798 ents) @k8 = 0.6667 (13/24), reproducing the recorded H53 vector@8; recorded v28 provenance is 0.90 hybrid / 60-63 det from the archived v1 harness (different metric, cited not re-run). Arm2 same graph @k16 = 0.8542 (20/24), the H53 lever - **+0.1875 absolute, +28.1% relative = honest headline**. Arm3 SOTA `neo4j` (~3.7k ents, local gpt-oss-120b extractor) @k16 = 0.7292 (14/24) = +9.4% relative full-stack, but BELOW Arm2 because the extractor swap (confound) dilutes the lever: SOTA@8->@16 adds only +0.0417 vs +0.1875 on baseline. Movers: k16 surfaces P01/P03 (rank 9-16) and five comparison probes; SOTA gains P19 (mode) but loses P08 (power), P14 (capacity) to extraction variance. Open flag: SOTA instance entity count drifted 3661->3693 across reads.

131. **Task - R19 token-economy batch + DEF-4 wrong-instance ingest** (v0.1.3): Adjudicated the retrieval token-economy trio H180/H181/H182 (plus H179/H183 interims) and root-caused the SOTA-graph contamination the three-arm executor flagged<br>
    **Result**: R19 executor (`token_economy_r19.ipynb`, report `token-economy-r19-20260707-123621.json`): H180 PARTIALLY CONFIRMED - fanout cap recall-safe at k=5 (zero loss) but 1-hop is only 17.6% of render mass, propositions 61.8%, so hub-p95 savings underdeliver; H181 PARTIALLY CONFIRMED - misses render 5x CHEAPER not blind (p=5.5e-7), top-seed-similarity 0.668 detects 86.7% at 0% false-abstention, short-circuit cuts miss tokens 95%; H182 PARTIALLY CONFIRMED - budget knee at B=30%, zero-loss at B=60% (~40% cut), 2% bar missed by exactly one gold. Three defaults promoted; H184/H185/H186 registered from the open questions. DEF-4: `.env NEO4J_URI` unconditionally overrode `--config` in `settings.py`, so the chain's wave-2 step wrote 77 docs into the SOTA instance (26 -> 103) instead of neo4j3; fixed (env fills gaps only), stale inverted-precedence test replaced with two contract tests, wave 2 killed and relaunched against neo4j3 (verified), defect filed in `docs/defects.md`.

132. **Task - R18 potentials extensions: four verdicts, views closed for good** (v0.1.3): Adjudicated H171/H173/H174/H175 from the executor batch extending the R10 gradient machinery<br>
    **Result**: `potentials_ext_r18.ipynb` (report `potentials-ext-r18-20260707T130627Z.json`), baseline 0.697 reproduced. H171 PARTIALLY CONFIRMED - sharp knee at +5.6% tokens/0.879 recall, 10.7x post-knee marginal cost, but knee-CI 14.4 pts (33-gold sample limit, tightening deferred to H186) and H81's +10% miss re-adjudicated as budget artifact; promoted: certificate carries a data-driven knee budget. H173 CONFIRMED - verbatim splitting of 13 fat propositions cuts PROP-attached cost 3-4x, zero regressions; promoted as ingest operator. H174 CONFIRMED - type edits move MDL bits but probe recall exactly 0 across all 79 edits; ontology is task-decorative for retrieval, its value is upstream. H175 REFUTED - no typed template lifts held-out recall in budget; answer-specific control matches typed templates; views closed under both framings (H86 final), conditional reopening trigger registered as H187 (on-seed share < 75%).

133. **Task - R19b forensics: dilution root cause, abstention gap, probe-instrument rule** (v0.1.3): Adjudicated H184/H185/H186 from the forensics executor batch<br>
    **Result**: `forensics_r19b.ipynb` (report `forensics-r19b-20260707T131124Z.json`). H184 CONFIRMED - the SOTA-graph deficit is 80% embedding dilution (carriers rank 8->83, 9->32 etc.), 20% fragmentation; promotes H119 canonicalization as the fix for BOTH identity and retrieval flanks. H185 PARTIALLY CONFIRMED - near-domain misses (right product, absent fact) defeat the shipped seed-similarity detector completely (0% detection at mean top-sim 0.768) and the registered keyword fix fails (26.7%, or 86.7% at 50% false-abstention); calibrated presence stage registered as H189. H186 REFUTED on its risk clause - the 130-gold graph-derived wide set is clean (82.3%) and tight (CI 1.15 pts) but self-fulfilling (99.2% recall, k-lever erased); discipline promotion: probe expansion must be document-grounded (successor H188). Registered H188/H189; wide-set artifact versioned at `data/processed/probes-wide-h186.json`.

134. **Task - Parser round adjudicated: text beats vision, the absent family dissolves** (v0.1.3): Recorded all six H146-H151 verdicts from the finisher agent (original executor died at session limit; GPU outputs were cached, scoring ran from disk)<br>
    **Result**: `parser_round_scoring.ipynb` (report `parser-round-final-20260707-135106.json`). H146 CONFIRMED - Docling 86.3% loss-set recovery, beats the union baseline, CPU-only. H147 REFUTED spectacularly - the all-parser-absent mode family is a trademark-glyph normalization artifact; every text parser recovers 7/7 under symbol-stripped matching, dissolving the standing benchmark mystery; fix registered as H190 (string operator, not parser). H148 NOT MEASURABLE - MinerU2.5 VLM tensor-shape crash (10/11 chunks); vision residue deferred as conditional H191. H149 CONFIRMED - olmOCR anchoring inherits text-layer losses (81.1% vs 87.4% image-only). H150 CONFIRMED - OmniDocBench rank does not predict corpus name recall; H51 harness promoted to standing parser acceptance test. H151 PARTIALLY - 75.8% of losses are in table regions, but a table stage is not necessary (table-less pypdf tops at 93.7%); the differentiator is escaping pymupdf4llm's cell-merge bug. Parser decision promoted: pypdf union / Docling backend swap. Table co-processor (H153 first) unblocked.

136. **Task - Instrument revolution: entailment scorer ships, wide benchmark adopted, H81 deflates** (v0.1.3): Adjudicated H172/H176 (entailment instrument + graded gradient) and H188 (document-grounded wide probe set)<br>
    **Result**: H172 CONFIRMED (`entailment_instrument_r18.ipynb`) - mDeBERTa NLI at 1.77 ms/pair, 94% human agreement, all 17 degenerate golds fixed; ships as THE harness instrument. Payload: H81's +18pp greedy lift was largely fuzzy-matcher artifact (honest gradient +3pp; the candidate MENU was matcher-built); H81/H83 carry post-verdict caveats, gradient promotion amended, honest re-derivation registered as H192. H176 REFUTED - graded instrument restores variance (CoV 0.72) but reveals NEGATIVE missing-mass/gradient dependence (r=-0.406, n=6 vacuous CI); H85 downgraded with back-reference. H188 REFUTED at its numeric bars but the objective is won (`wide_probes_h188.ipynb`): the 101-gold document-grounded set restores real difficulty (77% vs 99% self-fulfilling) and is ADOPTED as the standing wide benchmark; k-lever collapse (+4 vs +24 pts) exposes a ~81% corpus-coverage ceiling - the 19% residue maps to ingest-fidelity territory, census registered as H193. CI-bar discipline recorded (paired-delta or n~500+).

137. **Task - Replication crisis: the duplicate agents disagree, instrument decision reopened** (v0.1.3): The relaunched H172 and H188 executors turned out to be duplicates (originals had not died) - both pairs completed independently with conflicting verdicts; adjudicated the conflicts<br>
    **Result**: H172 run B (balanced 25/25 audit) found NLI at 78% agreement with catalogued value-identity failures (present numerics scored low, absent look-alikes high) vs run A's 94% on an unbalanced sample - the CONFIRMED verdict and instrument promotion are CONTESTED via post-verdict notes; both runs converge on a deterministic value-extraction comparator as successor, adjudication registered as H194 (balanced blind bench, router hypothesis: comparator for numerics, NLI for prose). H188 run B (feature-weighted 114 golds) saturated at 92.1% vs run A's catalogue-weighted 77% - difficulty is RULE-MIX-dependent; run A's set stays canonical, run B saved as h188b; run B also exposed the HNSW exact-k harness bug (k-lever 54.5 vs 72.7 at top_k=48-truncated) - convention audit of H53/three-arm registered as H195. H176 replicate agrees within noise (r=-0.26 vs -0.41, both underpowered). Artifact collisions resolved; duplicate-launch lesson recorded: verify agent death before relaunching, unique output names per run.

138. **Task - Ingest-fidelity pair ships: glyph operator + header carryover** (v0.1.3): Adjudicated H190/H153 from the glyph-carryover executor<br>
    **Result**: `glyph_carryover_r14.ipynb` (report `glyph-carryover-r14-20260707-141157.json`). H190 PARTIALLY CONFIRMED - the deterministic glyph operator recovers all 69 sym-strip-rescuable absent names end-to-end with exactly zero false conflations across the 2797-name vocabulary; ships in parser post-process and resolver name detector. Honest scope limit: 0/66 H107 variance pairs affected - that variance is word-level (qualifiers, order, expansions), confirming yet again that H119 extraction canonicalization is the identity lever. H153 CONFIRMED on every clause - header carryover closes all 64 severed table-row associations at 0.258% overhead with zero false injections (the separator anchor makes prose misfire near-impossible); shipped chunker untouched, transform validated at 28/28 offset parity. Both promoted; H152 row-records now must justify on extraction lift alone.

139. **Task - Instrument contest settled: deterministic comparator wins, GPU-free router ships** (v0.1.3): Adjudicated H194 (pre-registered blind balanced bench adjudicating the H172 replication conflict)<br>
    **Result**: H194 CONFIRMED (core) with the NLI-prose sub-clause REFUTED (`instrument_bench_h194.ipynb`, frozen bench `instrument-bench-h194.json`). On 60 blind-adjudicated pairs (30/30 balanced, 4 strata) the deterministic value comparator scores 1.000 on its home numeric+dimension strata (49 pairs) and corrects every failure that broke both predecessors - 19 NLI value-identity errors and 10 fuzzy near-miss false-positives reproduced exactly. Shipped instrument: GPU-free router (comparator + word-overlap) at 0.967 agreement; NLI loses even the prose stratum (0.714 vs word-overlap 0.857). Run B's contest vindicated; H172 run-A verdict superseded via post-verdict note, fuzzy retirement stands. Open questions registered per directive: H196 (drop NLI entirely via stop-word-filtered overlap) and H197 (bench snapshot pinning - 3 pairs drifted with graph re-embedding). H195+H196+H197 executor launched under the settled instrument.

140. **Task - Honest gradient confirmed, coverage ceiling flips ingest-side, variational round opens** (v0.1.3): Adjudicated H192/H193 from the honest-gradient executor; registered H198-H201<br>
    **Result**: H192 CONFIRMED (`honest_gradient_r18.ipynb`) - evidence-search menu derivation lifts recall 0.667 -> 0.970 at +0.28% tokens, monotone and swap-stable, sign-pair 98%; H172's "greedy stalls" was fuzzy menu contamination, and the predicted extraction-gap residue is zero on the 33-gold set (router ceiling 33/33). Edit selector promoted, superseding the amended H81 magnitude. H193 REFUTED as-run but instrument-superseded: the pre-H194 scorer classifies only 37.5% of wide-set misses as absent, the settled router flips it to 85.2% absent with 87% mapping to known fidelity classes (18 catalogue-code table cells); refutation branch fired under neither instrument - the ceiling is ingest-side. Sealing run registered as H199 (launched) with the 3 unmapped absents and 4 dilution-suspect unranked golds as clauses. Per user direction, R20 opened: H200 retrodictive expected-information-gain test on the 113-verdict ledger (launched), H201 prospective EFE backlog ordering (conditional). H198 promotion-debt wiring registered earlier this hour.

141. **Task - Variational round closes: naive EIG anti-predicts hypothesis value** (v0.8.73): Adjudicated H200; R20 closed per its pre-registered closure clause<br>
    **Result**: H200 REFUTED and INVERTED (`variational_selection_h200.ipynb`) - on 109 adjudicated hypotheses with temporally honest scoring, uniform-prior per-flank Beta-Bernoulli expected information gain lands at rho -0.185 (bar 0.40), AUC 0.35-0.43 (bar 0.70), top-10 overlap 0/10, robust across five label-weight schemes including the circularity-severing one. Mechanism: MI is maximal on early low-stakes probes and decays with cumulative probing, but campaign value concentrated LATE - all ten most-informative hypotheses are promoted REFUTEDs on the hardest flanks (identity/fidelity posterior defect rates 0.75). Judgment-ordered selection stands unchallenged; H201 CANCELLED via its conditionality clause; the executor's reformulation idea deliberately parked as user-gated H202 rather than auto-registered (post-hoc model shopping would violate the closure clause). Methodology promotion recorded: future acquisition schemes should track persistence-against-refutation, not prior uncertainty.

142. **Task - Instrument program closes: convention audit clean, benchmark v2 ships, NLI dropped** (v0.8.73): Adjudicated H195/H196/H197 from the instrument-v2 executor; registered H203-H205, launched H204+H205<br>
    **Result**: H195(a) CONFIRMED (`instrument_v2_h195.ipynb`) - zero verdict flips, convention shift exactly 0.0 pt everywhere (HNSW exact at 2798 embeddings; cached three-arm numbers reproduced to the digit); the top_k=16 lever survives 6/6 cells; generous-truncation promoted as harness law; H188 run-B's exact-k gap did not reproduce - scale question registered as H203 (blocked on wave 2). H195(b) PARTIAL - `probes-wide-v2-h195.json` ships as the H113-ready benchmark (219 golds; catalogue+spec core r@8 0.618, lever 8.2 pt); the 7 consolidation golds saturate, refuting consolidation-is-hard - the hard multi-doc class is CONFLICT (H204). H196 PARTIAL - NLI dropped entirely (filtered overlap dominates every stratum), harness now deterministic CPU-only; residual 0.81 prose ceiling is attribution ambiguity (H205). H197 CONFIRMED - 2 ms snapshot fingerprint, 60/60 matched reproduction, guaranteed mutation trip; promoted as harness precondition. H204+H205 executor launched.

143. **Task - Sealing run refutes the flip: render surface is the uncontrolled variable** (v0.8.73): Adjudicated H199; H193's strategic claim suspended; registered and launched H206/H207<br>
    **Result**: H199 REFUTED (`wide_census_h199.ipynb`) - the first-class router census found unranked 62% / absent 38.3%, failing both seal bars; the registered second-look clause fired: the shipped router has no arm for catalogue codes (61% of the wide set, unmeasured in every wide number). Main-session forensics corrected the executor's graph-drift attribution: neo4j2's last write was 2026-07-06 14:48 - the 0.762 vs 0.406 divergence on an unchanged graph is HARNESS render-surface disagreement, plus within-notebook run variance (0.376-0.406). H193's ingest-side promotion suspended via correction entry; solid residue kept: the 3 unmapped absents are extraction drops and the H144 flag set extends to two more docs. H207 registered+launched (render-spec freeze + fingerprint-pinned 3-repeat census - now GATES all wide-set numbers including H113); H206 registered+launched (exact-identifier arm + device-seeded query rewrite for the E0601 class).

144. **Task - Conflict census: 87% of cross-doc value conflicts silently drop a value** (v0.8.73): Adjudicated H204/H205 from the instrument-refinement executor; registered H208-H210<br>
    **Result**: H204 PARTIAL (`conflict_attribution_h204_h205.ipynb`) - conflict golds meet both bars literally (15 golds, 66.7 pt harder than corroboration under both-values scoring) but honestly deflate to 3 physical conflicts (1 genuine same-unit); the census is the keeper: 87% of conflicts silently drop a value (both kept 2/15, neither 7/15) - the graph stores no provenance-tagged variants, now the R17 reconciliation layer's evidence base. Two unit-representation "conflicts" exposed the comparator's cross-unit gap (lbs/kg, in/mm) - registered H208. H205 PARTIAL - foreign-device exclusion scoping ships (+5.5 pt feature, prose 0.917 passes, zero numeric regression structural) but the bars are blocked by contestable bench labels: genuinely shared features scored absent - shared-feature stratum registered H209; conflict-density-at-scale registered H210 (blocked on wave 2). H208/H209 queued behind the H206/H207 executor (same notebooks).

145. **Task - DEF-5 found and fixed: the census divergence was a cross-instance bug, ceiling re-confirmed ingest-side** (v0.8.73): Adjudicated H207/H206; lifted the H193 suspension; filed DEF-5; registered+launched H211<br>
    **Result**: H207 CONFIRMED (`render_parity_h207.ipynb`) - the render-surface theory refuted by its own forensics: both harnesses assemble byte-identical renders; the entire 0.406-vs-0.762 divergence was H199's notebook seeding from the .env default instance (live-mutating under H158) while rendering from neo4j2 (DEF-5, filed with fix). Attribution: +0.327 instance, +0.030 exact-digit arm, +0.010 propositions. The pinned harness (explicit driver + dual fingerprints + canonical CPU scorer) reproduces recall@64 0.762 at zero variance x3 - now THE frozen wide-set definition gating H113. FINAL H193 classification: 66.7% absent / 33.3% unranked - suspension lifted, ceiling re-confirmed ingest-side (restated at two-thirds). H206 PARTIAL - exact-identifier arm ships at ceiling (1.000, zero false credit), closing the catalogue-code blind spot; device-seeded rewrite refuted (1/5) - unranked codes are render/linkage victims, H211 registered+launched for the last 8 golds.

146. **Task - Identity stack shipped in-engine: precision 52.2%, false merges land on the replay exactly** (v0.8.73): Adjudicated H158 after both A/B arms completed through the session-limit outage; registered H212/H213<br>
    **Result**: H158 DEGRADED per its registered condition, but the identity clauses PASS decisively (`identity_stack_h158.ipynb`): `resolution.identity_stack: v2` shipped behind a flag (default v1, 303 tests green) - isotonic-calibrated cosine from versioned artifact `identity-calibration-v2.json` + runtime-frozen logistic arbitration + global NLI veto, wired into both decision surfaces. E2E on the wiped scratch instance: SAME_AS precision proxy 11.8% -> 52.2% (bar 50%), false merges 30 -> 11 = the offline replay EXACTLY, zero wall-clock cost. The recall clause fails confounded: 0.875 -> 0.833, but the 2 probes' gold strings never entered the v2 graph (extraction loss under concurrent Bedrock contention, 119 vs 76 failed chunks) - upstream of resolution; the v2 census (2201 vs 2937 entities at fewer merges) re-confirms H119 as the primary identity lever. Default flip gated on H212 (clean-conditions recall re-run, blocked on quiet Bedrock); H213 registered (defer judge economics on the 2.4x band). Implementation committed as checkpoint.

147. **Task - The last eight priced out: residue closed as abstention territory, H119 test launched** (v0.8.73): Adjudicated H211; registered H214/H215; launched the H119 determinism experiment and the H208/H209 instrument executor<br>
    **Result**: H211 PARTIAL (`unranked_residue_h211.ipynb`, pinned H207 harness, baseline 77/101 reproduced exactly) - clause (a) confirmed with a full per-gold forensic map of the 8 unranked golds: 2 rank victims (brand-name-only embedding surface), 2 extraction misattributions (codes on the wrong entity), 1 linkable orphan, 1 cross-product spec, 2 hub-fan-in victims. Clause (b) refuted per its registered refuter: 5/8 recovery exists only at 51.6x the H171 knee (+288.7% tokens); best in-budget mix recovers 2/8 at +2.99% - the residue goes to the gap ledger as priced-out abstention (H161 doctrine). Promoted: firing-geometry residue-pricing instrument, prop-val linkage rule, monotone-scorer regression lemma. Half the residue is extraction misattribution - the fourth independent finding pointing at H119, which remains untested and is now running: 3-arm 5x variance experiment (production / canonicalization+temp0 / temp0-attribution) on the local model. H214 (post-canonicalization re-census) and H215 (salience-weighted linkage) registered; H208/H209 executor launched.

148. **Task - H157 unblocked with the papers corpus; R21 image-ingestion round registered** (v0.8.73): Owner selected the transfer corpus; user directed a 10-hypothesis round on images in documents<br>
    **Result**: H157's precondition resolved - the owner chose `references/papers/` (64 scientific-paper PDFs: different domain and genre, entity classes flip to methods/datasets/metrics/authors); registration amended with the resolution note and the vocabulary-overlap caveat, executor launched (30-PDF ingest on the wiped scratch instance, pinned config, v2 identity stack with the shipped calibration artifact - the ECE-transfer clause targets exactly those constants). R21 pre-registered per user direction: H216 census + extraction-tool contest (pymupdf vs Docling vs page-render, decided by experiment), H217 absent-gold pixel forensics, H218 describe-then-extract VLM contest, H219 one-index-or-two embedding route, H220 anchoring, H221 rendered-table transcription with the zero-digit-error clause, H222 diagrams as relation sources, H223 cross-modal identity under the v2 stack, H224 decorative filter, H225 knee economics. H216/H217 executor launched (the census gates round breadth); four executors now in flight plus wave 2.

149. **Task - Unit conversion ships; render-side attribution refuted, ceiling routes ingest-side** (v0.8.73): Adjudicated H208/H209; registered H226; H119 run stabilized under a watcher<br>
    **Result**: H208 CONFIRMED (`unit_conversion_h208.ipynb`) - dimension-aware conversion (mass/length/pressure/flow, 2% tolerance) joins the canonical comparator: both H204 unit-variant conflicts reconcile, zero regression on both frozen benches; clause (c) null reported honestly (single-unit-system corpus - value case is H210's version-diverse scale test). H209 REFUTED per its registered refuter (`shared_feature_h209.ipynb`) - under a fair blind shared/exclusive taxonomy the exclusion variant reaches only 0.833 exclusive / 0.907 combined, and 3 of 4 persisting errors are provenance gaps: features the product owns per its own documents that extraction never attached to its node - no render policy at any cap can retrofit that. Attribution ceiling routes ingest-side: H226 registered (feature-ownership attachment census). H119 operational note: the executor's restart attempts left three concurrent runs of the same notebook - two killed, the detached survivor continues from checkpoints (~10-13h), watcher armed on its report file.

150. **Task - Pixels claim nothing: image census frozen, H191 dissolves as an instrument artifact** (v0.8.73): Adjudicated H216/H217; dissolved H191; registered H227; filed DEF-6; H119 parallelized<br>
    **Result**: H216 CONFIRMED-NARROW (`image_census_h216.ipynb`, 2124-record frozen artifact) - extraction-tool contest decided by measurement: Docling primary (raster + 226 structured tables), page-render for 558 vector-drawn regions, pymupdf demoted; 81.5% of images information-bearing but only 26% of docs carry image text absent from the text layer - the round narrows to new-content capture. H217 REFUTED decisively (`pixel_forensics_h217.ipynb`): 0/32 absent golds pixel-only; all 16 H207 absents are text-layer-present extraction drops (sixth finding converging on H119), and the H191 vision residue is a benchmark CROSS-PAIRING artifact (values live in their own docs, probes cite the wrong doc) - H191 DISSOLVED, H227 registered (probe-pair hygiene audit). DEF-6 filed: instructor's import-order-sensitive mode registry breaks engine client construction (RegistryError on Mode.JSON); notebook workaround verified, engine fix routed to H198. H119 harness parallelized (concurrency 2 -> 8x5): ~10-13h collapses to ~2-4h, run progressing.

151. **Task - Probe sets certified clean; attachment gap measured at 41% and routed to fragmentation** (v0.8.73): Adjudicated H227/H226; registered H228; wave 2 completed, scale legs launched<br>
    **Result**: H227 CONFIRMED (`probe_hygiene_h227.ipynb`) - 0/320 genuinely cross-paired golds across all canonical sets; the H191 artifact was residue-set-local, H186 document-grounding held, all wide-set numbers stand un-inflated; the hardened presence matcher promotes as the standing gold checker. H226 CONFIRMED-PARTIAL (`ownership_census_h226.ipynb`) - 41.3% of 92 blind-frozen document-stated feature ownerships missing from the owning node; clause (b) inverted: single-product manuals miss MORE than catalogues (47.8% vs 24.0%) - the driver is entity FRAGMENTATION (sibling-fragment 45% + orphan 16% = H119 naming territory), with a genuine 32% never-extracted slice - H228 registered (feature-targeted gleaning, queued behind H119). Wave 2 completed on neo4j3 (471 docs, 7903 entities, cured) - H203/H210 scale executor launched; H157 ingest resumed after its agent's API-error death (kill-resume loss-free).

152. **Task - Scale legs adjudicated: truncation law holds as approximation emerges; conflict surface is corpus-class** (v0.8.73): Adjudicated H203 (inconclusive) and H210 (refuted) on the completed wave-2 graph<br>
    **Result**: The frozen wave-2 graph fingerprinted at 952 docs / 4649 embeddings (both waves; far under the hypothesized ~50k). H203 INCONCLUSIVE (`exact_k_scale_h203.ipynb`) - HNSW approximation now measurably EXISTS (+0.83 pt @ k=16 vs the exact 0.0 at 2798 embeddings) with generous-truncation >= exact-k at every k, re-confirming the harness law protective; the >= 3 pt magnitude is untestable at 4649 and stays open for a true 10x graph - run B's 18 pt gap looks increasingly like a graph-state artifact. H210 REFUTED per its registered branch (`conflict_scale_h210.ipynb`) - 4 genuine conflicts vs the 40 bar across 124 multi-doc products: technical prose states no datasheet attributes (only pressure-range fired), so the conflict-reconciliation surface (R17) is a property of spec-bearing corpora and its priority drops for prose classes, evidence base narrowing to H204's 87% silent-drop on datasheet corpora.

153. **Task - H119 refuted decisively: determinism is not a prompt property, the mechanism theory corrected** (v0.8.73): Recorded the campaign's most consequential refutation from the completed 150-checkpoint experiment<br>
    **Result**: H119 REFUTED on both clauses (`extraction_determinism_h119.ipynb`, 3 arms x 5 runs x 10 docs on the local model): the canonicalization prompt made variance WORSE (A 0.749 -> B 0.822 JD, -9.7% vs the >= 50% bar) while losing 31% of the stable entity reference (69.2% retention vs 95% bar). Arm C (config-identical to A) measured the noise floor at 0.105 JD between identical arms. Mechanism theory corrected: run-to-run churn is 76% CONTENT churn (different entities chosen per run), not surface-form naming - H107's 71% surface figure described duplicate pairs, a conflated phenomenon; in-prompt naming rules never targeted the real mechanism. Consequences recorded: the resolver/v2-stack carries the identity burden (H212 ingest already done cleanly under quiet Bedrock, measurement pending), the cure class moves to R22 - whose own gate previews (3-of-5 voting 35% at 100% retention) already beat the dead prompt fix. H229 attributes the concurrency share of the 0.749 baseline.

154. **Task - H212 inconclusive: the confound changed shape; re-run launched under reset quota** (v0.8.73): Recorded the first H212 attempt and immediately relaunched under valid conditions<br>
    **Result**: H212 INCONCLUSIVE (`clean_recall_h212.ipynb`) - the concurrency confound was replaced by a Bedrock daily-token-cap confound: 60 "Too many tokens per day" retries cost 30 chunks (v1 arm had 0), so both clauses fail invalidly (82 failed chunks vs 76 bar; recall 0.750 vs 0.875) and neither registered branch can fire. The graph came up 28% thinner (1585 vs 2201 entities), yet the identity clauses held under the damage (precision proxy 0.517 PASS; false merges 14, +27% off the replay band). Per-probe forensics: only 2 of 8 misses are extraction loss (token-cap fingerprint - gold in chunk, absent from graph); 6 are pre-existing retrieval-reachability misses. H158 DEGRADED stands, default flip not approved. Operational lesson recorded: quiet Bedrock = no concurrent consumer AND daily headroom. Quota verified reset by direct probe; re-run executor launched (wipe neo4j4, same arm, same harness).

155. **Task - R22 sweep: seven refutations invert the extraction doctrine from stabilization to aggregation** (v0.8.73): Recorded all seven CPU-tier verdicts; registered H240 union extraction<br>
    **Result**: Seven-for-seven refutations (`failure_mechanism_r22.ipynb`): H230 - churn has no class structure (product-form is the LARGEST singleton class, codes churn at 0.94x base); H231 - the contrarian "harmless variance" position dead by an order of magnitude: single-run gold-carrier coverage 55.4% mean (swing 54 pts) vs union-of-5 at 100% - variance is UNDERSAMPLING; H234 - surface rules do -1.5% (nothing); H235 - chunking exonerated (odds ratio 0.47, inverted); H236 - sibling merges close 52.9% of sibling misses (quarter of the total gap is resolver-side, rest genuinely unextracted); H237 - voting passes its variance clause (30.01%) but retains only 60% of gold carriers - stabilization discards exactly what the benchmark needs; H238 - dangling endpoints are froth (5.3% overlap with miss classes), the drop is correct. Doctrine inverted: every stabilization cure (prompt/rules/voting) is measured dead; H240 registered - union-of-K extraction with the v2 resolver absorbing duplicates, clause (a) free on existing checkpoints. Correction (executor final report): H236 supersedes to CONFIRMED - 13/17 sibling closes (76.5%), 34.2% of the attachment gap is resolver-side; the sweep is six refutations plus one confirmed contrarian.

156. **Task - H212 re-run inconclusive-again: Bedrock design retired, identity clauses corroborated a third time** (v0.8.73): Recorded the re-run; H241 queued behind the H229 serial measurements<br>
    **Result**: The re-run (`clean_recall_h212_rerun.ipynb`) landed dirtier than attempt 1: 100 token-cap retries, 50 chunks lost, 5 whole documents dropped, 110 warnings vs the 76 bar - the smoke-probe reset check proved liveness, not headroom (cap fired ~13 min in). Recall 0.792 is cap noise (6 of 9 missing golds are chunk-present/graph-absent casualties). The salvage is real: the identity clauses passed for the THIRD consecutive time under damage (precision proxy 0.500, false merges 10 in band) - the v2 stack's identity behavior is corroborated across contention and two cap-damage regimes. The Bedrock-dependent H212 design is retired; H241 (paired v1/v2 arms on the local model, same engine same day - the stronger design the quota ordeal forced) launches when H229 releases the idle vLLM. H158 DEGRADED stands; default flip awaits H241.

157. **Task - R23 registered: the undersampling mechanism round** (v0.8.73): User directed hypothesizing the mechanism behind extraction undersampling; nine hypotheses pre-registered, CPU tier launched<br>
    **Result**: R22 established WHAT (undersampling: single pass = 55.4% of achievable gold-carrier coverage) and priced the blind cure (union-of-2 = 89% at 2x cost); R23 asks WHY one pass emits half, because a mechanism-informed cure could beat blind union on cost. H242 emission-budget saturation (density/position signatures, free on checkpoints), H243 perception-vs-emission (complement prompting - does the model know what it left out), H244 decorrelation (cross-prompt union vs same-prompt union - free, the three H119 arms already contain the answer), H245 logprob confidence signature, H246 enumerate-then-extract (recall/precision stage split - strongest cheaper-than-union candidate), H247 prompt-slot bias (type-list position vs churn), H248 candidate priming (lexical pre-scan feeds cued recall), H249 CONTRARIAN (union only works because serving is nondeterministic - determinism would kill it unless diversity is manufactured), H250 the cost frontier (adjudicates the H198 shipping operator). Free tier (H242/H244/H247/H248-scanner) running on existing artifacts; LLM gates queue behind H229's serial measurements.

158. **Task - R23 CPU tier recorded: mechanism narrowed by elimination, scanner instrument promoted** (v0.8.73): Recorded H242/H244/H247 verdicts and the H248 scanner gate<br>
    **Result**: Free tier on frozen checkpoints (`undersampling_mechanism_r23.ipynb`, anchors reproduced exactly): H242 REFUTED - omission is not a length budget (coverage density-flat, gap -4.4 pts inverted; singleton positions only +0.066 deeper); H244 REFUTED - cross-prompt union 86.8% vs same-prompt 89.0%, run stochasticity is the ONLY diversity source, so blind union stays repeat-same-prompt and H249's determinism contrarian becomes the live wire; H247 VOID with a load-bearing side-finding - the H119 runs used an EMPTY ontology (discovery mode), so all R22/R23 churn numbers characterize the uncured regime, post-cure churn is a registered unknown checked by H240 clause (b); H248 gate PASSED - the zero-token lexical scanner sees 87.3% of union-of-5 gold carriers (misses are consumable-class names), the primed-pass LLM clause earns its run. Net: selection - not budget, format, or prompt - is where the missing half dies; H246 enumerate-then-extract is the primary cheaper-than-union candidate. Promotions entry added.

159. **Task - R24 remedies registered plus five-lens fan-out extension (H251-H265)** (v0.8.73): User directed deep remedy hypothesizing then a fan-out; fifteen remedy hypotheses registered, gates launched<br>
    **Result**: Core R24 (H251-H254): parallel-sampling union economics (vLLM n-parameter shares the prefill - union may cost ~1.3x not Kx), scanner-audited surgical re-extraction, severed-table/attachment-gap linkage (prices H145 semantic chunking with a number), graph-as-lexicon cross-document healing. Fan-out: five parallel ideation lenses (sampling statistics, decoding systems, IE literature, contrarian economics, architecture) proposed ~20 mechanisms; curated to eleven (H255-H265): adaptive-K allocation (Chao2/Good-Turing/scanner-residue controllers), completeness instrumentation (Chapman two-source estimator), logit-level candidate harvesting (near-tie losers ARE the missing entities), mention-emission (move dedup out of the model into the resolver), extract-then-mask peeling, GLiNER span proposer (kill line = the 87.3% regex floor), in-context union distillation, chunk-fallback render (query-time recovery, re-prices the whole program), recall SLA per regime, heterogeneous union member, ship-then-top-up async union. Rejected with reasons recorded: diverse beam, chain-of-verification, chunk-claim queue (deferred to H198 logging). Three gate executors running (checkpoint arithmetic, GLiNER on idle GPU, graph-touching gates).

160. **Task - R24b fanout gates recorded: estimators dead, known-but-dropped confirmed at 81%** (v0.8.73): Recorded six checkpoint-gate verdicts plus the H260 GLiNER gate<br>
    **Result**: The fan-out's free tier resolved fast (`fanout_gates_r24b.ipynb`, `gliner_gate_h260.ipynb`): H260 gate PASSED decisively - gliner_multi-v2.1 on the idle Ada card sees 95.2% of union-of-5 gold carriers in 7.6 seconds, a strict superset of the regex scanner (catches 5 of its 8 consumable-class misses); GLiNER supersedes the scanner as audit/priming lexicon. H258 gate CONFIRMED by 51 points: 81.4% of a run's locally-missed carriers were extracted by the SAME run in another document - the model knows what it drops, licensing mention-emission and corroborating graph-lexicon healing. Refuted: H255 adaptive-K (Chao2 wrecked by pipeline flakiness, greedy +3.2 pts under the 5-pt bar), H256 completeness estimators (Chapman +48% biased, GT rho 0.095), H259 salience shadowing (inverted, 0.77x), H263 class scoping (no leverage on name-shape classes, low confidence flagged). H261 demo split frozen. RECOVERY-STATE rewritten to the new board.

161. **Task - R24 graph gates recorded: surgical repair and lexicon healing pass, severance linkage refuted** (v0.8.73): Recorded H252/H253/H254 gate verdicts<br>
    **Result**: `remedies_free_gates_r24.ipynb` (anchors exact, neo4j2 read-only): H252 gate CONFIRMED - 68.6% of per-run missed carriers are scanner-visible and span-locatable at 5% pooled token cost (bar 60% / 40%), so the surgical micro-pass earns its LLM clause with GLiNER upgrading the audit lexicon. H254 ceiling CONFIRMED - 62.9% of missed carriers already exist in the graph from other documents and re-appear in the missed doc's text; healing covers the recurring slice for free, paid cures need only the 31.5% first-appearance residue; convergent with H258's 81.4% known-but-dropped. H253 REFUTED on robustness - the nominal 3/12=25.0% graze collapses to 0/12 at threshold 75 with one semantic false positive; severed table rows and never-extracted ownerships are separate loss classes, H145 semantic chunking stays deprioritized with a measured answer. Free-tier gates now fully drained; LLM tier queued behind H229 (14/15).

162. **Task - Peer climb recorded: graphify and TrustGraph do not turn the tables; R25 harvest registered** (v0.8.73): User directed cloning and climbing both peer builders for mechanisms; five hypotheses harvested (H266-H270)<br>
    **Result**: Two Opus climbers examined graphify (SKILL pipeline, 1156 lines) and TrustGraph (HEAD 0374098, four extraction processors). Verdict: our diagnoses transfer to BOTH wholesale - single-pass extraction with no variance awareness (they cannot see the 55% ceiling because they never run K passes), lexical-slug identity that extraction variance defeats permanently, no completeness auditing (graphify's "honest audit trail" is edge provenance - a precision instrument under a recall name), and TrustGraph's RDF granularity AMPLIFIES attachment fragmentation (ownership scatters across duplicate surface-form URIs; OntoRAG validation can even drop valid ownership triples). Neither has ontology curing, drift detection, or bitemporal versioning. Harvest registered as R25: query-answer write-back (H266), deterministic-ID resolver fast path (H267), soft similarity links as abstention primitive (H268), ontology-constrained validation as precision lever (H269), schema-as-graph substrate (H270); truncation-resilient JSONL parsing and the discrete confidence rubric route to H198 as engineering items. All five gates are free on frozen artifacts.

163. **Task - H266 superseded by design, R26 usage-coupling round registered (H271-H280)** (v0.8.73): User endorsed the peer-harvest direction but killed naive answer write-back; ten researched mechanisms registered<br>
    **Result**: The user's constraint - answers are tied to the graph AS IT WAS at answer time, naive reinjection creates untracked derived content - became R26's organizing law: write back SIGNALS (demand, aliases, gaps) which age gracefully; treat CONTENT (answers, renders) as derived artifacts with dependency tracking, never source-of-truth nodes. Registered: H271 demand ledger (queries allocate repair budget), H272 abstention-triggered repair, H273 in-graph derived layer with invalidation (frame-problem clause as honest killer), H274 CONTRARIAN out-of-graph fingerprint-keyed cache (head-to-head with H273, round ships exactly one or neither), H275 recurrence gate, H276 materialized render views, H277 query-alias harvesting (usage teaches the resolver), H278 demand decay, H279 CONTRARIAN staleness law measured (runs first, parameterizes the round), H280 composition frontier. H267 fast path stands endorsed. Executor launched for the R25/R26 free tier (H267 census, H268 soft-link sim, H269 validation replay, H279 staleness curve).

164. **Task - R25/R26 first free gates recorded: soft links pass big, fast path and validation die honestly** (v0.8.73): Recorded H267/H268/H269/H279 verdicts; launched the remaining R26 free tier and the R21 VLM tier in parallel<br>
    **Result**: `peer_harvest_gates_r25.ipynb` (live graph read-only): H268 soft similarity links gate CONFIRMED at 2.2x the bar - 64.7% (11/17) of sibling-fragment attachment misses close by hopping a posterior-weighted SIMILAR_TO edge to the counterpart owning the fragment, zero false-merge risk, H212 attribution preserved; ships to H198. H267 deterministic-ID fast path REFUTED - only 4.5% of h157 merges are exact-normalized (pooled 1.3%); extraction variance owns the merge population, peers' free-merge trick has nothing to reserve here. H269 ontology validation PARTIAL, lever rejected - 48.8% of relationships flag as domain/range violations but only 46.7% are genuine junk and 29.2% touch gold carriers; validation would repeat H237's mistake on the relationship axis. H279 staleness law PARTIAL - evidence-stale 9.5% (window-limited, fails 30% clause) but concentration 2.05x confirms mutation follows demand; signals-over-content doctrine stands. Two executors launched: R26 remainder (H271/H273-H278) and R21 H218 VLM contest + H224 decorative filter on the free GPU.

165. **Task - H229 package recorded: determinism family closed, variance is model-inherent** (v0.8.73): Recorded H229 + three R22 gates + H249 adjudication; vLLM freed for the LLM tier<br>
    **Result**: `serving_determinism_h229.ipynb` closed four questions in one report: H229 REFUTED - serial/concurrent JD ratio 0.924, temp-0 variance is model-inherent (concurrency exonerated, H119's magnitude stands, quiet figure ~0.60); H232 CLOSED at gate - seed+serial leaves JD 0.55, zero identical reproductions, deterministic mode does not exist on this stack; H233 CLOSED at gate - guided-JSON made variance WORSE (-21.3%), choice churn not format; H239 gate GO in the surprise direction - Qwen2.5-7B grammar-constrained shows JD 0.42, half the 120B's variance (recall gate mandatory: 17-20 entities vs 44; 7B cannot run the production path unmodified). H249 contrarian REFUTED per its registered branch - no determinism exists to kill union; H232's gate becomes the standing per-engine-upgrade canary. Promotions entry added. Aggregation doctrine now on fully closed ground.

166. **Task - H157 corpus transfer recorded: lifecycle transfers, calibration breaks at 5.18x** (v0.8.73): Recorded the campaign's first cross-corpus generalization verdict<br>
    **Result**: CONFIRMED exactly as registered (`corpus_transfer_h157.ipynb`): on the 30-paper transfer corpus (16547 entities, 12203 rels, 51 cured types) the lifecycle machinery is corpus-robust - Good-Turing gate cured at doc 13, held STABLE, zero spurious recures - while identity calibration breaks: transfer ECE 0.2606 vs the honest H129 held-out reference 0.0503 = 5.18x past the 2x threshold, systematically under-confident. Blind labeling (60 stratified pairs, H101 protocol): merge precision 0.95, block correctness 1.00, defers well-placed at 0.55 ambiguity; the single false merge is author-name morphology - the predicted per-domain deterministic false-merge surface. Decisive nuance: decisions transfer while probabilities miscalibrate, which argues for per-corpus SELF-calibration (H142 lineage) over threshold re-tuning - routed to H198 as a shipping requirement. Promotions entry added.

167. **Task - R26 resolved to two shippers; R27 agentic-escalation round registered, web arm user-gated** (v0.8.73): Recorded seven R26 verdicts, pre-registered the agentic identity-resolution fan-out (H281-H290), deferred H286 web escalation per user directive<br>
    **Result**: `usage_coupling_gates_r26.ipynb` (union-of-5 reproduced 63/101, harness valid): H271 demand allocator CONFIRMED 1.7x-16.4x uniform at every budget (mechanism honest - demand CONCENTRATION, doc-level demand-gap correlation null); H274 external fingerprint cache CONFIRMED and PREFERRED - 100% correct invalidation at zero graph coupling, dominating H273's in-graph derived layer (confirmed, not shipped); H275 recurrence gate REFUTED (retains 44% of gain), H276 render views REFUTED (494% storage, hub blast 185), H277 alias harvest REFUTED (probe vocabulary matches graph names), H278 decay PARKED. R27 registered with the cost warning as design law: escalation ladder deterministic -> calibrated -> single-shot -> tool-agent -> web, effort budget (H284), tools-vs-no-tools contrarian (H285), escalation gate (H287), H288 oracle-ceiling kill switch running FIRST free. User direction: exhaust non-web paths to SOTA first - H286 web arm user-gated, does not run this wave. Promotions entry added for H271/H274.

168. **Task - R27 kill switch fired: recall-agentics dead, round re-scoped to precision** (v0.8.73): Recorded H288/H281 verdicts and launched the precision-scoped LLM arms<br>
    **Result**: `agentic_escalation_gates_r27.ipynb` (neo4j2 read-only, 908 defer pairs from 9020 events): H288 REFUTED as the contrarian predicted - on the same defer band, soft links lift recall 0.202 -> 0.347 (+14.6 pts, H268 re-confirmed at band scale) and merging EVERYTHING on top adds +0.0 pts (sensitivity band [-1.88, 0.0], entire band below the 2-pt gate) - the render already reaches counterpart neighborhoods through the soft hop. The precision residue survives: 57 type-conflicting SAME_AS merges + 64 model-code-shared false merges only a resolution decision can remove. H281 census CONFIRMED: 85.6% of the defer band decidable by deterministic instruments or one call, source-span-starved 13.3%, world-knowledge 1.1% - the near-empty web class independently vindicates the user's web gate. Round re-scoped: H282-H285/H289 re-aimed at false-merge removal at zero true-merge loss (executor launched, labels frozen blind before any LLM call); H290 prices against the do-nothing baseline.

169. **Task - R21 image tier recorded: tables transcribe, photos hallucinate brands, filter near-perfect** (v0.8.73): Recorded H218/H224 verdicts and registered the H291 brand guard<br>
    **Result**: `image_vlm_gates_r21.ipynb` (36 images, 191 gold facts frozen blind from pixels pre-VLM): H218 PARTIAL PASS with the registered prediction INVERTED - Qwen2.5-VL-7B does NOT lose rendered-table cells (0.94/1.00, cells transcribed) or diagram facts (0.98/0.92); the failure moved to product photos as a PRECISION failure (0.83, three fabricated manufacturer brands on unbranded/competitor devices) - the worst possible image error for a graph. SmolVLM-256M fails recall everywhere (0.32-0.62, precision-by-abstention). Diagrams + tables GO, photos BLOCKED pending H291 (guard candidates: prompt-constrained brand assertion, OCR-attested strip, abstain-on-brand with H220 anchoring carrying identity - predicted winner, same abstention doctrine as the engine). H224 CONFIRMED decisively: recurrence >= 24 placements OR area <= 98 px discards 96.85% of the raw image stream at 0.00% false-discard; color entropy dropped (does not separate - logos are colorful, photos smooth). VLM pays for 3% of images; H225 economics now winnable.

170. **Task - R27 precision arms recorded: demote-don't-delete ships, agent loop priced out** (v0.8.73): Recorded H282-H285/H287/H289/H290 and registered H292 type-label reconciliation<br>
    **Result**: `agentic_precision_arms_r27.ipynb` (127 SAME_AS edges blind-labeled pre-LLM: 26 true / 101 false): the single-shot judge detects 98.0% of false merges at 513 tok/pair; its only failure is over-splitting 8 true cross-type aliases, ALL caused by conflicting extractor type labels (type-conflict strata preservation 0.29/0.00) - upstream noise, H292 registered (type-blind judging predicted winner). H283 tool-agent REFUTED on all three clauses: -12.9 pts detection at 3.93x tokens, trades detection for merge-leniency. H284 effort law CONFIRMED: saturates K=2-4, DIPS at K=8 with late rounds re-arguing not re-fetching. H285 theater clause confirmed (+1.0 pt without evidence); fetch-then-judge corollary refuted but moot. H289 UNTESTABLE (Strands not installed). H290 RESOLVED as demote-don't-delete: judge + demotion to soft links = zero-true-loss by construction via H288's merge/soft-link equivalence; whole docket ~65k tokens, H287 moot-by-scale. The escalation ladder collapsed to one call plus one edge rewrite. Promotions entry added.

171. **Task - H292 recorded confirmed-not-shipped; H198 wiring plan drafted under the new goal** (v0.8.73): Recorded the type-reconciliation verdict, adopted the release-candidate goal, drafted the wiring-sweep plan, launched the promotion audit<br>
    **Result**: `type_reconciliation_h292.ipynb` (byte-exact H282 contexts, only type treatment varied): type-blind judging CONFIRMED at both bars - TC-strata preservation 0.750 exactly, detection 95.05% by one pair, 480 tok/pair, cheapest-candidate prediction confirmed; multi-label (0.625) and cured-ontology arbitration (0.250 at 980 tok/pair) FAIL. NOT SHIPPED on composition arithmetic: demote-don't-delete makes H282's over-splits costless while type-blind's 3 extra missed false merges would stand as SAME_AS poison - the court ships as H282 + demotion, and type labels are recorded as genuine identity evidence on this corpus. New active goal adopted (compose, rebuild twice, capstone, release candidate); `docs/h198-wiring-plan.md` drafted with four decision slots (SLOT-3 resolved same hour); H198 clause (a) promotion-vs-code audit executor launched.

172. **Task - H198 clause (a) recorded: 66 promotions inventoried, zero levers live in defaults** (v0.8.73): Recorded the wiring audit, closed the ledger gap it found, folded the inventory into the wiring plan<br>
    **Result**: The auditor classified all 66 promotion entries with zero unaccounted (15 lever / 22 doctrine / 18 instrument / 9 negative / 2 superseded) and delivered the headline the sweep exists to fix: NOT ONE promoted lever runs in the engine default path - top_k still 8 and identity_stack still v1 (implemented, wrong default), three levers notebook-only, nine not-implemented (R19 trio, glyph normalization, header carryover, parser union, prop splitter, soft links, demotion court, demand allocator, external cache) plus the self-calibration requirement. DEF-6 root-caused to `engines/local_gpu.py:28` with a one-import minimal fix. Audit flags actioned: H260 GLiNER got its own promotions line (was riding the R23 tier entry); H37 optional-removal note carried into the R19 composition step. Binding constraint recorded in the plan: no engine edits until the H241 chain and R23/R24 batch release the working tree. Inventory: `reports/h198-wiring-inventory-20260708T102631Z.json`.

173. **Task - H198 tranche-1 wiring complete in isolated worktree; extraction frontier turns two-horse** (v0.8.73): Six slot-independent levers wired behind config keys, merge parked pending tree release; R23/R24 interim results reshape SLOT-2<br>
    **Result**: Worktree executor (`agent-aea0ebaec7aac3122`, base 0735aac) delivered all six tranche-1 levers: DEF-6 closed with the one-import fix in `engines/local_gpu.py` plus a fresh-interpreter regression test; top_k default 8 -> 16; H190 glyph normalization split correctly - char-level cleanup on parsed text, full glyph_norm only as resolver comparison key so entity IDs stay backward-compatible; H153 header carryover; H146-151 pypdf parser union; H173 proposition splitter (300-token gate). 325 tests pass (+16 new), zero new lint failures; merge watch-items recorded in `docs/h198-wiring-plan.md` (H195a over-fetch factor deliberately not invented, repo-wide ruff drift is separate housekeeping, chunk ids change by design). Merge blocked until H241 chain + R23/R24 batch release the main tree. Batch interim (verdicts recorded at round close): H246 enumerate-then-extract 0.971 coverage at 1.05x cost; H258 mention-emission 1.000 carrier coverage at 1.53x mention inflation single-pass - internal-dedup mechanism confirmed, SLOT-2 now a two-horse frontier; H248 GLiNER-primed 0.943; H243 complement zero-new.

174. **Task - R23/R24 LLM tier closed: SLOT-2 decided for enumerate-then-extract; vLLM outage survived** (v0.8.73): Recorded eight verdicts + the H250 frontier resolution; restored the LLM server after an external kill; H241 v2 arm re-running<br>
    **Result**: Round report `undersampling-llm-gates-r23r24-20260708T132938Z.json` closed the extraction-remedy question: H246 enumerate-then-extract CONFIRMED (0.971 of union-of-5 at 1.05x cost - SLOT-2 default); H258 mention-emission coverage clause CONFIRMED spectacularly (1.000 at 0.759x, 1.53x mention inflation) with the resolver-precision clause staged to H240(b); REFUTED: H243 complement (zero new), H245 logprob signature (0.0066 vs 0.5 nats), H251 n-sampling (3.05x on a build where vLLM n>1 500s - harmony bug), H257 logit harvest (34.5% vs 85%), H260 adjudication clause (0.873 vs 0.89), H261 union demos (NEGATIVE -15.3 pts); H248 PARTIAL final (+8.6 vs 20-pt bar). Doctrine: exhaustivity moves only by restructuring the task, never by instructing/cueing/demonstrating. Operational: vLLM killed by external SIGTERM 12:20Z - v1 arm valid (recall 0.7708, precision 0.083), v2 first attempt quarantined; server restored after six documented JIT traps (`scripts/vllm-serve.sh`, my-gpu issues file); v2 arm re-running detached. Promotions entry added; global /brace command created.

175. **Task - H241 verdict PARTIAL: SLOT-1 resolves NO-GO, v1 stays default with the court as precision repair** (v0.8.73): Executed the registered A/B notebook on the completed paired arms, recorded the verdict across ledger/plan/promotions, opened the gap ledger<br>
    **Result**: The v2 rerun finished clean (28 docs, zero timeouts, uncontended server) and `identity_ab_h241.ipynb` produced `identity-ab-h241-20260708T160009Z.json`: recall tie 0.7708 = 0.7708 PASS, precision proxy 0.611 vs 0.50 bar PASS (v1 same-run 0.083 - 11 of 12 sampled merges false), false-merge factor 1.57x vs the registered 2x bar FAIL (11 -> 7 raw count) - 2 of 3 clauses, flip NOT approved, H158 not lifted. Recorded honestly what the raw-count clause misses: v2 merged 1.94x volume at 11x bench-pair recall, per-merge false rate 3.1x lower, 355 NLI vetoes active. SLOT-1 NO-GO per the registered branch: v1 default + demote-don't-delete court (98.0% detection, docket = v1's false-merge surface); v2 stays behind `resolution.identity_stack`. Created `docs/gap-ledger.md` (GAP-1 v2 stack with reopen condition, GAP-2 images, GAP-3 web, GAP-4 priced-out residue). Confounds recorded: v1's 6 contention timeouts, the outage-invalidated first v2 attempt, H229 extraction variance, Evox 0-char parse.

176. **Task - Tranche 1 merged into main; SLOT-2 recipe wiring dispatched** (v0.8.73): Merged the six-lever worktree branch after the H241 arms released the tree; launched the tranche-2 executor<br>
    **Result**: The wiring constraint lifted with the H241 verdict, so the tranche-1 branch (six levers behind config keys: DEF-6 import fix, top_k 16, H190 glyph normalization, H153 header carryover, H146-151 parser union, H173 proposition splitter) merged clean into main - full suite re-verified post-merge at 325 passed / 15 skipped, worktree and branch removed. The engine default path now carries its first promoted levers since the campaign began (the H198 audit had found zero live). Tranche 2 dispatched to an Opus executor with the measured H246/H258 prompt texts verbatim from the round notebook: `extraction.recipe: single|enumerate|mention` config key, robust stage-1 name parsing, 150-cap candidate priming block, plus `config-h240b-enum.yml`/`config-h240b-mention.yml` arm configs for the H240(b) scratch validation. Default stays `single` until H240(b)'s verdict - every flip evidence-backed.

177. **Task - H240(b) SLOT-4 verdict; merge train composes tranches into main** (v0.8.73): Closed the last decision slot from the overnight two-arm chain, then merged the wiring backlog<br>
    **Result**: The 12.5h H240(b) scratch A/B (both recipes, v2 resolver, matched 27-doc corpus on neo4j4) resolved SLOT-4 - composed ingest path survives E2E (resolver held false merges at 13/arm, no drowning), H246 enumerate CONFIRMED default, H258 mention REFUTED as displacement: it lost 6.25 pt recall (0.7292 vs 0.6667) and 7.2 pt precision (0.5517 vs 0.48) under composition, the offline 1.000-dominance not surviving the pipeline. Verdict recorded across `h198-wiring-plan.md`, `sota-promotions.md`, and `defects.md` (DEF-8: extraction underfeeds vLLM at `concurrency` 4, dropped ~16 catalog chunks at `retries=1`). Then ran the merge train - defect/format-sweep, 3b, 3a branches into main, resolving a DEF-7/DEF-8 numbering collision plus an E741 lint fix, `ruff` format + full suite green at each step (380 passed / 15 skipped). Recipe-default flip to enumerate deferred (cascades into 13 gleaning/split tests that pin the single-path default); DEF-8 concurrency/retry tuning deferred to the Phase-3 rebuilds.

178. **Task - Enumerate shipped as default + DEF-8 retry fix; Phase-3 rebuild launched** (v0.8.73): Landed the two flips entry 177 deferred, committed the composed engine, launched the twin scratch rebuild<br>
    **Result**: Flipped `ExtractionSettings.recipe` default `single -> enumerate` (H246/SLOT-4) and pinned the 13 single-path gleaning/split tests to `recipe="single"` via a shared `SINGLE` constant - their mock engines implement `complete`, not the enumerate path's `complete_text`. Root-caused DEF-8: the ~16 catalog chunk drops were stage-1 `complete_text` calling `litellm.completion` with no retry arg (defaults to one attempt), not the instructor path; added `num_retries=self.cfg.max_retries`, decoupling correctness from the concurrency speed lever. 380 passed / 15 skipped, committed `f3b8d38` and pushed. Launched the Phase-3 twin rebuild on neo4j4 (`config-phase3.yml`, concurrency 16); caught and fixed a two-writer collision (two chains wiping one graph) - killed both, relaunched one clean writer. Phase-4 peer head-to-head deferred to the post-RC campaign per user decision.

179. **Task [Extended] - R28 maintenance/self-healing round pre-registered under the stateless kernel** (v0.8.73): Registered a 40-arm round (R28-H293-H339, seven persona groups A-G) in `docs/experiments/kgf-redesign-experiments.md` and launched its free-replay execution<br>
    **Result**: The load-bearing constraint is a STATELESS session - consecutive KGF sessions may trigger with no local state, so the shared Neo4j graph is the only cross-session state - which makes in-graph storage the substrate and kills the first-pass "external store wins" C arms (H301-H304 superseded; marginal E H309/H310/H312 folded into H320/H321). A dynamic multi-agent workflow (Opus executors, 42 candidates -> 21 survivors) generated revised group C (in-graph metric+state), E (repair-strategy x scenario), F (provenance-in-graph) and the new group G (statelessness solvability); user sign-off added H336-H339 (in-graph answer-cache reopening R26's H273/H274 head-to-head, calibration-artifact reconstruction, demand-ledger round-trip, wave-seal integrity). Three head-to-heads kept adversarial: snapshot-vs-fold (H315/H332 vs H331, with H332's permuted-order test the decisive discriminator), lease-vs-CAS (H324 vs H334), provenance-minimality (H328 vs H327/H329). Free-replay execution launched as four cluster notebooks (drift-recon, drift-trigger, recall, identity - CPU, over the 10 static event logs + frozen reports); GPU and Neo4j-write arms queue behind the Phase-3 rebuild per the user's free-replay-first order. Phase-3 itself was retuned to `concurrency` 8 / `timeout` 600 after correcting a warning-class misread - the `extraction.warning` event conflates timeouts with benign `dangling relationship` warnings, so the apparent drop count was inflated (true timeouts: 46 at c16, 0 at c8).

180. **Task [Extended] - R28 free-replay execution: 23 arms reconstructed, two RC defects ledgered** (v0.8.73): Four executor notebooks replayed the free R28 arms over the 10 static event logs + frozen reports; verdicts recorded, DEF-9/DEF-10 opened<br>
    **Result**: 14 CONFIRMED, 3 REFUTED, 5 PARTIAL, 1 MEASURED across `r28_freeplay_{drift_recon,drift_trigger,recall,identity}.ipynb` (reports `reports/r28-freeplay-*.json`), all written into the R28 free-replay results subsection of `docs/experiments/kgf-redesign-experiments.md`. Two findings cut across their arms and bear on the RC. DEF-9: the drift rebuild gate is structurally unreachable - `drift.py` needs `jsd>0.15 AND 3-consecutive remap>0.3`, but the two windowed signals are anti-phase, so even a continuous stateful oracle fires 0 rebuild at js 0.5079 (3.4x threshold); the 52-warnings/0-actions pathology is thus partly a detector-DESIGN bug, and R28-H317's CUSUM (fires at the 0.5079 doc AND on a sub-threshold ramp the boolean cannot see) is the fix. DEF-10: the default resolver runs uncalibrated (`calibration_path=None` skips the calibrate branch at `resolver.py:242`), so merges fire at raw 0.6 where `isotonic(0.6)=0.085` (~7x overconfident, only 6.3% survive) - the strongest single argument for wiring per-corpus self-calibration before the RC (H337). The snapshot-vs-fold head-to-head is settled: the in-graph MetricSnapshot ledger is necessary - H331's pure fold is REFUTED (lossy under 4945 merges, and the verdict is order-dependent while final state is order-free), with H332's permuted-order test decisive (0->15 recure under a legal breach-clustering permutation). CONFIRMED levers queued for wiring: H317, H299 (rel-type-entropy monitor), H300 (calibration-drift monitor), H311 (targeted micro-pass 17x), H336 (in-graph answer cache), H337. GPU / Neo4j-write arms queue behind the Phase-3 rebuild.

181. **Task [Extended] - DEF-10 reconciled; the SLOT-1 v2 reopen (R29) launched under the GAP-1 protocol** (v0.8.73): Investigating DEF-10 revealed the fix was a registered-verdict change, not a wiring task; caught a premature flip, reverted it, and opened the disciplined reopen<br>
    **Result**: DEF-10 (default resolver uncalibrated) turned out NOT to be the per-corpus isotonic refit its note proposed - `fit_calibration_from_events` (R15-H157) needs a ground-truth pairs file, so self-calibration is unimplementable under unsupervised ingest (H198 classed it NOT-IMPLEMENTED). The real candidate is the calibrated v2 identity stack. I prematurely flipped the default `identity_stack` v1 -> v2 citing the H241 A/B (raw SAME_AS precision 0.083 -> 0.611, recall tie 0.7708), then caught that this re-cites the exact numbers a pre-registered gate already declined: H241 was a registered SLOT-1 NO-GO (v2 missed the >=2x false-merge-COUNT clause, delivering 1.57x, confounded by 1.94x merge volume) and GAP-1 forbids renegotiating that bar post-hoc. Crucially the shipped default is v1 + demotion court (H290, 98% false-merge detection), so the raw 8.3% is PRE-court on the within-batch surface the court never touches. Reverted all four premature edits (`settings.py`, `config-phase3.yml`, `tests/test_resolution.py`, `defects.md` DEF-10 -> open), aborted the v2 rebuild. With the user's direction, launched R29 - the disciplined reopen: a NEW pre-registered decider (R29-H340) replacing the confounded count clause with a rate-normalized false-merge clause + 2-run variance, A/Bing v1+court vs v2+court across 4 ingests (`scripts/r29_chain.sh`, `config-r29-{v1,v2}.yml`, `scripts/r29_measure.py`), promoting v2 IFF all four clauses pass; the winning arm's two matching runs double as the Phase-3 reproducibility evidence. Added registered-default guards (`tests/test_settings.py::test_registered_resolution_defaults` pins identity_stack / demotion_court / soft_links / thresholds - the guard that catches a silent flip). Measured a vLLM starvation signal - at `concurrency` 8 the server runs 1-2 reqs, KV cache 3-5% (single-streaming); the c16 speedup is deferred per the no-mid-A/B rule. 382 tests green.

182. **Task [Extended] - R30 GPU-throughput round pre-registered; document_concurrency knob designed** (v0.8.73): Measured the shipped engine feeding local gpt-oss-120b at ~5% of the card and registered a progressive-loading round to price the fix<br>
    **Result**: A 25 s vLLM window showed ~397 tok/s aggregate (158 gen + 238 prefill), 3-8 requests in flight with 0 queued, GPU1 at 190 W under a nominal 98% util - against the my-gpu r6 ceiling of 8,232 tok/s at b64. Root cause: `extractor.py:240` fans chunks within one document through a `ThreadPoolExecutor(max_workers=concurrency)` while `pipeline.py:322` walks documents strictly serially, so the card bursts during a single doc's extraction and idles to 0% between documents (curing/resolution/graph-write are non-LLM). Registered R30 (H341-H348) in `docs/experiments/kgf-redesign-experiments.md`, throughput-only signal after pruning the KV/power machinery as unnecessary GPU-debugging - and unavailable off the local card anyway, so the knob stays engine-agnostic (`frontier`/`claude-cli`/`local-gpu`). H341 fixed concurrency ramp finds the knee; H342 maps the concurrency x timeout 0-drop frontier for a safe `(c,t)` default; H343 tests cross-doc `document_concurrency` as the larger lever (the between-doc idle); H344 de-scoped to an optional local diagnostic; H345 guards quality invariance; H346 is a gradient auto-controller on client-observable signals (goodput/latency/429) shipping as `extraction.concurrency=auto`; H347 finds the knee as the spline max-curvature point (vs Kneedle/chord/segmented baselines, bootstrapped); H348 asks whether a constant-velocity Kalman beats EMA/Holt/median for the controller gradient (Simplicity-First, ship simplest within 5%). H347/H348 are pure replay over H341's trace - no extra GPU run. Designed the `extraction.document_concurrency` knob (default 1 = current serial, R29-safe) for a post-R29 build. All gated on R29 (shared vLLM); tasks #56-#58 created.

183. **Task [Extended] - Fixes-first directive: DEF-11/12/13 registered, R31 pre-registered, H351 executed same-day** (v0.8.73): User directive - fixes prioritized over all other work, hypotheses before fixes, standing GPU max-out rule; three defects registered and the first fix verdict landed<br>
    **Result**: Registered in `docs/defects.md`: DEF-11 (run-to-run extraction variance - two config-identical v1 ingests swung recall 0.7917 vs 0.7292, sinking R29's C4 on the CONTROL arm; cause is single-pass emission undersampling per R22-H231 55.4%-vs-union-100% coverage, with prompt canonicalization H119 and seeded decoding H232 already REFUTED as cures), DEF-12 (GPU fed at 3-5% of the 8,232 tok/s ceiling - serial doc loop + phase-misaligned per-doc pool; absorbs DEF-8's speed half, DEF-8 closed; fix is the pre-registered R30), DEF-13 (coverage residue - ~6/8 failed probes are never-extracted entities per H107; rides DEF-11's cure). Pre-registered R31 in the experiments log - the three defects compose into one economy: union-of-K extraction (H349) buys coverage+stability with compute the idle card (R30) supplies free; H350 pass-count identity prior, H351 CI gate form, H352 bounded enumeration budget. Executed H351 immediately (`scripts/r31_gate_power.py`, pure replay, no GPU): CONFIRMED - the 2-run |dR|<=0.03 band false-fails a TRUE-NULL control at 0.464-0.598 (pooled run-mean sigma 0.0290), so R29's C4 was ~half coin flip by construction (NO-GO stands per GAP-1); replacement one-sided Welch gate holds type-I at 0.05, and detecting +0.03 recall needs ~12 runs/arm - so H349's bar was amended PRE-RUN: the near-deterministic never-extracted census is the primary clause, small-delta recall demoted to supporting evidence at N=3/arm. Goal set via /goal: every defect DEF-8..13 fixed with measured verdicts. R29 chain still live (v2-run2 in embeddings; verdict locked NO-GO regardless).

184. **Task - R32 chunking round registered from survey; throughput-cache design captured** (v0.8.73): User-directed creative fanout on chunking strategy - survey first, then 8 hypotheses (H354-H361)<br>
    **Result**: Research agent (Opus) surveyed chunking literature; decisive facts: nearly all published evidence is retrieval-side - the only extraction-side numbers are chunk-size ablations (GraphRAG ~2x entity refs at 600 vs 2400 tokens; LightRAG monotone 2000->500), putting KGF's 2000-token window in the published low-yield regime - and no splitter creates co-occurrence (DocRED: 40.7% multi-sentence facts), so cross-section relations got a document-level relation pass (H356) instead of a chunking fix. Round opens with the H354 offline failure-class census gate (a class under 10% of residual mass kills its hypothesis pre-build); executes post-R31 (union confound), census exempt. H36 closed SUPERSEDED by H355+H361; semantic/late/agentic chunking excluded as evidenced dead ends. Archived 4 papers + digests in `references/papers/`. Also captured the throughput-calibration cache design (task #64: per-setup entries, engine-class signal split, raw-429 counting per user, pre-shipped calibrations) and the new acc-crit "Throughput Calibration" section (6 criteria, TOC verified).

185. **Task - Throughput-calibration cache registered as R30-H362 + acc-crit spec** (v0.8.73): user command "register" - first vehicle wrong, corrected same day<br>
    **Result**: Initially created `docs/enhancements.md` with the design as E01 (per-setup entry list, trust-but-verify warm start, engine-class signal split with raw-429 counting, pre-shipped calibrations); user redirected the vehicle - enhancements doc deleted, the cache registered as hypothesis R30-H362 in the canonical experiments doc (falsifiable warm-start/band clauses: >= 0.9x knee goodput in window 1, false-trigger <= 1/10, perturbation caught; naive baseline = the ~17 min cold ramp at every start) with the full behavior spec as acc-crit "Throughput Calibration" criteria (now 13). Task #64 tracks seeding from the R30 report + implementation. Same message signed off the R32 pre-registration.

186. **Task - R30 ramp verdicts: starvation confirmed, instrument self-invalidated; H353 gate launched at max feed** (v0.8.73): Ramp completed c=1..64; H341/H342 verdicts recorded, H363 clean re-ramp registered, gold-carrier gate launched detached<br>
    **Result**: The ramp (`reports/r30-ramp-20260710T094808Z.json`) proved H341's starvation half - gen tok/s climbs monotonically 156.6 (c1) -> 846.2 (c64), zero server queueing, shipped c8 at ~1/3 the c64 rate - but self-invalidated its goodput column (90s window < one chunk's 357s+ lifetime -> chunks/min = c/1.5 fake linearity) and contaminated tok/s above c~24: p50 wall latency exceeds the 600s client timeout from c~32 (maxima quantized at 600s multiples), so "errors=0" was silent-retry-masked - demonstrating H362's raw-attempt-counter claim on our own engine. Verdicts: H341 PARTIAL, H342 CONFIRMED directionally, H347 blocked. Registered R30-H363: clean re-ramp with per-attempt counter, completion-sized windows, t3600, ladder to c96; seeds the first H362 cache entry. Launched the R31-H353 gate detached (`logs/r31-h353-gate.log`): 33 carrier chunks x 6 passes at c=64 on `config-r31-gate.yml` (r29-v1 + timeout 3600 only), 26/27 golds carried - doubles as the live (c64,t3600) 0-drop trial.

187. **Task - Benchmark corpora acquired for the peer campaign** (v0.8.73): User opened the web gate - multi-hop QA benchmarks pulled into `data/external/multihop-qa-benchmarks/`, README updated<br>
    **Result**: Downloaded the exact evaluation artifacts the HippoRAG papers report on (`OSU-NLP-Group/HippoRAG`, `reproduce/dataset/`): 1000-question subsets plus retrieval corpora for HotpotQA (9,811 docs), MuSiQue (11,656) and 2WikiMultiHopQA (6,119), all JSON-verified - published GraphRAG/LightRAG/HippoRAG-2 numbers become directly comparable without re-running competitors. Full HotpotQA dev distractor (7,405 questions, canonical CMU host) added as the tuning-slice source. Split discipline recorded in `data/external/README.md`: the 1000-question subsets are SEALED eval sets, never touched by tuning or hypothesis rounds; tuning draws only on full-dev questions outside them - test-set leakage would invalidate the peer comparison. Current fix chain stays on the CPAP corpus (all baselines, sigma and the goal's terminal recall gate are CPAP-pinned); benchmark-dev rounds begin post-R31. Task #59 updated.

188. **Task [Short] - Benchmark recipe document** (v0.8.73): Wrote `docs/benchmark-recipe-multihop-qa.md`<br>
    **Result**: Execution-ready recipe for the peer benchmark (task #59): fixed regime (Llama-3.3-70B backbone per user decision, sealed-set discipline), data map, serving layout, 4-item harness, 7 execution phases, 17-item checklist, open decisions (embedder parity, typed-vs-schema-free). TOC verified.

189. **Task [Short] - Full HotpotQA dev acquired via mirror** (v0.8.73): CMU host unreachable (curl exit 7) - switched source<br>
    **Result**: Pulled the HF `hotpotqa/hotpot_qa` distractor validation parquet, converted to canonical JSON shape (7,405 questions). All 1000 sealed eval ids verified present -> tuning pool 6,405 questions, zero leakage. README provenance corrected.

190. **Task - H353 gate FAIL: union-of-K refuted at zero cost; H363 clean ramp launched** (v0.8.73): The gold-carrier gate killed R31's central bet and re-pointed both defects<br>
    **Result**: Gate (`reports/r31-h353-gate-20260710T122224Z.json`, 33 chunks x 6 passes at c64/t3600, 197/198 clean): single-pass emission coverage **0.8077** - not the 0.55 R22-H231 measured; the enumerate recipe (H246) most plausibly closed the stochastic mass in between. Union-2 gain +0.0385 (bar +0.25), union-3 adds nothing, union-6 ceiling 0.8846; JD ratio 0.962 (bar 0.75) - unioning neither recovers coverage nor stabilizes naming. **H349 REFUTED with zero ingests** (the gate saved the ~6-ingest budget), H350 CLOSED-MOOT. Residue is deterministic: 2 golds never emitted in any pass ("AutoSet for Her", "gradually acclimate"), 1 carrier-less ("9.0W" - parse/chunk loss) - DEF-13 re-pointed at R32 chunking/context (H354 census decisive), DEF-11 at extraction-form levers; both defect entries annotated. Launched H363 clean re-ramp detached (engine `calls` counter + 3 unit tests, completion-sized windows, hidden-attempts reconciliation vs vLLM `request_success_total`, ladder 16-96, ~5.5h).

191. **Task - H354 census kills the R32 chunking round; R33 localization registered** (v0.8.73): The offline census gate closed all seven chunking hypotheses pre-build and overturned the failure theory a second time in one day<br>
    **Result**: `scripts/r32_h354_census.py` (offline, zero GPU) classified the 7 failed probes' residual mass (5.0) over the shipped chunk pool: **60% emitted-but-lost** (P09/P11/P16/P17 spec golds carried AND emitted 6/6 H353 passes, yet probes fail recall@16 - the loss is downstream in resolution/graph/ranking, contradicting H107's never-extracted theory on the current engine), **20% reader parse loss** (P08 "9.0W" absent from every extracted document text), **20% cross-document** (P15/P18 comparison golds live in different PDFs - first census draft mislabeled these as co-occurrence class; amended with an intra/cross-document split before recording). Every chunking class (seam, table-severed, subject-sparsity, emission-miss) measured 0% vs the 10% gate bar - **H355-H361 all closed pre-build** (`reports/r32-h354-census-20260710T125440Z.json`). Registered R33-H364 stage-localization census (walk each lost gold through loader/resolution/ranking/render on the freshest graph); DEF-13 annotated; caveat recorded: recall map from v2-h212 graph vs emissions measured now.

192. **Task - H364 stage walk: resolution is the fix target; stale-map drift exposed; H355 revives** (v0.8.73): The localization census assigned the emitted-but-lost mass its pipeline stage and re-anchored the failure map to a consistent graph pair<br>
    **Result**: First walk (`scripts/r33_h364_stage_census.py`) returned 50% `recovered_now` - exposing that the h212-v2 recall map was measured on a graph state that no longer exists (neo4j4 layered by H241 v1/v2 + H240b enum/mention ingests: 1094 -> 5213 entities). Measured a fresh consistent pair (recall@16 mean 0.7917, failed set MOVED: P11/P17/P18 pass, P21/P22 regressed 1.0 -> 0.0), re-derived the census, re-walked. **Verdict: `resolution_loss` 83.3% of walked mass, ranking loss ZERO** - SleepStyle dims sit only in `prop_*` of HC23x model-code entities linked PART_OF -> 'HC230 Product Range', an unmerged duplicate of the retrieved-but-empty 'HC230-Series'; retrieval finds the right subjects, all property-less and disconnected from the carriers. Registered R33-H365 (fragment merge + provenance-tagged spec hoist, offline prototype gated before engine wiring). Fresh census revives R32-H355 at 40% (P21/P22 golds verified absent from every entity surface; pinned recipe never/rarely emits them). H354 addendum + DEF-13 note recorded. H363 ramp rung 1 clean: c=16, hidden_attempts=0.

193. **Task - H365 prototype CONFIRMED: resolution repair flips both probes, recall 0.79 -> 0.85** (v0.8.73): The registered fix for H364's resolution verdict passed its bar exactly on the first measured attempt<br>
    **Result**: All 12 spec props measured UNANIMOUS across the HC231-238 children (spec-soup refutation mode structurally impossible), so `scripts/r33_h365_repair.py` hoisted them onto the two retrieved series entities ('HC230-Series', 'Sleep Style 200 Series') with an `h365_hoisted` rollback marker plus 6 additive `PART_OF {h365:true}` edges as the merge simulation - no destructive writes. Recall@16 re-measure (`reports/h365-repaired-recall-20260710T143152Z.json`): **P09 0.0 -> 1.0, P16 0.5 -> 1.0, other 22 probes bit-identical - zero regressions; mean 0.7917 -> 0.8542, fully covered 18 -> 20**. This causally validates H364's stage assignment: a resolution-only repair recovered the emitted-but-lost mass with retrieval and render untouched. Verdict + DEF-13 note recorded; engine wiring (fragment merge + unanimous-spec hoist at resolution/load, verified on the clean rebuild) tracked as the follow-on task.

194. **Task - R34+R35 registered: HippoRAG transfer + associative query memory, 10 hypotheses from 4 research agents** (v0.8.73): Two user-directed fanouts grounded by parallel Fable research agents and registered in the canonical experiments doc<br>
    **Result**: Four agents delivered: HippoRAG-1/2 mechanism cards with exact ablation sizes (PPR +13.7-16.7, query-to-triple +12.5, passage nodes +6.1, synonym +2.4, specificity +2.0, recognition filter +0.7 - rejected as weakest/costliest), a file:line KGF capability map (chunks stored but never embedded/rendered, proposition seeding feeds only dormant PPR, SIMILAR_TO created three ways and consumed by nothing), the ingest-time query-generation literature (doc2query/Doc2Query--/QuOTE/Dense X/RAPTOR/sleep-time compute - 5 papers archived with digests), and an honest Hawkins analysis (2 Numenta papers archived; prediction-as-recognition and voting testable, reference frames/temporal pooling metaphor). Registered **R34** (H366 passage nodes, H367 proposition-seeded render, H368 PPR re-ablation at benchmark scale superseding H37 conditionally, H369 specificity, H370 SIMILAR_TO expansion with H365-rollback retro-arm) and **R35** (H371 gated question nodes, H372 entity-scoped cross-document questions, H373 gap-ledger duality, H374 multi-view voting, H375 SDR identity signatures). Naive baseline: post-H365 live graph 0.8542, failed mass 3.5. Tasks #70/#71 created.

195. **Task - R36 registered: derived-object maintenance, 6 hypotheses from 2 research agents; acc-crit Embeddings; H366 goes local-GPU** (v0.8.73): User-directed maintenance fanout (research-first) registered, plus the no-Titan embeddings directive codified<br>
    **Result**: Two Fable agents delivered: external literature pass (IVM delta/DRed/self-maintainability, TMS justifications + IN/OUT labels, EraRAG locality 77.5% rebuild savings, Mem0/A-MEM/sleep-time, Drift-Adapter 95-99% recall at ~200x cheaper, Memory-Worth rho 0.89; verdict: generated-object staleness literature EMPTY, five slots open) and internal cartographer (invalidation matrix empty for ALL derived-object classes, RECURING dead-end - `end_recure` zero callers, fact-drift alarm dead by default via `functional_relationship_types=[]`). Both briefs persisted verbatim to `reports/r36-*.md` (cartographer recovered from subagent transcript post-compaction); 5 papers archived. Registered **R36** (H376 justification DAG, H377 entailment revalidation, H378 CUSUM wiring + RECURING exit = DEF-9 workstream, H379 functional-type inference, H380 outcome-linked worth, H381 gap re-probe) fenced against R28/R15-R17; task #72. Also: `docs/acc-crit-kgf.md` Embeddings section (10 criteria - provider abstraction, GPU deployment, space provenance) codifying the user's no-Titan-for-bulk directive, and H366 rewritten to bge-m3 on GPU 2 (instrument-change note in the registration; Titan kept only for the entity-index query channel).

196. **Task [Extended] - R34 closed same-evening: 2 confirmed + 2 refuted, composed frontier 23/24, DEF-14 found, R37 registered** (v0.8.73): All four offline prototypes measured, verdicts recorded, levers composed, and the user's progressive-context concept registered<br>
    **Result**: H366 CONFIRMED via three arms - full chunks lift recall 0.8542 -> 0.9583 but breach the 30% budget (+78%); head-truncation structurally lossy (golds sit > 1200 chars deep); query-anchored 900-char spans (`scripts/r34_h366_window_arm.py`) deliver the bar exactly: P21+P22 flip, zero regressions, +9.5% ctx. H367 CONFIRMED - proposition-hit entities in the render flip P15 (cross-document) at 0.8750/21; the facts-only arm dead flat proves entity BLOCKS carry the gain; wiring is one routing line (`pipeline.py:1085`). H369 REFUTED (hard s_i rerank craters to 0.4792, hands top-16 to single-chunk entities). H370 REFUTED (forward flat; retro-arm DOA - ZERO SIMILAR_TO edges on the fragment trio; the creators missed the class that cost recall). Side yield: DEF-14 - the pinned harness omits the engine's `overfetch_factor=4`; parity alone flips P21 (0.8542 -> 0.8958). Composed frontier (`reports/r34-composed-frontier-20260710T162930Z.json`): parity + prop seeds + window = **0.9583, 23/24, +27.8%, zero regressions** - only P08 (parse loss) remains. True-need decomposition (only P15/P22 = 8.3% needed escalation) seeds **R37-H382**: calibrated sufficiency-gated progressive context (user directive), task #73.

197. **Task - H382 arm 1 CONFIRMED on the free signal; H379 inference arm profiled** (v0.8.73): Two runnable-now hypothesis tests executed while the H363 ramp holds GPU 1<br>
    **Result**: Lineage papers archived first per the paper rule (Self-RAG, FLARE, Adaptive-RAG - PDFs + digests). `scripts/r37_h382_ladder.py` replayed the 3-rung ladder from the R34 caches with 2-fold cross-calibration (P15/P22 split across folds; fitted thresholds 0.7650/0.7644): pooled holdout hits the always-escalate frontier exactly - **0.9583, 23/24** - at **+6.9%** context (vs +27.8% static) and **25%** escalation rate; every registered clause met by the free H181 top-seed signal, so H382 CONFIRMED, LLM self-report arm demoted to a post-H363 precision rider. Shipped miss_threshold 0.668 escalates nothing - sufficiency needs its own operating point (~0.765). H379 inference arm (`scripts/r36_h379_cardinality.py`): `valid_to` absent graph-wide (alarm confirmed dead); only MANUFACTURED_BY clears 90% single-valued, none clear 95% - identity noise inflates fanout, naive inference under-labels; replay proceeds with {MANUFACTURED_BY}.

198. **Task - R38 registered: adaptive-gate self-calibration (H383-H387)** (v0.8.73): User directive - ship an a-priori escalation threshold, calibrate per corpus, persist calibration for future ingestions<br>
    **Result**: Research-first: two Fable briefs ground the round - external literature (`reports/r38-calibration-literature-brief-20260710.md`, 11 papers archived with digests: conformal family, Platt/Beta calibration, Thompson sampling, t-digest/KLL) and internal map (`reports/r38-calibration-internal-map-20260710.md`, file:line cartography). Headline: the fitted cut 0.765 is EXACTLY the conformal-risk-control solution at alpha=0.08 on the 24-outcome ledger, and calibration-in-graph is shipped precedent (resolver curve in KGFControl, `pipeline.py:454`). Registered in canonical doc: H383 CRC certificate (offline, runnable now), H384 two-tier state (settings prior + graph record with provenance + corpus fingerprint), H385 label harvesting (`query.answered` + `fit_gate_from_events` - the engine records signal without outcome today), H386 drift-triggered refit (Podkopaev-Ramdas monitor + quantile sketch), H387 cross-class transfer with prior warm-start (gated on #59 second corpus). Online controllers (PID/DtACI) deliberately deferred - guarantees vacuous at n=24; isotonic contraindicated (ICML 2005 + H157).

199. **Task - H383 CONFIRMED + gate wiring tranche shipped** (v0.8.73): CRC certificate verified on the H382 ledger, then the H382/H384/H385 engine wiring landed with tests<br>
    **Result**: `scripts/r38_h383_crc.py` on the H382 ledger: zero rung-inversions (monotone-loss premise holds), theta_CRC(0.08)=0.7644 - EXACTLY the fold-B fitted cut - and alpha=0.08 is the structural feasibility floor because P08 (parse loss) pins min risk at 1/24; out-of-fold miss 0.0417 <= 0.08. H383 CONFIRMED: the gate threshold now carries a distribution-free certificate plus a deterministic refit rule. Then the wiring: `escalation_gate` lever (default off) + `detect_escalation` in `graphrag.py`, `_gate_threshold()` two-tier loader (file > fingerprint-checked graph record > prior), rung-1 escalation render (prop-seeded entities scored by their prop hit so `render_budget` ranks them honestly), `query.answered`/`query.escalated` events (registry drift on `query.miss` fixed), new `graph/gate_calibration.py` (CRC fitter reproduces H383 exactly - pinned as unit tests; `fit_gate_from_events` two-pass join, floor discipline). 411 tests passed incl. live metanode round-trip (KGF_INTEGRATION=1, prior state restored). Deferred honestly: replay match clause + `optimize()` hook ride R34 tranche #70 post-H363.

200. **Task - H386 REFUTED on the within-budget clause** (v0.8.73): Two-channel drift monitor measured over 500-seed bootstrap replays at the certified cut<br>
    **Result**: `scripts/r38_h386_monitor.py` - outcome channel = mixture-LR supermartingale (Ville, H0 miss <= 0.10, delta 0.05, state mergeable by addition), signal channel = escalation-rate-at-theta vs stored class rate (3-sigma). Guarantee side perfect: benign ever-alarm 0.000 vs delta 0.05, the registered 3-ingestion benign run silent. Detection side underpowered at 24-label batches, exactly the anticipated REFUTED mode: embedder-swap breach (miss 0.125 vs tolerance 0.10) outcome-alarms in 10.8% of runs only - a 2.5-point gap needs hundreds of labels, information-theoretic, no better test fixes it; register-change (miss ~0.29) alarms 65.6% by batch 5, 29.8% within budget. Sketch channel carries the swap class instead: 97.2% detection, near-immediate at production query volumes. Fallback adopted per registration: per-ingestion CRC recompute primary (it is the H383 rule anyway), monitor kept as zero-false-alarm tripwire, sketch promoted to first detector. Report `reports/r38-h386-monitor-20260710T185118Z.json`.

201. **Task - R34 engine wiring tranche landed** (v0.8.73): Embeddings provider abstraction + H366 rung 2 in the engine + DEF-14 harness parity fix<br>
    **Result**: `embed_channel_texts` in `extraction/embeddings.py` (bedrock/local-gpu/openai in one module) with `ChannelEmbedding`/`EmbeddingChannels` settings - per-channel spaces, GPU device pinning before torch import with fail-fast on mask conflict or silent-CPU, dims derived from the model; 8 of 10 acc-crit criteria implemented, space-provenance on legacy channels + NV-Embed-v2 verification honestly partial. New `graph/passages.py`: span store reproduces the offline cache keying `{chunk_id}:900:{start}` exactly (H385 comparability), `(:KGFIndexSpace)` registry refuses mismatched query spaces before the vector query, `optimize()` builds passages under `passages_enabled`, escalation render appends the top span (rung 2 live). DEF-14 fixed: harness defaults to engine parity, `overfetch_factor` stamped per report. 418 tests + 9 live integration green. Gotcha caught: first integration test was non-hermetic - `generate_passages` walks ALL chunks, briefly creating 6,365 fake-dim passages on the dev pile; deleted, test redesigned to snapshot-delta cleanup.

202. **Task - H385 engine replay: shipped gate reproduces offline decisions** (v0.8.73): 24-probe three-arm replay through the live `_retrieve_local` on the benchmark pile, retrieval-only<br>
    **Result**: `scripts/r38_h385_engine_replay.py` (detached per compute rule) first built 4,253 KGFPassage nodes on the pile through the engine's own machinery (bge-m3, GPU 2, passage channel), then replayed all probes in three arms. Gated arm at the shipped prior 0.765 (H384 cold-start path - pile has no `gate_calibration` record) escalated EXACTLY the offline pooled-holdout set [P01,P06,P08,P12,P15,P22]: the live gate signal reproduces the offline instrument label-for-label on the escalation side. Recall gate_off 0.7917 -> gated 0.8125 = always-escalate 0.8125, but at +5.0% context vs +20.4% - the gate captures the full recall gain at a quarter of the cost, zero regressions; sole mover P12 (0.5 -> 1.0 via the appended span). Caveat: pile lacks `kgf_proposition_embeddings`, so rung 1 never fired - additive-vs-displacement parity unadjudicated, gains are pure gate + rung 2. Report `reports/r38-h385-engine-replay-20260710T200750Z.json`.

203. **Task - H365 engine wiring: series bridge + spec hoist** (v0.8.73): Generalized the confirmed prototype repair into `graph/hoist.py` under the `resolution.spec_hoist` lever<br>
    **Result**: Corpus-agnostic rules derived from the prototype's verified semantics: `bridge_series_fragments` calls two entities fragments of one series only when they share a model-code stem AND both names carry a series marker (series/range/family/line) - the marker guard exists because the pile shows the fragments sit at cosine 0.7477, below every resolver threshold, so embeddings can never find this merge; `hoist_unanimous_specs` copies `prop_*` unanimous across ALL children onto series-marker parents only, preventing component-to-product false hoists (battery specs onto a device). Runs at `optimize()` BEFORE proposition generation because propositions derive from `prop_*` - hoisted specs materialize as propositions. No-overwrite + accumulating `spec_hoisted` marker make the pass exactly reversible; the integration test exploits that for bit-identical rollback (dev instance verified zero residue). 428 unit + 10 live integration green. Clean-rebuild verification clause rides the goal-terminal rebuild.

204. **Task - R36 offline arms: H376 CONFIRMED, H380 REFUTED** (v0.8.73): The two non-LLM maintenance hypotheses measured on the pile while the H363 ramp holds GPU 1<br>
    **Result**: H376 (justification substrate): provenance is thick enough to reconstruct field-level justifications for every stored derived-object class (5213/5213 descriptions, 573/573 SIMILAR_TO, 4253/4253 passages) EXCEPT hoist-derived props (0/2 - `spec_hoisted` names keys, not sources) - measured evidence for the creation-time-justification acc-crit rule. Dirty flood on the two registered events: coverage 1.0, false/true 0.25 (bar 2x), E1 prop-change flags zero SIMILAR_TO (field-level discipline), OUT-labeling all 148 flagged entities regresses only P09/P16 whose golds ARE the flagged specs - unaffected probes flat. H380 (worth counters): 96 episodes, 278 entities priced; bottom-decile evacuation costs exactly zero recall, but the superseded carrier gets ZERO retrieval traffic so the staleness clause cannot fire - REFUTED at harness density per the registration's own anticipated mode; bottom decile turns out to be failure-cluster neighbors (PowerConsumption in P08's orbit), gap signal not staleness signal. Reports `r36-h376-justifications-*.json`, `r36-h380-worth-*.json`.

205. **Task - R35 offline arms: H375 REFUTED, H374 deferred to precision** (v0.8.73): SDR identity signatures head-to-head + three-channel consensus voting, both on the pile<br>
    **Result**: H375's first pass looked like an SDR win (fragment pairs rank 40/41) but was CONTAMINATION - the H365 repair marks on the pile create exactly the shared-neighbour tokens the signature needs; the honest pre-repair arm (h365 edges excluded, hoisted props stripped) puts the HC230 pair at rank 58,062/59,761 and the SleepStyle pair off the candidate list entirely - pre-repair the fragment class is structurally token-poor, which is WHY nothing caught it. Benchmark recall mixed (cosine wins at scale), +10% bar failed, Hawkins substrate retired per registration; corollary kept: token signatures VERIFY repairs cheaply (top-41 post-repair) but cannot DISCOVER them. Also found: 54/68 forensics pairs no longer name-resolvable (benchmark decay from layered resolution). H374: consensus reorders genuinely (Spearman 0.55, turnover 8-10/16) at bit-identical recall 0.8958 both arms - not theater, tail-fungibility finding; precision clause rides the LLM judge post-H363. Reports `r35-h375-sdr-*.json`, `r35-h374-consensus-*.json`.

206. **Task - H378 wired: CUSUM trigger + RECURING exit (DEF-9 mechanics)** (v0.8.73): The two R28 corpses closed in the engine behind `drift.cusum_enabled`<br>
    **Result**: H317's CUSUM ported verbatim into `DriftDetector._cusum_evaluate` - one-sided S=max(0,S+(x-(mu0+k))) over the dense per-doc JSD series, mu0 from the first window's median, H317 constants k=0.02/h=0.30, reset-on-fire, state in to_dict/from_dict so the left-fold survives sessions. RECURING exit: `end_recure` gets its first production caller - after `window` post-recure docs, `adopt_drifted_types` adopts the register's sustained types (>=`recure_adopt_share` of window mass) into the cured ontology, detector re-baselines on the observed window, `lifecycle.cure()` walks back to STABLE. Unit verdicts pin the DEF-9 anatomy both ways: the anti-phase case (drifted register, sub-threshold remap) fires CUSUM and provably cannot fire the boolean; livelock guard holds (same register silent post-exit). 437 tests green. DEF-9 stays open pending the induced-drift replay (post-H363).

207. **Task - docs consolidation and currency audit** (v0.8.73): Reorganized docs/ per user placement calls and audited the orientation docs against the live codebase while the H363 ramp holds the GPU<br>
    **Result**: Moved to `docs/@archive/`: `sota-decision.md` (pre-campaign research rationale, superseded by the promotions ledger and the future canonical SOTA doc), `h198-wiring-plan.md` (all four decision slots RESOLVED, tranches merged - consumed plan), `DESIGN.md` (user call). Relocated living registers: `docs/defects/defects.md`, `docs/acceptance-criteria/acc-crit-kgf.md`; verified no relative links broke. Four-agent opus workflow audited the remaining docs code-grounded: `gap-ledger.md` CURRENT; `usage-scenarios.md` fixed - S1 claimed PPR as its retrieval carrier but `ppr_enabled` defaults False (H37-refuted theater, `settings.py:163`), replaced with the shipped vector-seed + proposition path; S9 updated to the shipped KGFIndexSpace space-refusal (passage channel live). `api.md` removed on user call (git rm; rewrite post-RC) - findings parked in `scratchpad/api-md-audit-findings-20260710.md` (missing: repair/repurpose methods, CUSUM knobs, escalation-gate and passages surface, embedding channels, resolution levers).
